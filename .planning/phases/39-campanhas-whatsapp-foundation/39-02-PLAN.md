---
phase: 39-campanhas-whatsapp-foundation
plan: 02
type: execute
wave: 2
depends_on: ["39-01", "38-01"]
files_modified:
  - src/app/campanhas/nova/page.tsx
  - src/app/campanhas/[id]/editar/page.tsx
  - src/db/schema.ts
autonomous: true
requirements: [WA-05, WA-06, WA-07, WA-08]

must_haves:
  truths:
    - "Campaign create page uses shared WhatsAppPreview component instead of inline duplicate"
    - "Campaign edit page uses shared WhatsAppPreview component instead of inline duplicate"
    - "Message textarea allows 65536 chars (not 100 char limit)"
    - "WhatsApp preview shows the selected chip's profile name and photo"
    - "Campaign schema has startDate and endDate fields"
  artifacts:
    - path: "src/app/campanhas/nova/page.tsx"
      provides: "Campaign creation page with shared WhatsApp preview"
      contains: "WhatsAppPreview"
    - path: "src/app/campanhas/[id]/editar/page.tsx"
      provides: "Campaign edit page with shared WhatsApp preview"
      contains: "WhatsAppPreview"
  key_links:
    - from: "src/app/campanhas/nova/page.tsx"
      to: "src/components/whatsapp-preview.tsx"
      via: "import WhatsAppPreview"
      pattern: "WhatsAppPreview"
    - from: "src/app/campanhas/nova/page.tsx"
      to: "/api/chips"
      via: "fetch chip profiles for preview"
      pattern: "api/chips"
---

<objective>
Replace the duplicated inline WhatsApp previews in campaign create/edit pages with the shared WhatsAppPreview component, fix the 100-char message limit, integrate chip profile selection into the preview, and add campaign date range fields to the schema.

Purpose: Campaign pages currently have duplicated WhatsApp preview code with hardcoded "EEL Eleição" branding and a 100-char message limit (WhatsApp supports 65,536). This plan replaces those with the shared component and connects it to real chip profiles.

Output: Campaign editor pages using shared WhatsApp preview with chip profiles, and campaign date range in DB.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-campanhas-whatsapp-foundation/39-01-SUMMARY.md
@.planning/phases/38-chip-profile-management/38-01-SUMMARY.md
@src/app/campanhas/nova/page.tsx
@src/app/campanhas/[id]/editar/page.tsx
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline previews with shared WhatsAppPreview and fix message limits</name>
  <files>
    src/app/campanhas/nova/page.tsx
    src/app/campanhas/[id]/editar/page.tsx
  </files>
  <action>
  1. **Campaign create page (nova/page.tsx, 694 lines):**
     - Remove the entire inline WhatsApp preview JSX (look for the duplicated phone-frame rendering code with hardcoded "EEL Eleição" and "EE" avatar)
     - Replace with: `<WhatsAppPreview message={template} profileName={selectedChipProfile?.profileName} profilePictureUrl={selectedChipProfile?.profilePictureUrl} />`
     - The page already has chip selection via SendConfigPanel (selectedChipIds). When a chip is selected, fetch its profile data and pass to WhatsAppPreview
     - Add state: `const [selectedChipProfile, setSelectedChipProfile] = useState<{profileName?: string; profilePictureUrl?: string} | null>(null)`
     - When selectedChipIds changes (from SendConfigPanel), fetch the first chip's profile from the chips data
     - The chips list is likely already fetched for SendConfigPanel — reuse that data

  2. **Fix message textarea limit:**
     - Find the textarea maxLength prop (currently set to 100)
     - Change to 65536 (WhatsApp's actual limit)
     - Update any character counter display to work with the new limit
     - If there's a word counter (120 word limit mentioned in CAMP-02), keep that as a soft warning, not a hard limit

  3. **Campaign edit page (editar/page.tsx):**
     - Same treatment: remove inline WhatsApp preview, replace with shared `<WhatsAppPreview>` component
     - Same chip profile integration
     - Same message limit fix
     - The edit page should load the campaign's selectedChipIds and show the corresponding chip profile in the preview

  4. **Live preview updates:**
     - The preview should update in real-time as the user types (this likely already works since template is state)
     - WhatsApp formatting (*bold*, _italic_) should render in the preview as the user types
     - Variable placeholders ({nome}, {candidato}) should show as highlighted chips in the preview (or literal text if not resolved)

  Import WhatsAppPreview from 'src/components/whatsapp-preview'.
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/integration-evolution-wizard && npx next build 2>&1 | tail -5</automated>
    <manual>Campaign create/edit pages show WhatsApp preview with selected chip's profile, textarea accepts long messages</manual>
  </verify>
  <done>
  - Campaign create page uses shared WhatsAppPreview (no inline duplicate)
  - Campaign edit page uses shared WhatsAppPreview (no inline duplicate)
  - Message textarea limit is 65536 chars (not 100)
  - Preview shows selected chip's profileName and profilePictureUrl
  - Live preview updates as user types with formatting
  - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Add campaign date range to schema and creation form</name>
  <files>
    src/db/schema.ts
    src/app/campanhas/nova/page.tsx
    src/app/campanhas/[id]/editar/page.tsx
  </files>
  <action>
  1. **Schema update** — Add to campaigns table in src/db/schema.ts:
     - `startDate: timestamp('start_date')` — Campaign start date (when it becomes active)
     - `endDate: timestamp('end_date')` — Campaign end date (when it stops)
     These are nullable — campaigns can still be one-shot without a date range.

  2. **Migration** — Create `drizzle/0016_campaign_date_range.sql`:
     ```sql
     ALTER TABLE campaigns ADD COLUMN start_date TIMESTAMP;
     ALTER TABLE campaigns ADD COLUMN end_date TIMESTAMP;
     ```

  3. **Campaign create form** — Add a "Período da Campanha" section to nova/page.tsx:
     - Two date picker inputs: "Data de início" and "Data de fim"
     - Use native `<input type="date">` or shadcn DatePicker if available
     - Optional fields — can be left empty for one-shot campaigns
     - Validation: endDate must be after startDate if both are set
     - Persist to campaign record on save

  4. **Campaign edit form** — Same date range fields in editar/page.tsx:
     - Load existing startDate/endDate from campaign record
     - Allow editing
     - Same validation

  5. **db-campaigns.ts** — Update createCampaign and updateCampaign to accept and persist startDate/endDate.

  6. **Campaign API** — Verify PUT /api/campaigns handles the new fields.
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/integration-evolution-wizard && npx next build 2>&1 | tail -5</automated>
    <manual>Campaign create/edit pages have date range fields, persisted to DB</manual>
  </verify>
  <done>
  - campaigns table has startDate and endDate columns
  - Migration 0016 exists
  - Campaign create form has "Período da Campanha" with date pickers
  - Campaign edit form loads and saves date range
  - Validation: endDate > startDate when both set
  - API and DB layer updated
  - Build succeeds
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Campaign pages use shared WhatsApp preview with chip profiles
- Date range fields present and functional
</verification>

<success_criteria>
Campaign editors use the shared WhatsApp preview with chip profiles, 65K char limit, and date range support.
</success_criteria>

<output>
After completion, create `.planning/phases/39-campanhas-whatsapp-foundation/39-02-SUMMARY.md`
</output>
