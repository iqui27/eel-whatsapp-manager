---
phase: 21
plan: 07
type: feature
name: Message History & Analytics
autonomous: true
wave: 4
depends_on: [21-02, 21-05, 21-06]
requirements: []
---

# Plan 21-07: Message History & Analytics

## Objective
Criar histórico completo de mensagens com busca, filtros e analytics para auditoria e análise.

## Tasks

### Task 1: Create message history page
type: auto

**Implementation:**
1. Create `/historico` page
2. Table with pagination
3. Filters: date range, chip, campaign, status
4. Sort by date, status, chip
5. Export functionality

**Files:**
- `src/app/historico/page.tsx`
- `src/components/message-history-table.tsx`

---

### Task 2: Add search functionality
type: auto

**Implementation:**
1. Search by phone number
2. Search by voter name
3. Search by message content
4. Full-text search with highlights
5. Debounced input

**Files:**
- `src/app/api/messages/search/route.ts`
- `src/components/message-search.tsx`

---

### Task 3: Add export functionality
type: auto

**Implementation:**
1. Export to CSV
2. Export to PDF (with formatting)
3. Export filtered results
4. Include delivery status
5. Include timestamps

**Files:**
- `src/app/api/messages/export/route.ts`
- `src/lib/export-messages.ts`

---

### Task 4: Create analytics charts
type: auto

**Implementation:**
1. Delivery timeline chart (hourly/daily)
2. Response rate by segment
3. Conversion funnel visualization
4. Chip performance comparison
5. Best send times analysis

**Files:**
- `src/components/message-analytics-charts.tsx`
- `src/app/api/messages/analytics/route.ts`

---

### Task 5: Add per-campaign message logs
type: auto

**Implementation:**
1. Campaign detail page shows all messages
2. Filter by status
3. Show delivery timeline per message
4. Link to conversation if reply received
5. Export campaign messages

**Files:**
- `src/app/campanhas/[id]/mensagens/page.tsx`

---

### Task 6: Add per-chip message logs
type: auto

**Implementation:**
1. Chip detail page shows all messages sent
2. Show health events alongside messages
3. Filter by date range
4. Show daily/hourly usage
5. Export chip logs

**Files:**
- `src/app/chips/[id]/mensagens/page.tsx`

---

### Task 7: Create history API endpoints
type: auto

**Implementation:**
1. GET /api/messages/history - paginated list
2. GET /api/messages/search - full-text search
3. GET /api/messages/export - CSV/PDF export
4. GET /api/messages/analytics - aggregated stats
5. Optimize queries with indexes

**Files:**
- `src/app/api/messages/history/route.ts`
- `src/app/api/messages/search/route.ts`
- `src/app/api/messages/export/route.ts`
- `src/app/api/messages/analytics/route.ts`

---

## Success Criteria

- [ ] Message history page with filters
- [ ] Search finds messages in < 3 seconds
- [ ] Export to CSV and PDF works
- [ ] Analytics charts show useful insights
- [ ] Campaign and chip logs accessible
- [ ] API endpoints performant