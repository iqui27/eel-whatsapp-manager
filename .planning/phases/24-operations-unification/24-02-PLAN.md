---
phase: 24-operations-unification
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - src/components/quick-actions-panel.tsx
  - src/components/help-panel.tsx
  - src/app/operacoes/page.tsx
autonomous: true
requirements:
  - OPSV2-05
  - OPSV2-06
  - OPSV2-07
  - OPSV2-08

must_haves:
  truths:
    - "QuickActionsPanel shows contextual info (e.g. '3 chips offline') next to each action"
    - "QuickActionsPanel import link points to /segmentacao/importar (not /segmentacao)"
    - "HelpPanel has a real support email (not suporte@exemplo.com)"
    - "HelpPanel help links point to actual documentation or in-app guides"
    - "Operations page uses latest component versions with consistent card styling"
  artifacts:
    - path: "src/components/quick-actions-panel.tsx"
      provides: "Contextual quick actions with dynamic counts"
      contains: "segmentacao/importar"
    - path: "src/components/help-panel.tsx"
      provides: "Help panel with real contact info and proper links"
    - path: "src/app/operacoes/page.tsx"
      provides: "Unified operations page with consistent component versions"
  key_links:
    - from: "src/components/quick-actions-panel.tsx"
      to: "operacoes/page.tsx"
      via: "Receives system state as props for contextual info"
      pattern: "QuickActionsPanel"
    - from: "src/app/operacoes/page.tsx"
      to: "All operations components"
      via: "Imports and renders all operations sub-components"
      pattern: "import.*from.*components"
---

<objective>
Complete operations professionalization: contextual quick actions, real help content, and unified page layout.

Purpose: QuickActionsPanel currently shows static labels like "Importar Eleitores" without context — it should show "Importar Eleitores (1.234 eleitores)" with dynamic counts. The import link goes to /segmentacao instead of /segmentacao/importar. HelpPanel has placeholder content (suporte@exemplo.com). The operations page needs consistent card styling and spacing across all sub-components.

Output: Fully professional operations page with contextual actions, real help content, and consistent visual quality
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/components/quick-actions-panel.tsx
@src/components/help-panel.tsx
@src/app/operacoes/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add contextual info to QuickActions + fix HelpPanel content</name>
  <files>src/components/quick-actions-panel.tsx, src/components/help-panel.tsx</files>
  <action>
**QuickActionsPanel (quick-actions-panel.tsx):**

1. Add a `contextInfo` optional field to the `QuickAction` interface:
   ```ts
   contextInfo?: string; // e.g., "3 chips offline", "1.234 eleitores"
   ```

2. Add a `systemContext` prop to `QuickActionsPanelProps`:
   ```ts
   interface QuickActionsPanelProps {
     actions?: QuickAction[];
     systemContext?: {
       chipsOffline?: number;
       chipsTotal?: number;
       voterCount?: number;
       groupCount?: number;
       campaignCount?: number;
     };
   }
   ```

3. When rendering each action, if `systemContext` is provided, dynamically compute contextual info:
   - `create-campaign`: Show `"${campaignCount} campanhas"` 
   - `add-chip`: Show `"${chipsOffline} offline de ${chipsTotal}"` if offline > 0, else `"${chipsTotal} conectados"`
   - `import-voters`: Show `"${voterCount.toLocaleString('pt-BR')} eleitores"`
   - `sync-groups`: Show `"${groupCount} grupos"`
   - `view-reports`: Show `"Ver relatórios"`

4. Fix the import link: Change `href: '/segmentacao'` → `href: '/segmentacao/importar'` for the `import-voters` action.

5. Fix Portuguese accents in labels: `'Ver Relatorios'` → `'Ver Relatórios'`

6. Render the contextual info as a small `text-xs text-muted-foreground` line below each action label.

**HelpPanel (help-panel.tsx):**

1. Replace `suporte@exemplo.com` with `suporte@eel.app` (or whatever the real domain is — check if there's a support email configured anywhere, otherwise use `suporte@eel.app`).

2. Fix FAQ Portuguese accents:
   - `'pagina'` → `'página'` (multiple occurrences)
   - `'Chips'` is fine (proper noun)
   - `'instrucoes'` → `'instruções'`
   - `'e a troca automatica'` → `'é a troca automática'`
   - `'saudavel'` → `'saudável'`
   - `'Quando um grupo atinge'` is fine
   - `'Va para'` → `'Vá para'`

3. Update help links to point to relevant in-app pages (they currently already do — just verify they're correct).
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | grep -v "Cannot find module '@/lib/notifications'" | grep -v "Cannot find module '@/lib/group-link-cache'" | grep -c "error TS" || echo "0 errors"</automated>
    <manual>Check that quick-actions shows contextual counts when systemContext is passed. Check help-panel has no "exemplo.com" references.</manual>
  </verify>
  <done>QuickActionsPanel shows contextual info per action. Import link fixed to /segmentacao/importar. HelpPanel has real support email and proper Portuguese accents.</done>
</task>

<task type="auto">
  <name>Task 2: Unify operations page with consistent component versions and styling</name>
  <files>src/app/operacoes/page.tsx</files>
  <action>
1. **Pass systemContext to QuickActionsPanel**: In the operations page, construct the `systemContext` object from the fetched `opsData` and pass it to `<QuickActionsPanel>`:
   ```tsx
   <QuickActionsPanel systemContext={{
     chipsOffline: opsData.chips.filter(c => c.healthStatus === 'disconnected' || c.healthStatus === 'offline').length,
     chipsTotal: opsData.chips.length,
     voterCount: opsData.systemStatus?.voterCount ?? 0,
     groupCount: opsData.groups.length,
     campaignCount: opsData.campaigns.length,
   }} />
   ```
   Note: If `voterCount` is not in opsData, add a fetch to `/api/voters?limit=1` in the existing `fetchData` function to get the total.

2. **Ensure consistent card styling**: All operations sub-components should be wrapped in the same `<Card>` pattern. Check that each section (ChipHealthGrid, CampaignProgressBars, GroupCapacityGrid, ConversionKPIs, MessageFeed) is inside a Card with consistent `<CardHeader>` + `<CardContent>` structure. Fix any that don't follow this pattern.

3. **Fix spacing**: Ensure all grid gaps are consistent — use `gap-6` between major sections and `gap-4` within sections.

4. **Pass autoRefresh={true} to MessageFeed**: The operations page should enable auto-refresh on the message feed since it's the operations monitoring center:
   ```tsx
   <MessageFeed messages={opsData.messages} autoRefresh={true} refreshInterval={10000} />
   ```

5. **Fix any remaining Portuguese accent issues** in the page itself (check all string literals).
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | grep -v "Cannot find module '@/lib/notifications'" | grep -v "Cannot find module '@/lib/group-link-cache'" | grep -c "error TS" || echo "0 errors"</automated>
    <manual>Load /operacoes — all components should have consistent card styling, contextual quick actions, and no text overflow</manual>
  </verify>
  <done>Operations page has consistent card styling, contextual quick actions with dynamic counts, auto-refreshing message feed, and uniform spacing.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit` passes (ignoring known pre-existing errors)
- `grep -n "exemplo.com" src/components/help-panel.tsx` returns nothing
- `grep -n "href.*'/segmentacao'" src/components/quick-actions-panel.tsx` shows only `/segmentacao/importar`
- Operations page renders all sections without text overflow
</verification>

<success_criteria>
- QuickActionsPanel shows dynamic contextual information for each action
- Import link corrected to /segmentacao/importar
- HelpPanel has real support email and proper Portuguese accents
- Operations page has consistent Card layout, uniform spacing, and autoRefresh enabled on MessageFeed
- No TypeScript compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/24-operations-unification/24-02-SUMMARY.md`
</output>
