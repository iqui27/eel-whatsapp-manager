---
phase: 30-header-cleanup
plan: 02
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
  - 25-01
  - 25-02
  - 25-03
  - 26-01
  - 26-02
  - 27-01
  - 27-02
  - 28-01
  - 28-02
  - 29-01
  - 29-02
  - 30-01
files_modified:
  - src/app/history/page.tsx
  - src/app/history/loading.tsx
  - src/app/contacts/page.tsx
  - src/app/contacts/loading.tsx
  - src/app/clusters/page.tsx
  - src/app/clusters/loading.tsx
  - src/components/help-panel.tsx
  - src/components/SidebarLayout.tsx
  - src/app/operacoes/page.tsx
  - src/app/segmentacao/page.tsx
autonomous: true
requirements:
  - CLN-01
  - CLN-02
  - CLN-03
  - CLN-04

must_haves:
  truths:
    - "Only /historico exists for message history — /history is removed or redirects"
    - "/contacts redirects to /crm, /clusters is removed"
    - "No placeholder content remains (suporte@exemplo.com replaced)"
    - "Every page has SidebarLayout, error toasts, loading states, empty states, Portuguese accents, no emoji indicators"
  artifacts:
    - path: "src/app/history/page.tsx"
      provides: "Redirect to /historico (or deleted)"
      contains: "redirect"
    - path: "src/app/contacts/page.tsx"
      provides: "Redirect to /crm"
      contains: "redirect"
    - path: "src/components/help-panel.tsx"
      provides: "Real support email (not suporte@exemplo.com)"
      contains: "suporte@eel.app"
  key_links:
    - from: "src/components/SidebarLayout.tsx"
      to: "/historico"
      via: "Legacy nav item points to /historico (not /history)"
      pattern: "href.*historico"
---

<objective>
Final cleanup sweep — remove deprecated routes (/history, /clusters), redirect /contacts to /crm, fix placeholder content, fix all missing Portuguese accents, and audit every page for consistency (SidebarLayout, toasts, loading states, empty states, no emoji indicators).

Purpose: After all individual page overhauls (Phases 24-29), this final plan catches cross-cutting issues: duplicate routes that confuse users, placeholder emails that look unprofessional, missing accents that look careless, and any page that slipped through without the standard UX patterns (SidebarLayout wrapper, error handling, loading states, empty states).

Output: Clean route structure, no placeholders, proper accents everywhere, consistent UX patterns across all pages
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/app/history/page.tsx
@src/app/contacts/page.tsx
@src/app/clusters/page.tsx
@src/components/help-panel.tsx
@src/components/SidebarLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deprecated routes and fix placeholder content</name>
  <files>src/app/history/page.tsx, src/app/history/loading.tsx, src/app/contacts/page.tsx, src/app/contacts/loading.tsx, src/app/clusters/page.tsx, src/app/clusters/loading.tsx, src/components/help-panel.tsx, src/components/SidebarLayout.tsx</files>
  <action>
1. **Remove /history route** — replace `src/app/history/page.tsx` content with a redirect to /historico:
   ```tsx
   import { redirect } from 'next/navigation';
   export default function HistoryRedirect() {
     redirect('/historico');
   }
   ```
   Also simplify `src/app/history/loading.tsx` to just return null (or delete content and replace with simple loading).

2. **Redirect /contacts to /crm** — replace `src/app/contacts/page.tsx` content with:
   ```tsx
   import { redirect } from 'next/navigation';
   export default function ContactsRedirect() {
     redirect('/crm');
   }
   ```
   Simplify `src/app/contacts/loading.tsx` similarly.

3. **Remove /clusters** — replace `src/app/clusters/page.tsx` content with a redirect to the dashboard:
   ```tsx
   import { redirect } from 'next/navigation';
   export default function ClustersRedirect() {
     redirect('/');
   }
   ```
   Simplify `src/app/clusters/loading.tsx` similarly.

4. **Fix help-panel.tsx placeholder email** — on line 108, replace `suporte@exemplo.com` default with `suporte@eel.app`.

5. **Update SidebarLayout.tsx** — verify the legacy nav items:
   - `history` nav item (line 88) should point to `/historico` (it already does: `href: '/historico'` — verify this is correct)
   - Consider removing `contacts` and `clusters` from legacyNavItems if they exist (check if they're in the nav — based on line 83-90 they are NOT in legacyNavItems, which is correct)
   - The `'contacts'` and `'clusters'` entries in PageId type can remain for backward compatibility but are no longer navigable

6. **Search for any other instances of `suporte@exemplo.com`** across the entire codebase and replace with `suporte@eel.app`.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | grep -v "Cannot find module '@/lib/notifications'" | grep -v "Cannot find module '@/lib/group-link-cache'" | grep -c "error TS" || echo "0 errors"</automated>
    <manual>1) Navigate to /history — redirects to /historico. 2) Navigate to /contacts — redirects to /crm. 3) Navigate to /clusters — redirects to /. 4) Search codebase for "exemplo.com" — zero results.</manual>
  </verify>
  <done>/history redirects to /historico. /contacts redirects to /crm. /clusters redirects to /. All instances of suporte@exemplo.com replaced with suporte@eel.app.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Portuguese accents and cross-page consistency audit</name>
  <files>src/app/operacoes/page.tsx, src/app/segmentacao/page.tsx, src/components/SidebarLayout.tsx</files>
  <action>
1. **Fix missing Portuguese accents** — search all .tsx files for unaccented Portuguese words and fix them:
   
   | Current (wrong) | Correct | Files to check |
   |-----------------|---------|----------------|
   | `Operacoes` | `Operações` | operacoes/page.tsx (line 239, 254), SidebarLayout.tsx |
   | `Segmentacao` | `Segmentação` | segmentacao/page.tsx (line 619), SidebarLayout.tsx |
   | `Configuracoes` | `Configurações` | SidebarLayout.tsx nav label |
   | `Historico` | `Histórico` | SidebarLayout.tsx nav label |
   | `Relatorios` | `Relatórios` | SidebarLayout.tsx nav label |
   | `Conversao` | `Conversão` | relatorios/page.tsx if present |
   | `Saudavel` | `Saudável` | any health status labels |
   | `Atencao` | `Atenção` | any alert labels |
   
   **Important**: Only fix user-visible strings (labels, titles, descriptions, pageTitle props). Do NOT change:
   - Route paths (`/operacoes`, `/segmentacao` — these must stay unaccented in URLs)
   - PageId type values (`'operacoes'`, `'segmentacao'` — these are code identifiers)
   - Variable names, function names, CSS classes
   
   In SidebarLayout.tsx, the `label` property of nav items is what users see:
   - Line 73: `label: 'Operacoes'` → `label: 'Operações'`
   - Line 75: `label: 'Segmentacao'` → `label: 'Segmentação'`
   - Line 79: `label: 'Relatorios'` → `label: 'Relatórios'`
   - Line 88: `label: 'Historico'` → `label: 'Histórico'`
   - Line 89: `label: 'Configuracoes'` → `label: 'Configurações'`
   
   In operacoes/page.tsx:
   - Line 239: `pageTitle="Operacoes"` → `pageTitle="Operações"`
   - Line 254: `pageTitle="Operacoes"` → `pageTitle="Operações"`
   
   In segmentacao/page.tsx:
   - Line 619: `pageTitle="Segmentacao"` → `pageTitle="Segmentação"`

2. **Cross-page consistency audit** — verify each page file has these patterns. For each page that's missing something, note it for the executor to fix:
   
   **Checklist per page:**
   - [ ] Wrapped in `<SidebarLayout currentPage="..." pageTitle="...">`
   - [ ] Has error handling (try/catch on API calls, toast.error or error state)
   - [ ] Has loading state (skeleton or spinner while data loads)
   - [ ] Has empty state (meaningful message when no data, not blank screen)
   - [ ] Portuguese accents correct in user-visible strings
   - [ ] No emoji used as status indicators (dots, badges, text labels instead)
   - [ ] No text truncation without tooltip or overflow handling
   
   **Pages to audit** (the executor should open each and verify the checklist):
   - `/` (dashboard) — src/app/page.tsx
   - `/operacoes` — src/app/operacoes/page.tsx
   - `/campanhas` — src/app/campanhas/page.tsx
   - `/segmentacao` — src/app/segmentacao/page.tsx
   - `/conversas` — src/app/conversas/page.tsx
   - `/crm` — src/app/crm/page.tsx
   - `/compliance` — src/app/compliance/page.tsx
   - `/relatorios` — src/app/relatorios/page.tsx
   - `/historico` — src/app/historico/page.tsx
   - `/admin` — src/app/admin/page.tsx
   - `/chips` — src/app/chips/page.tsx
   - `/grupos` — src/app/grupos/page.tsx
   - `/wizard` — src/app/wizard/page.tsx
   - `/configuracoes` — src/app/configuracoes/page.tsx (new from 29-01)
   - `/perfil` — src/app/perfil/page.tsx (new from 29-02)

3. **Fix any issues found in the audit**. Common fixes:
   - Missing SidebarLayout → wrap page content
   - Missing error toast → add try/catch with toast.error
   - Missing empty state → add conditional rendering
   - Missing accents → fix user-visible strings
   - Emoji indicators → replace with colored dots or text badges
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | grep -v "Cannot find module '@/lib/notifications'" | grep -v "Cannot find module '@/lib/group-link-cache'" | grep -c "error TS" || echo "0 errors"</automated>
    <manual>1) Check sidebar — all nav labels have proper Portuguese accents (Operações, Segmentação, Relatórios, Histórico, Configurações). 2) Visit each page — no blank screens, error states handled, loading states present.</manual>
  </verify>
  <done>All user-visible Portuguese strings have proper accents. Every page passes the consistency checklist: SidebarLayout wrapper, error handling, loading states, empty states, no emoji indicators, no text truncation.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit` passes (ignoring known pre-existing errors)
- /history → redirects to /historico
- /contacts → redirects to /crm  
- /clusters → redirects to /
- Zero instances of `suporte@exemplo.com` in codebase
- Sidebar labels show proper accents: Operações, Segmentação, Relatórios, Histórico, Configurações
- All 15 pages pass consistency audit checklist
</verification>

<success_criteria>
- Deprecated routes redirect properly (/history → /historico, /contacts → /crm, /clusters → /)
- No placeholder emails remain
- All user-visible Portuguese strings have proper accents
- Every page has: SidebarLayout, error handling, loading state, empty state, no emoji indicators
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/30-header-cleanup/30-02-SUMMARY.md`
</output>
