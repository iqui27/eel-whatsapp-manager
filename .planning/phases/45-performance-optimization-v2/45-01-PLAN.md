---
phase: 45-performance-optimization-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/crm/page.tsx
  - src/app/conversas/page.tsx
  - src/app/campanhas/page.tsx
  - src/app/chips/page.tsx
  - src/app/grupos/page.tsx
  - src/app/segmentacao/page.tsx
  - src/app/relatorios/page.tsx
  - src/app/logs/page.tsx
  - src/lib/use-swr.ts
autonomous: true
requirements:
  - PERF-45-03
user_setup: []

must_haves:
  truths:
    - "All page components use useSWR for data fetching instead of useEffect+fetch"
    - "API calls are automatically deduplicated across navigation"
    - "Loading and error states handled consistently via SWR"
    - "No more manual fetch calls in useEffect hooks for data fetching"
  artifacts:
    - path: "src/lib/use-swr.ts"
      provides: "Shared SWR fetcher and configuration"
      exports: ["fetcher", "swrConfig"]
    - path: "src/app/page.tsx"
      provides: "Dashboard with SWR-based data fetching"
      contains: "useSWR"
    - path: "src/app/crm/page.tsx"
      provides: "CRM with SWR-based voter list"
      contains: "useSWR"
  key_links:
    - from: "src/app/page.tsx"
      to: "/api/dashboard"
      via: "useSWR('/api/dashboard', fetcher)"
      pattern: "useSWR.*dashboard"
    - from: "src/app/crm/page.tsx"
      to: "/api/voters"
      via: "useSWR('/api/voters', fetcher)"
      pattern: "useSWR.*voters"
---

<objective>
Expand SWR usage from 3 instances to 25+ instances across all data-fetching pages to eliminate redundant API calls, add automatic caching/revalidation, and standardize loading/error states.

Purpose: SWR provides automatic deduplication, caching, revalidation, and error handling - reducing boilerplate and improving performance.

Output: All data-fetching pages use useSWR with shared fetcher and configuration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-performance-optimization-v2/planner-context.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared SWR fetcher and configuration</name>
  <files>src/lib/use-swr.ts</files>
  <read_first>
    - package.json (verify SWR version)
    - src/app/relatorios/page.tsx (existing SWR usage pattern)
  </read_first>
  <behavior>
    - fetcher function handles JSON responses and errors
    - swrConfig exported with revalidation settings (60s focus, 5s interval)
    - Type-safe fetcher with generic response type
  </behavior>
  <action>
    Create src/lib/use-swr.ts with:
    
    ```typescript
    import type { SWRConfiguration } from 'swr';
    
    export const fetcher = async <T>(url: string): Promise<T> => {
      const res = await fetch(url);
      if (!res.ok) {
        const error = new Error('An error occurred while fetching the data.');
        throw error;
      }
      return res.json();
    };
    
    export const swrConfig: SWRConfiguration = {
      revalidateOnFocus: true,
      revalidateOnReconnect: true,
      dedupingInterval: 5000,
      focusThrottleInterval: 60000,
    };
    ```
    
    This provides a shared fetcher for all useSWR calls with consistent error handling and revalidation settings.
  </action>
  <verify>
    <automated>grep -n "export const fetcher" src/lib/use-swr.ts && grep -n "export const swrConfig" src/lib/use-swr.ts</automated>
  </verify>
  <done>
    - src/lib/use-swr.ts exists with exported fetcher and swrConfig
    - fetcher handles JSON parsing and error throwing
    - swrConfig has revalidation settings
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate Dashboard page to SWR</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current useEffect+fetch pattern)
    - src/lib/use-swr.ts (shared fetcher)
    - src/app/relatorios/page.tsx (existing SWR pattern for reference)
  </read_first>
  <behavior>
    - Replace useState for data with useSWR
    - Remove useEffect for data fetching
    - Keep loading state from useSWR
    - Handle errors from useSWR
  </behavior>
  <action>
    Migrate src/app/page.tsx from useEffect+fetch to useSWR:
    
    **Current pattern:**
    ```typescript
    const [data, setData] = useState(null);
    const [loading, setLoading] = useState(true);
    
    useEffect(() => {
      fetch('/api/dashboard')
        .then(res => res.json())
        .then(setData)
        .finally(() => setLoading(false));
    }, []);
    ```
    
    **Target pattern:**
    ```typescript
    import useSWR from 'swr';
    import { fetcher } from '@/lib/use-swr';
    
    const { data, error, isLoading } = useSWR('/api/dashboard', fetcher);
    ```
    
    Changes:
    1. Add `import useSWR from 'swr'`
    2. Add `import { fetcher } from '@/lib/use-swr'`
    3. Replace useState+useEffect with useSWR hook
    4. Replace `loading` with `isLoading` from useSWR
    5. Add error handling from useSWR's `error` property
    6. Remove the useEffect entirely
  </action>
  <verify>
    <automated>grep -n "useSWR" src/app/page.tsx && grep -n "from 'swr'" src/app/page.tsx</automated>
  </verify>
  <done>
    - Dashboard uses useSWR for data fetching
    - No useEffect for data fetching
    - Loading state from useSWR
    - Error handling from useSWR
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate CRM page to SWR</name>
  <files>src/app/crm/page.tsx</files>
  <read_first>
    - src/app/crm/page.tsx (current data fetching)
    - src/lib/use-swr.ts (shared fetcher)
  </read_first>
  <behavior>
    - Replace fetch calls with useSWR
    - Handle pagination with useSWR's key function
    - Maintain search/filter state
  </behavior>
  <action>
    Migrate src/app/crm/page.tsx:
    
    1. Replace voter list fetch with `useSWR('/api/voters?...' + searchParams, fetcher)`
    2. Use SWR's conditional fetching for search: `useSWR(search ? url : null, fetcher)`
    3. Replace manual loading state with `isLoading` from useSWR
    4. Remove useEffect for data fetching
    5. Add error toast on `error` from useSWR
    
    Key: Use SWR's conditional fetching pattern for search/filters to prevent unnecessary requests.
  </action>
  <verify>
    <automated>grep -n "useSWR" src/app/crm/page.tsx && ! grep -n "useEffect.*fetch" src/app/crm/page.tsx</automated>
  </verify>
  <done>
    - CRM uses useSWR for voter list
    - Search/filters trigger conditional SWR fetching
    - Loading state from useSWR
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate Conversas page to SWR</name>
  <files>src/app/conversas/page.tsx</files>
  <read_first>
    - src/app/conversas/page.tsx (current data fetching)
    - src/lib/use-swr.ts (shared fetcher)
  </read_first>
  <behavior>
    - Replace conversation queue fetch with useSWR
    - Handle SSE fallback (keep SSE for realtime, use SWR for initial load)
    - Maintain realtime updates
  </behavior>
  <action>
    Migrate src/app/conversas/page.tsx:
    
    1. Use `useSWR('/api/conversations', fetcher)` for initial queue load
    2. Keep SSE EventSource for realtime updates (don't replace with polling)
    3. Use SWR's `mutate` to update data when SSE events arrive
    4. Remove manual initial fetch in useEffect
    
    Pattern: SWR for initial hydration, SSE for realtime updates via mutate().
  </action>
  <verify>
    <automated>grep -n "useSWR.*conversations" src/app/conversas/page.tsx</automated>
  </verify>
  <done>
    - Conversas uses useSWR for initial queue load
    - SSE still handles realtime updates
    - SWR mutate called on SSE events
  </done>
</task>

<task type="auto">
  <name>Task 5: Migrate remaining pages to SWR (Campanhas, Chips, Grupos, Segmentacao, Relatorios, Logs)</name>
  <files>
    - src/app/campanhas/page.tsx
    - src/app/chips/page.tsx
    - src/app/grupos/page.tsx
    - src/app/segmentacao/page.tsx
    - src/app/relatorios/page.tsx
    - src/app/logs/page.tsx
  </files>
  <read_first>
    - Each page's current data fetching pattern
    - src/lib/use-swr.ts (shared fetcher)
  </read_first>
  <behavior>
    - All pages use useSWR for data fetching
    - Loading states from useSWR
    - Error handling from useSWR
    - No useEffect for data fetching
  </behavior>
  <action>
    For each page, apply the same migration pattern:
    
    1. Import: `import useSWR from 'swr'; import { fetcher } from '@/lib/use-swr';`
    2. Replace useState+useEffect with: `const { data, error, isLoading } = useSWR('/api/[endpoint]', fetcher);`
    3. Remove manual loading state (use `isLoading`)
    4. Add error handling (toast on `error`)
    5. For pages with filters/search, use conditional fetching: `useSWR(filter ? url : null, fetcher)`
    
    Pages to migrate:
    - campanhas: `/api/campaigns`
    - chips: `/api/chips`
    - grupos: `/api/groups`
    - segmentacao: `/api/segments`
    - relatorios: `/api/reports` (already has SWR, verify pattern)
    - logs: `/api/system-logs`
  </action>
  <verify>
    <automated>
      for file in src/app/campanhas/page.tsx src/app/chips/page.tsx src/app/grupos/page.tsx src/app/segmentacao/page.tsx src/app/logs/page.tsx; do
        echo "Checking $file:"
        grep -c "useSWR" "$file" || echo "NOT FOUND"
      done
    </automated>
  </verify>
  <done>
    - All 6 pages use useSWR for data fetching
    - Total SWR usage increased from 3 to 25+ instances
    - No useEffect+fetch patterns for data fetching
    - Consistent loading/error states across all pages
  </done>
</task>

</tasks>

<verification>
## Bundle Analysis

After migration, run:
```bash
npm run build
```

Verify:
- [ ] All pages compile without errors
- [ ] No console warnings about missing dependencies
- [ ] SWR instances count: `grep -r "useSWR" src/app --include="*.tsx" | wc -l` should be 25+
- [ ] No useEffect+fetch patterns remain: `grep -r "useEffect" src/app --include="*.tsx" | grep -c "fetch("` should be 0

## Runtime Verification

1. Start dev server: `npm run dev`
2. Open each migrated page
3. Verify data loads correctly
4. Check Network tab - requests should be deduplicated on navigation
5. Test loading states appear during fetch
6. Test error states by breaking API endpoints temporarily
</verification>

<success_criteria>
- [ ] src/lib/use-swr.ts created with shared fetcher and config
- [ ] All 10 pages migrated to useSWR
- [ ] SWR usage increased from 3 to 25+ instances
- [ ] No useEffect+fetch patterns for data fetching remain
- [ ] Loading states handled via useSWR's isLoading
- [ ] Error states handled via useSWR's error
- [ ] Build succeeds without warnings
- [ ] All pages render correctly with data
</success_criteria>

<output>
After completion, create `.planning/phases/45-performance-optimization-v2/45-01-SUMMARY.md`
</output>