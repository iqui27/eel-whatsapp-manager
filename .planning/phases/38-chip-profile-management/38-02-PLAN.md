---
phase: 38-chip-profile-management
plan: 02
type: execute
wave: 1
depends_on: ["38-01"]
files_modified:
  - src/app/chips/page.tsx
  - src/components/chip-profile-editor.tsx
autonomous: true
requirements: [CHIP-PROF-04, CHIP-PROF-05]

must_haves:
  truths:
    - "Operators can view and edit chip profile name and photo from the chips page"
    - "Profile changes are saved to both Evolution API and the local DB"
    - "Profile photo can be uploaded or set via URL"
  artifacts:
    - path: "src/components/chip-profile-editor.tsx"
      provides: "Reusable chip profile editor component"
    - path: "src/app/chips/page.tsx"
      provides: "Chips page with profile management integrated"
  key_links:
    - from: "src/components/chip-profile-editor.tsx"
      to: "/api/chips"
      via: "PUT with action=updateProfile"
      pattern: "updateProfile"
    - from: "src/app/chips/page.tsx"
      to: "src/components/chip-profile-editor.tsx"
      via: "import and render in chip detail/dialog"
      pattern: "ChipProfileEditor"
---

<objective>
Add chip profile management UI to the chips page — a profile editor component where operators can set/change the chip's WhatsApp display name and profile picture, integrated into the existing chips health dashboard.

Purpose: Operators need to configure chip profiles (name, photo) before using them in campaigns. The campaign WhatsApp preview will show the chip's profile, so profiles must be set up first.

Output: A ChipProfileEditor component and chips page integration.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/chips/page.tsx
@.planning/phases/38-chip-profile-management/38-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChipProfileEditor component</name>
  <files>src/components/chip-profile-editor.tsx</files>
  <action>
  Create a reusable ChipProfileEditor component at src/components/chip-profile-editor.tsx:

  **Props:**
  ```typescript
  interface ChipProfileEditorProps {
    chip: { id: string; instanceName: string; profileName?: string; profilePictureUrl?: string };
    onSave?: () => void; // callback after successful save
  }
  ```

  **UI Layout (V2 Editorial Light):**
  - Card with warm background (bg-[#F8F6F1]), warm border
  - Profile picture section:
    - Current photo displayed as a 64x64 rounded-full avatar (fallback: initials from profileName or instanceName)
    - "Alterar foto" button that opens a Dialog with:
      - URL input field (for pasting a URL to an image)
      - File upload button (reads file, converts to base64 for Evolution API)
      - Preview of the selected image
      - "Salvar" button
  - Profile name section:
    - Current name displayed with edit icon
    - Inline edit: click to switch to input field, blur/enter to save
    - Input: max 25 chars (WhatsApp profile name limit)
  - Status text section (optional, read-only for now):
    - Display current status text if available

  **Behavior:**
  - On save: PUT /api/chips with `{ id, action: 'updateProfile', profileName, profilePictureUrl }`
  - Show toast on success ("Perfil atualizado") via sonner
  - Show toast on error ("Erro ao atualizar perfil") via sonner
  - Loading state while API call in progress (disable save button, show spinner)

  Use shadcn/ui components: Dialog, Button, Input, Avatar, Label. Import from appropriate paths.
  No emoji in UI — use text labels and icons from lucide-react.
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/integration-evolution-wizard && npx next build 2>&1 | tail -5</automated>
    <manual>ChipProfileEditor component renders with avatar, name edit, and photo change dialog</manual>
  </verify>
  <done>
  - ChipProfileEditor component exists at src/components/chip-profile-editor.tsx
  - Shows current profile photo as avatar with fallback initials
  - Inline name editing with 25-char limit
  - Photo change dialog with URL input and file upload
  - Calls PUT /api/chips with action=updateProfile
  - Toast feedback on success/error
  - V2 Editorial Light styling
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate ChipProfileEditor into chips page</name>
  <files>src/app/chips/page.tsx</files>
  <action>
  Integrate the ChipProfileEditor into the existing chips page (src/app/chips/page.tsx, 1310 lines):

  1. **Profile column in chip health grid** — Add a profile photo column to the chip list/grid:
     - Show a small avatar (32x32) with the chip's profilePictureUrl or initials
     - Next to the avatar, show the profileName (or instanceName if no profile set)
     - This replaces or enhances the current chip name display

  2. **Profile editor in chip detail** — When a chip row is expanded/selected (the chips page likely has a detail view or dialog):
     - Render `<ChipProfileEditor chip={selectedChip} onSave={refetchChips} />` in the detail section
     - If the chips page uses a dialog/sheet for chip details, add the profile editor as a section within it
     - If chips page is a flat list without detail view, add an "Editar Perfil" button per chip row that opens a Dialog containing ChipProfileEditor

  3. **Profile data in API response** — The chips page fetches from `/api/chips`. Verify the API response includes the new profileName and profilePictureUrl fields (they should if db-chips.ts was updated in 38-01).

  4. **Visual indicator for unconfigured profiles** — If a chip has no profileName set, show a subtle warning badge: "Perfil não configurado" in muted text. This hints to operators that they should set up the profile before using the chip in campaigns.

  Keep the existing chip health dashboard functionality intact. The profile management is an addition, not a replacement.
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/integration-evolution-wizard && npx next build 2>&1 | tail -5</automated>
    <manual>Chips page shows profile photos and names, profile editor accessible per chip</manual>
  </verify>
  <done>
  - Chip list shows profile photo avatar + profileName for each chip
  - ChipProfileEditor accessible for each chip (via detail view or dialog)
  - API response includes profile fields
  - Unconfigured profiles show "Perfil não configurado" hint
  - Existing health dashboard functionality preserved
  - Build succeeds
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Chips page displays chip profiles with avatars
- Profile editor allows name and photo changes
- Changes persist to Evolution API and DB
</verification>

<success_criteria>
Chip profile management is fully operational — operators can view and edit chip profiles from the chips page.
</success_criteria>

<output>
After completion, create `.planning/phases/38-chip-profile-management/38-02-SUMMARY.md`
</output>
