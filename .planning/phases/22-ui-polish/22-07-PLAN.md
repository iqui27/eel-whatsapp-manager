---
phase: 22-ui-polish
plan: "07"
type: execute
wave: 3
depends_on: ["22-04"]
files_modified:
  - src/app/relatorios/page.tsx
  - src/app/compliance/page.tsx
  - src/app/admin/page.tsx
autonomous: true
requirements: [REP-UX-01, COMP-UX-01, ADM-UX-01, ADM-UX-02]

must_haves:
  truths:
    - "Reports page shows error toast when API calls fail"
    - "Reports page has loading skeleton for schedules section"
    - "Reports schedule removal has confirmation dialog"
    - "Compliance page shows error toast when operations fail"
    - "Compliance revoke action has confirmation dialog"
    - "Admin page shows success/error toasts for all mutations"
    - "Admin remove user uses AlertDialog instead of Dialog"
  artifacts:
    - path: "src/app/relatorios/page.tsx"
      provides: "Reports page with error toasts, loading states, and delete confirmation"
    - path: "src/app/compliance/page.tsx"
      provides: "Compliance page with error toasts and revoke confirmation"
    - path: "src/app/admin/page.tsx"
      provides: "Admin page with complete toast feedback and proper destructive dialogs"
  key_links:
    - from: "src/app/relatorios/page.tsx"
      to: "sonner toast"
      via: "error handling"
      pattern: "toast\\.error"
    - from: "src/app/compliance/page.tsx"
      to: "@/components/ui/alert-dialog"
      via: "revoke confirmation"
      pattern: "AlertDialog"
    - from: "src/app/admin/page.tsx"
      to: "sonner toast"
      via: "mutation feedback"
      pattern: "toast\\.(error|success)"
---

<objective>
Polish Reports, Compliance, and Admin pages to production quality.

Purpose: All three governance pages share the same issues: silent error handling, missing confirmation dialogs for destructive actions, and no loading states. Fix them in one plan since the pattern is identical across all three.
Output: Three governance pages with consistent error feedback, confirmation dialogs, and loading indicators.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/relatorios/page.tsx
@src/app/compliance/page.tsx
@src/app/admin/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reports page error toasts + loading state + delete confirmation</name>
  <files>src/app/relatorios/page.tsx</files>
  <action>
    Fix all reports page UX issues:

    1. **Add error toasts to all API calls:** Currently loadCampaigns, loadSchedules, createSchedule, toggleSchedule, removeSchedule all fail silently. Add `toast.error()` with specific messages to each:
       - `loadCampaigns` catch: `toast.error('Erro ao carregar campanhas')`
       - `loadSchedules` catch: `toast.error('Erro ao carregar agendamentos')`
       - `createSchedule` catch: `toast.error('Erro ao criar agendamento')`
       - `toggleSchedule` catch: `toast.error('Erro ao atualizar agendamento')`
       - `removeSchedule` catch: `toast.error('Erro ao remover agendamento')`
       - Add success toasts for mutations: `toast.success('Agendamento criado')`, `toast.success('Agendamento atualizado')`, `toast.success('Agendamento removido')`
       Import `toast` from `sonner` if not already present.

    2. **Add loading skeleton for schedules section:** Add `isLoadingSchedules` state (default true). While loading, show 2 skeleton rows (`animate-pulse bg-muted h-10 rounded`). Set false in finally block of loadSchedules.

    3. **Add confirmation dialog for schedule removal (line ~253):** Currently removes directly. Add AlertDialog:
       - State: `const [scheduleToRemove, setScheduleToRemove] = useState<string | null>(null)`
       - AlertDialog title: "Remover Agendamento?"
       - AlertDialog description: "O agendamento de relatorio sera removido permanentemente."
       - On confirm: call existing removeSchedule with scheduleToRemove ID
       Import AlertDialog components from `@/components/ui/alert-dialog`.

    4. **Add error feedback to downloadReport (line ~207-216):** Wrap in try/catch with `toast.error('Erro ao baixar relatorio')` on failure.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | head -20</automated>
    <manual>Reports: error toasts on failures; schedule section shows skeleton while loading; remove schedule shows confirmation dialog</manual>
  </verify>
  <done>Reports page: all API calls have error/success toasts; schedules section has loading skeleton; schedule removal has AlertDialog confirmation; download has error handling</done>
</task>

<task type="auto">
  <name>Task 2: Compliance + Admin error toasts and confirmation dialogs</name>
  <files>src/app/compliance/page.tsx, src/app/admin/page.tsx</files>
  <action>
    Fix compliance and admin page UX issues:

    **Compliance page (src/app/compliance/page.tsx):**

    1. **Add error toasts:** Currently revokeVoter and anonymizeVoter fail silently.
       - `revokeVoter` catch: `toast.error('Erro ao revogar consentimento')`
       - `anonymizeVoter` catch: `toast.error('Erro ao anonimizar eleitor')`
       - Load function catch: `toast.error('Erro ao carregar dados de compliance')`
       - Add success toasts: `toast.success('Consentimento revogado')`, `toast.success('Eleitor anonimizado')`
       Import `toast` from `sonner`.

    2. **Add confirmation for revokeVoter (line ~180):** Currently revokes directly without confirm. Add AlertDialog:
       - State: `const [voterToRevoke, setVoterToRevoke] = useState<string | null>(null)`
       - AlertDialog title: "Revogar Consentimento?"
       - AlertDialog description: "O consentimento do eleitor sera marcado como revogado. Esta acao pode ser revertida."
       - On confirm: call existing revoke handler
       Import AlertDialog components.

    **Admin page (src/app/admin/page.tsx):**

    1. **Add toasts for ALL mutations:** Currently none of the admin operations provide feedback.
       - `updateRole` success: `toast.success('Funcao atualizada')` / error: `toast.error('Erro ao atualizar funcao')`
       - `toggleEnabled` success: `toast.success('Status do usuario atualizado')` / error: `toast.error('Erro ao atualizar status')`
       - `removeUser` success: `toast.success('Usuario removido')` / error: `toast.error('Erro ao remover usuario')`
       - `handleInvite` success: `toast.success('Convite enviado')` / error: `toast.error('Erro ao enviar convite')`
       - `savePermissions` success: `toast.success('Permissoes salvas')` / error: `toast.error('Erro ao salvar permissoes')`
       Import `toast` from `sonner`.

    2. **Replace Dialog with AlertDialog for remove user (line ~480-495):** Remove user is a destructive action. Change from Dialog to AlertDialog:
       - Replace `<Dialog>` / `<DialogContent>` with `<AlertDialog>` / `<AlertDialogContent>`
       - Use AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter
       - Confirm button: `<AlertDialogAction className="bg-destructive text-destructive-foreground hover:bg-destructive/90">`
       Import AlertDialog components from `@/components/ui/alert-dialog`.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | head -20</automated>
    <manual>Compliance: revoke shows AlertDialog + error/success toasts; Admin: all mutations have toasts, remove user uses AlertDialog</manual>
  </verify>
  <done>Compliance: error toasts on all operations, AlertDialog for revoke; Admin: success/error toasts on all 5 mutation types, AlertDialog for destructive remove user action</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `node_modules/.bin/tsc --noEmit` passes
- No silent catch blocks in relatorios, compliance, or admin pages
- All destructive actions (remove schedule, revoke consent, remove user) use AlertDialog
- All mutations provide success/error toast feedback
</verification>

<success_criteria>
- Reports: all 5 API functions have error toasts; schedules have loading skeleton; removal has AlertDialog
- Compliance: revoke has AlertDialog; all operations have error/success toasts
- Admin: all 5 mutations have success/error toasts; remove user uses AlertDialog with destructive styling
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/22-ui-polish/22-07-SUMMARY.md`
</output>
