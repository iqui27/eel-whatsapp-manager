---
phase: 22-ui-polish
plan: "06"
type: execute
wave: 3
depends_on: ["22-04"]
files_modified:
  - src/app/crm/page.tsx
autonomous: true
requirements: [CRM-UX-01, CRM-UX-02, CRM-UX-03]

must_haves:
  truths:
    - "CRM page shows error toast when voter list fails to load"
    - "AlertDialog props use only standard shadcn attributes"
    - "Tags column displays properly without overflow"
    - "CRM delete voter shows confirmation dialog"
  artifacts:
    - path: "src/app/crm/page.tsx"
      provides: "Polished CRM voter list with proper error handling and valid component props"
  key_links:
    - from: "src/app/crm/page.tsx"
      to: "sonner toast"
      via: "error handling in catch blocks"
      pattern: "toast\\.error"
---

<objective>
Polish the CRM voter list page to production quality.

Purpose: Fix invalid shadcn component props, silent error handling, tag column overflow, and inconsistent UX patterns in the voter management page.
Output: CRM page with proper error feedback, valid component API usage, and professional table layout.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/crm/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CRM error handling + invalid props + tag overflow</name>
  <files>src/app/crm/page.tsx</files>
  <action>
    Fix all identified CRM page issues:

    1. **Add error toasts to catch blocks (line ~131):** The load function has a silent catch block. Add `toast.error('Erro ao carregar eleitores')`. Ensure `import { toast } from "sonner"` is present.

    2. **Fix invalid AlertDialogContent prop (line ~526):** Remove `size="sm"` from `<AlertDialogContent>` — this prop does not exist on shadcn AlertDialogContent. Use `className="sm:max-w-[425px]"` if a smaller dialog is desired.

    3. **Fix invalid AlertDialogAction prop (line ~537):** Remove `variant="destructive"` from `<AlertDialogAction>`. Instead, add `className="bg-destructive text-destructive-foreground hover:bg-destructive/90"` to achieve the destructive button styling.

    4. **Fix tags column overflow (line ~345):** Replace `max-w-[140px]` with a more generous width. Use `max-w-[200px]` with `flex-wrap gap-1` for the tags container. Each tag badge should use `line-clamp-1` if the tag text itself is long. Add `title` attribute on the container showing all tags joined by comma for full visibility on hover.

    5. **Add error toast for delete voter failure:** If the delete voter API call fails, show `toast.error('Erro ao excluir eleitor')`. If it succeeds, show `toast.success('Eleitor excluido com sucesso')`.

    6. **Add error toast for add voter failure:** If the add voter API call fails, show `toast.error('Erro ao adicionar eleitor')`. On success, show `toast.success('Eleitor adicionado com sucesso')`.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | head -20</automated>
    <manual>CRM page: error toast on load failure; delete dialog has proper destructive styling; tags display cleanly; add/delete show success/error toasts</manual>
  </verify>
  <done>CRM page: all catch blocks show toast.error; AlertDialog uses valid props with className for destructive styling; tags column uses 200px max-w with flex-wrap; add/delete mutations show success/error feedback</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `node_modules/.bin/tsc --noEmit` passes
- No `size=` prop on AlertDialogContent
- No `variant=` prop on AlertDialogAction
- No silent catch blocks in CRM page
- Tags column has proper width and wrap behavior
</verification>

<success_criteria>
- All API failures show specific error toasts
- All mutations show success/error feedback
- AlertDialog components use only valid shadcn props
- Tags column displays without overflow or excessive truncation
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/22-ui-polish/22-06-SUMMARY.md`
</output>
