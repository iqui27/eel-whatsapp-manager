---
phase: 22-ui-polish
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/app/page.tsx
  - src/components/ChatQueuePanel.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]
must_haves:
  truths:
    - "Dashboard right column is wide enough to display ChatQueuePanel without cramping"
    - "When there are no groups or messages, components show empty state, not perpetual loading"
    - "API failures display error toast via sonner"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Dashboard with wider right column and error handling"
    - path: "src/components/ChatQueuePanel.tsx"
      provides: "Chat queue panel with adequate width"
  key_links:
    - from: "src/app/page.tsx"
      to: "sonner toast"
      via: "toast.error on API failure"
      pattern: "toast\\.error"
---

<objective>
Polish the dashboard layout and data handling: widen the right column for ChatQueuePanel, and add error toasts for API failures.

Purpose: The right column at 280px is too narrow for the ChatQueuePanel content. The dashboard already has toast imported and uses it for some errors, but the KPI division-by-zero was fixed in plan 22-01. This plan focuses on layout and UX improvements.

Output: Dashboard with proper right-column width and error handling.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-ui-polish/22-01-SUMMARY.md
@src/app/page.tsx
@src/components/ChatQueuePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen dashboard right column and add comprehensive error toasts</name>
  <files>src/app/page.tsx</files>
  <action>
1. **Widen right column**: Change the grid template from `lg:grid-cols-[1fr_280px]` to `lg:grid-cols-[1fr_320px]`. This gives the ChatQueuePanel 40px more space for conversation previews and queue items.

2. **Add error toasts for individual API failures**: The current `fetchAll` function has a single catch that shows `toast.error('Erro ao carregar dados')`. Improve this to show more specific errors. Change the fetch pattern so that each `Promise.all` response that is not ok generates a specific toast:
   - If `campRes` is not ok (and not 401): `toast.error('Erro ao carregar campanhas')`
   - If `segRes` is not ok: `toast.error('Erro ao carregar segmentos')`
   - If `voterRes` is not ok: `toast.error('Erro ao carregar eleitores')`
   - If `chipRes` is not ok: `toast.error('Erro ao carregar chips')`
   
   Keep the existing 401 redirect for campRes. The outer catch should remain for network-level failures.

3. **Add error toast for warm-all**: The `handleWarmAll` function already has error toasts. No change needed there.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Dashboard right panel should be noticeably wider. API failures should show specific error toasts.</manual>
  </verify>
  <done>Right column is 320px wide. Each API failure shows a specific error toast. Dashboard compiles and renders correctly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Dashboard right column is 320px (verify in source)
- When an API endpoint fails, specific error message appears as toast
</verification>

<success_criteria>
- Dashboard layout gives ChatQueuePanel adequate space
- Error handling is granular (per-endpoint error messages)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-ui-polish/22-02-SUMMARY.md`
</output>
