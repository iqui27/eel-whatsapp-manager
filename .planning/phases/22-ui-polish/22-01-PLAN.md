---
phase: 22-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/SidebarLayout.tsx
  - src/app/campanhas/[id]/page.tsx
  - src/app/campanhas/[id]/mensagens/page.tsx
  - src/app/grupos/page.tsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05]
must_haves:
  truths:
    - "Dashboard page compiles without errors (no references to removed operations tab)"
    - "Every page renders inside SidebarLayout with correct sidebar/topbar/bottom-nav"
    - "Mobile bottom nav shows correct 5 items: Dashboard, Campanhas, Conversas, CRM, Relatorios"
    - "Sidebar does not show deprecated Contatos or Clusters items"
    - "All internal links use Next.js <Link> component, not <a href>"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Dashboard without operations tab"
    - path: "src/components/SidebarLayout.tsx"
      provides: "Fixed mobile nav indices and cleaned legacy nav"
    - path: "src/app/campanhas/[id]/page.tsx"
      provides: "Campaign detail with SidebarLayout wrapper"
    - path: "src/app/campanhas/[id]/mensagens/page.tsx"
      provides: "Campaign messages with SidebarLayout wrapper"
    - path: "src/app/grupos/page.tsx"
      provides: "Groups page with SidebarLayout wrapper and Link components"
  key_links:
    - from: "src/app/page.tsx"
      to: "SidebarLayout"
      via: "wraps entire page"
      pattern: "SidebarLayout"
    - from: "src/components/SidebarLayout.tsx"
      to: "mobileNavItems"
      via: "correct array indices"
      pattern: "electoralNavItems\\[\\d\\]"
---

<objective>
Fix all broken navigation across the application: complete the broken dashboard operations tab removal, wrap orphaned pages in SidebarLayout, fix mobile bottom nav indices, convert `<a href>` to `<Link>`, and remove deprecated sidebar items.

Purpose: The dashboard page is currently in a broken state (imports removed but JSX still references operations code). Several pages render without the app shell. Mobile nav shows wrong items. This plan makes the entire navigation layer functional.

Output: All pages compile, render inside the app shell, and navigate correctly on both desktop and mobile.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.tsx
@src/components/SidebarLayout.tsx
@src/app/campanhas/[id]/page.tsx
@src/app/campanhas/[id]/mensagens/page.tsx
@src/app/grupos/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete dashboard operations tab removal and fix KPI division by zero</name>
  <files>src/app/page.tsx</files>
  <action>
The file is in a BROKEN state: imports for operations components (ChipHealthGrid, CampaignProgressBars, AlertsPanel, GroupCapacityGrid, ConversionKPIs, MessageFeed, Activity icon) and state variables (activeTab, opsData, kpiData, messagesData, groupsData) were already removed by a prior aborted subagent, BUT the JSX still references all of them.

Fix the file by removing these specific blocks:

1. **Remove the `fetchOperations` function** (approx lines 366-394) — entire `const fetchOperations = useCallback(async () => { ... }, []);` block
2. **Remove the auto-refresh useEffect** (approx lines 396-403) — the `useEffect` that checks `activeTab === 'operations'` and calls `fetchOperations`
3. **Remove the Tab Switcher JSX** (approx lines 467-485) — the `<div className="flex gap-2">` containing the "Visao Geral" / "Operacoes" buttons that reference `activeTab`/`setActiveTab`/`Activity`
4. **Remove the entire Operations Tab JSX** (approx lines 487-567) — the `{activeTab === 'operations' && (...)}` block that references `AlertsPanel`, `ConversionKPIs`, `ChipHealthGrid`, `CampaignProgressBars`, `GroupCapacityGrid`, `MessageFeed`, `opsData`, `kpiData`, `groupsData`, `messagesData`
5. **Remove the overview tab conditional wrapper** — change `{activeTab === 'overview' && (<>...</>)}` to just render the content directly (remove the conditional and the fragment)
6. **Fix KPI division by zero** — On the "Respostas" KPI card (approx line 619), change `${Math.round((totalReplied / totalSent) * 100)}%` to use the already-defined guard: `totalSent > 0 ? \`${Math.round((totalReplied / totalSent) * 100)}% sobre as enviadas\` : 'Nenhuma campanha enviada'`. Similarly fix "Bloqueios" KPI card (approx line 630) with same guard pattern.

After removal, clean up any unused imports. The `Activity` icon import should already be removed (it was in the aborted removal). Verify no remaining references to `activeTab`, `setActiveTab`, `opsData`, `setOpsData`, `kpiData`, `setKpiData`, `messagesData`, `setMessagesData`, `groupsData`, `setGroupsData`, `fetchOperations`, `ChipHealthGrid`, `CampaignProgressBars`, `AlertsPanel`, `GroupCapacityGrid`, `ConversionKPIs`, `MessageFeed`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify dashboard loads at / without any operations tab</manual>
  </verify>
  <done>Dashboard page compiles, renders KPI cards with safe division, no operations tab, no references to removed components/state</done>
</task>

<task type="auto">
  <name>Task 2: Fix SidebarLayout mobile nav indices and remove deprecated items</name>
  <files>src/components/SidebarLayout.tsx</files>
  <action>
Fix the mobile bottom nav and clean up deprecated sidebar items:

1. **Fix mobile nav indices** (lines 97-103). Current incorrect mapping:
   ```
   electoralNavItems[0], // Dashboard  (correct)
   electoralNavItems[1], // Operacoes  (WRONG - should be Campanhas at [2])
   electoralNavItems[3], // Segmentacao (WRONG - should be Conversas at [4])
   electoralNavItems[4], // Conversas  (WRONG - should be CRM at [5])
   electoralNavItems[6], // Compliance  (WRONG - should be Relatorios at [7])
   ```
   
   The electoralNavItems array is (0-indexed):
   - 0: Dashboard
   - 1: Operacoes
   - 2: Campanhas
   - 3: Segmentacao
   - 4: Conversas
   - 5: CRM
   - 6: Compliance
   - 7: Relatorios
   
   Correct mapping for mobile should be:
   ```
   electoralNavItems[0], // Dashboard
   electoralNavItems[2], // Campanhas
   electoralNavItems[4], // Conversas
   electoralNavItems[5], // CRM
   electoralNavItems[7], // Relatorios
   ```

2. **Remove deprecated items from legacyNavItems** (lines 90-91): Remove the `contacts` entry (`{ id: 'contacts', label: 'Contatos', href: '/contacts', icon: Users }`) and the `clusters` entry (`{ id: 'clusters', label: 'Clusters', href: '/clusters', icon: Layers }`).

3. **Clean up unused imports**: After removing Contatos and Clusters, check if `Users` and `Layers` icons are still used elsewhere in the file. `Users` is NOT used elsewhere (only in legacyNavItems for contacts). `Layers` is NOT used elsewhere. Remove both from the import if unused.

4. **Clean up PageId type**: Remove `'contacts'` and `'clusters'` from the PageId union type (lines 48-49).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check mobile bottom nav shows Dashboard, Campanhas, Conversas, CRM, Relatorios</manual>
  </verify>
  <done>Mobile bottom nav shows correct 5 electoral items. Sidebar no longer has Contatos or Clusters. File compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Wrap orphaned pages in SidebarLayout and convert a-href to Link</name>
  <files>src/app/campanhas/[id]/page.tsx, src/app/campanhas/[id]/mensagens/page.tsx, src/app/grupos/page.tsx</files>
  <action>
Three pages render outside the app shell and/or use `<a href>` instead of `<Link>`.

**src/app/campanhas/[id]/page.tsx** (server component):
- This is an async server component. SidebarLayout is a client component. Create a client wrapper approach: either add a `layout.tsx` at `src/app/campanhas/[id]/layout.tsx` that wraps children in `<SidebarLayout currentPage="campanhas" pageTitle="Campanha">`, OR convert this page to a client component. The layout.tsx approach is cleaner since it also covers the `mensagens` sub-route.
- Create `src/app/campanhas/[id]/layout.tsx`:
  ```tsx
  'use client';
  import SidebarLayout from '@/components/SidebarLayout';
  export default function CampaignDetailLayout({ children }: { children: React.ReactNode }) {
    return <SidebarLayout currentPage="campanhas" pageTitle="Campanha">{children}</SidebarLayout>;
  }
  ```
- In the page itself, convert `<a href={...}>` links to `<Link href={...}>` (lines 92-103). Import `Link` from `next/link`.
- Change `<a href={"/campanhas/${id}/mensagens"}` to `<Link href={...}>`
- Change `<a href="/campanhas"` to `<Link href="/campanhas">`

**src/app/campanhas/[id]/mensagens/page.tsx** (client component):
- Already has `import Link from 'next/link'` and uses `<Link>` for some navigation.
- Wrap the returned JSX in `<SidebarLayout currentPage="campanhas" pageTitle="Mensagens da Campanha">`. Import SidebarLayout. The layout.tsx created above will handle this automatically since it's a child route, so no need to wrap again.

**src/app/grupos/page.tsx** (server component):
- Needs SidebarLayout wrapper. Since this is a standalone page (no sub-routes), create `src/app/grupos/layout.tsx`:
  ```tsx
  'use client';
  import SidebarLayout from '@/components/SidebarLayout';
  export default function GruposLayout({ children }: { children: React.ReactNode }) {
    return <SidebarLayout currentPage="grupos" pageTitle="Grupos WhatsApp">{children}</SidebarLayout>;
  }
  ```
- Convert filter `<a href="/grupos...">` links (lines 67-98) to `<Link href="/grupos...">`. Import `Link` from `next/link`. There are 4 filter links that need conversion.

Note: Do NOT double-wrap pages that already have SidebarLayout inside the page component itself. The layout.tsx wraps at the route level.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Visit /campanhas/[any-id] and /grupos — both should show sidebar, topbar, and bottom nav</manual>
  </verify>
  <done>All three pages render inside SidebarLayout shell. All internal links use Next.js Link component. No `<a href>` for internal navigation.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Dashboard at `/` loads without operations tab, KPI cards don't show NaN
- `/campanhas/[id]` and `/campanhas/[id]/mensagens` show sidebar and topbar
- `/grupos` shows sidebar, topbar, and filter links work without full page reload
- Mobile bottom nav shows: Dashboard, Campanhas, Conversas, CRM, Relatorios (in that order)
- Sidebar does not show Contatos or Clusters
</verification>

<success_criteria>
- TypeScript compilation succeeds
- All pages render inside the app shell (SidebarLayout)
- Mobile navigation shows correct 5 items
- No `<a href>` for internal navigation
- No NaN in KPI cards
- No references to removed operations tab code in dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/22-ui-polish/22-01-SUMMARY.md`
</output>
