---
phase: 22-ui-polish
plan: "05"
type: execute
wave: 2
depends_on: ["22-03"]
files_modified:
  - src/app/conversas/page.tsx
autonomous: true
requirements: [CONV-UX-01, CONV-UX-02, CONV-UX-03]

must_haves:
  truths:
    - "Conversation right panel is 320px wide (not 280px)"
    - "Priority level 3 uses text label not emoji"
    - "New conversation dialog uses shadcn Dialog component"
    - "Conversations page has basic mobile layout fallback"
  artifacts:
    - path: "src/app/conversas/page.tsx"
      provides: "Polished HITL conversations page with consistent components and mobile support"
  key_links:
    - from: "src/app/conversas/page.tsx"
      to: "@/components/ui/dialog"
      via: "new conversation modal"
      pattern: "Dialog|DialogContent|DialogTrigger"
---

<objective>
Overhaul the Conversas (HITL conversations) page for production quality.

Purpose: Fix the cramped right panel width, remove emoji from priority indicators, replace custom modal with shadcn Dialog, and add basic mobile responsiveness to the three-column conversation layout.
Output: Professional HITL conversations page matching the polish level of dashboard and operacoes pages.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ui-polish/22-03-SUMMARY.md
@src/app/conversas/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix right panel width + remove emoji priority + mobile layout</name>
  <files>src/app/conversas/page.tsx</files>
  <action>
    Fix core layout and content issues:

    1. **Widen right panel (line ~827):** Change `w-[280px]` to `w-[320px]` for the voter context right panel. This matches the dashboard right column change from plan 22-02.

    2. **Replace emoji priority indicator (line ~924):** Priority level 3 currently uses `🔥` emoji. Replace with text-based indicator following the no-emoji policy:
       - Priority 1: "Baixa" with `text-muted-foreground`
       - Priority 2: "Media" with `text-yellow-600`
       - Priority 3: "Alta" with `text-orange-600`
       - Priority 4 or higher: "Crit" with `text-red-600 font-semibold`
       Search the entire file for any other emoji usage in priority/status indicators and replace all with text equivalents.

    3. **Basic mobile responsiveness:** The three-column layout breaks on mobile. Add responsive behavior:
       - Wrap the three columns in a container that on mobile (`< lg`) shows only the conversation queue as a full-width list
       - When a conversation is selected on mobile, show the chat panel full-width with a back button
       - The voter context panel should be hidden on mobile (accessible via expanding/toggling)
       - Use Tailwind responsive classes: `hidden lg:block` for right panel, `hidden lg:flex` for three-column layout on desktop
       - On mobile, add a simple state toggle: `showChat` boolean that swaps between queue list and active chat view
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | head -20</automated>
    <manual>Right panel is 320px; no emojis in priority indicators; mobile shows queue list with tap-to-chat navigation</manual>
  </verify>
  <done>Right panel 320px; priority uses text labels (Baixa/Media/Alta/Crit); mobile shows single-column with queue/chat toggle</done>
</task>

<task type="auto">
  <name>Task 2: Replace custom NewConvDialog with shadcn Dialog</name>
  <files>src/app/conversas/page.tsx</files>
  <action>
    Replace the custom modal implementation (lines ~163-391) with shadcn Dialog:

    1. **Remove custom modal markup:** The NewConvDialog currently uses `fixed inset-0 z-50 bg-black/50` overlay pattern. Replace the entire custom modal with shadcn Dialog components.

    2. **Import shadcn Dialog:** Add imports:
       `import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog"`

    3. **Restructure the dialog:**
       - Use `<Dialog open={showNewConv} onOpenChange={setShowNewConv}>` as wrapper
       - Move trigger button (the "Nova conversa" button) to `<DialogTrigger asChild>`
       - Wrap dialog body in `<DialogContent className="sm:max-w-[500px]">`
       - Use `<DialogHeader>` with `<DialogTitle>Nova Conversa</DialogTitle>`
       - Add `<DialogDescription>Selecione um eleitor e chip para iniciar uma nova conversa</DialogDescription>`
       - Keep all existing form fields (voter search, chip selector, message input) inside DialogContent
       - Wrap action buttons in `<DialogFooter>`

    4. **Preserve all existing logic:** The voter search, chip selection, message composition, and submit handler must remain exactly the same. Only the modal container/overlay changes.

    5. **Remove old overlay divs:** Delete the `fixed inset-0` overlay div and the manual close button that were part of the custom implementation.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | head -20</automated>
    <manual>New conversation dialog opens/closes properly; form fields work; submission creates conversation</manual>
  </verify>
  <done>NewConvDialog uses shadcn Dialog/DialogContent/DialogHeader; no custom fixed overlay markup remains; all form logic preserved</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `node_modules/.bin/tsc --noEmit` passes
- No emoji characters in the conversas page source
- No `fixed inset-0` custom overlay divs remain
- Right panel width is 320px
- Mobile viewport shows queue list without horizontal overflow
</verification>

<success_criteria>
- Right panel widened to 320px matching dashboard
- Priority indicators use text labels (no emoji) per project policy
- NewConvDialog uses shadcn Dialog component
- Basic mobile responsiveness: queue list on mobile, chat on selection
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/22-ui-polish/22-05-SUMMARY.md`
</output>
