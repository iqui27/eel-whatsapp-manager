---
phase: 22-ui-polish
plan: "04"
type: execute
wave: 2
depends_on: ["22-03"]
files_modified:
  - src/app/campanhas/page.tsx
  - src/app/segmentacao/page.tsx
autonomous: true
requirements: [CAMP-UX-01, CAMP-UX-02, SEG-UX-01, SEG-UX-02]

must_haves:
  truths:
    - "Campaign list shows error toast when API call fails"
    - "Campaign message preview uses line-clamp instead of truncate+max-w"
    - "Campaign action buttons have tooltips explaining each action"
    - "Segmentation delete uses AlertDialog instead of window.confirm"
    - "Segmentation dropdowns use shadcn Select instead of raw HTML select"
    - "Segmentation page shows loading skeleton during initial load"
  artifacts:
    - path: "src/app/campanhas/page.tsx"
      provides: "Polished campaign list with error toasts and accessible truncation"
    - path: "src/app/segmentacao/page.tsx"
      provides: "Professional segmentation page with shadcn components and loading states"
  key_links:
    - from: "src/app/campanhas/page.tsx"
      to: "sonner toast"
      via: "catch block error handling"
      pattern: "toast\\.error"
    - from: "src/app/segmentacao/page.tsx"
      to: "@/components/ui/alert-dialog"
      via: "delete confirmation"
      pattern: "AlertDialog"
    - from: "src/app/segmentacao/page.tsx"
      to: "@/components/ui/select"
      via: "filter dropdowns"
      pattern: "Select|SelectTrigger|SelectContent"
---

<objective>
Polish Campanhas (campaign list) and Segmentacao (segment builder) pages to production quality.

Purpose: Fix silent error handling, inconsistent component usage (raw HTML vs shadcn), artificial text truncation, and missing loading states across both campaign management pages.
Output: Both pages use consistent shadcn components, proper error feedback, and professional UX patterns matching Wave 1 polish level.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ui-polish/22-03-SUMMARY.md
@src/app/campanhas/page.tsx
@src/app/segmentacao/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campanhas page error handling + UX fixes</name>
  <files>src/app/campanhas/page.tsx</files>
  <action>
    Fix the campaign list page UX issues:

    1. **Error toast on load failure (line ~76-77):** The catch block currently does `console.error` only. Add `toast.error('Erro ao carregar campanhas')` in the catch block. Import toast from sonner if not already imported.

    2. **Message template preview truncation (line ~200):** Replace `max-w-[200px] truncate` with `line-clamp-1` and add `title={campaign.template}` attribute for hover tooltip. This matches the pattern established in 22-03 for campaign-progress-bars.tsx.

    3. **Action button tooltips:** The icon-only action buttons (edit, delete, etc.) are not self-documenting. Wrap each icon button in a `<span title="Editar">`, `<span title="Excluir">`, `<span title="Monitorar">` etc. for native tooltip accessibility. Do NOT add a tooltip library — use HTML title attributes only.

    Ensure `import { toast } from "sonner"` is present at top of file.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | head -20</automated>
    <manual>Campaign list loads with error toast on API failure; template previews show full text on hover</manual>
  </verify>
  <done>Campaign page catch block shows toast.error; template preview uses line-clamp-1 + title; action buttons have title tooltips</done>
</task>

<task type="auto">
  <name>Task 2: Segmentacao page shadcn migration + loading state</name>
  <files>src/app/segmentacao/page.tsx</files>
  <action>
    Fix the segmentation page UX issues:

    1. **Replace window.confirm with AlertDialog (line ~561):** The delete segment action uses `window.confirm()`. Replace with shadcn AlertDialog component:
       - Import AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger from `@/components/ui/alert-dialog`
       - Add state: `const [segmentToDelete, setSegmentToDelete] = useState<string | null>(null)`
       - Wrap the delete button as AlertDialogTrigger
       - AlertDialog content: title "Excluir Segmento?", description "Esta acao nao pode ser desfeita. O segmento sera removido permanentemente."
       - On confirm, call the existing delete handler with segmentToDelete ID

    2. **Replace raw `<select>` with shadcn Select (lines ~299, ~678):** Find ALL raw `<select>` elements and replace with shadcn Select components:
       - Import Select, SelectContent, SelectItem, SelectTrigger, SelectValue from `@/components/ui/select`
       - Replace each `<select>` with `<Select value={...} onValueChange={...}>` pattern
       - Map each `<option>` to `<SelectItem value={...}>`
       - Preserve all existing onChange logic, just adapt from event.target.value to direct value parameter

    3. **Add loading skeleton for initial segment list:** Add an `isLoading` state (default true, set false after first fetch). While isLoading, show 3 skeleton rows in the saved segments table using `<div className="animate-pulse bg-muted h-12 rounded" />` pattern. Set isLoading=false in the finally block of the initial fetch.

    4. **Filter summary truncation (line ~809):** Replace `max-w-[280px]` truncation on filter summary column with `line-clamp-1` + `title` attribute for full text on hover.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | head -20</automated>
    <manual>Delete segment shows AlertDialog; dropdowns use shadcn Select; initial load shows skeleton; filter summary shows on hover</manual>
  </verify>
  <done>Segmentation page uses AlertDialog for delete confirmation; all dropdowns are shadcn Select; loading skeleton shown during initial fetch; filter summary not artificially truncated</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `node_modules/.bin/tsc --noEmit` passes
- Both pages render inside SidebarLayout (already established in Wave 1)
- No raw `<select>` elements remain in segmentacao page
- No `window.confirm` calls remain in segmentacao page
- No `max-w-[Xpx] truncate` patterns remain in either file
</verification>

<success_criteria>
- Campanhas page: error toast on API failure, line-clamp-1 on template preview, title tooltips on action buttons
- Segmentacao page: AlertDialog for delete, shadcn Select for all dropdowns, loading skeleton, line-clamp-1 on filter summary
- TypeScript compiles cleanly (pre-existing errors in chip-failover.ts and campaign-groups.ts excluded)
</success_criteria>

<output>
After completion, create `.planning/phases/22-ui-polish/22-04-SUMMARY.md`
</output>
