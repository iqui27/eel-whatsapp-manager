---
phase: 22-ui-polish
plan: "08"
type: execute
wave: 3
depends_on: ["22-05"]
files_modified:
  - src/components/topbar.tsx
  - src/components/chip-health-grid.tsx
autonomous: true
requirements: [TOP-UX-01, TOP-UX-02, CLEANUP-01]

must_haves:
  truths:
    - "Topbar period section shows useful context instead of static placeholder text"
    - "Topbar session section shows authenticated user information"
    - "Uncommitted chip-health-grid changes are committed"
  artifacts:
    - path: "src/components/topbar.tsx"
      provides: "Topbar with meaningful content in period and session sections"
    - path: "src/components/chip-health-grid.tsx"
      provides: "Clean committed state for chip health grid"
  key_links:
    - from: "src/components/topbar.tsx"
      to: "session/user context"
      via: "display user email or role"
      pattern: "session|user|email"
---

<objective>
Polish the Topbar component and commit the orphaned chip-health-grid changes.

Purpose: Fix the topbar's static placeholder sections that waste space and commit the orphaned modification to chip-health-grid.tsx from a prior aborted session. This is the final cleanup plan of the UI polish phase.
Output: Topbar with meaningful content in all sections; clean git state with no orphaned changes.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/topbar.tsx
@src/components/chip-health-grid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Topbar content improvements</name>
  <files>src/components/topbar.tsx</files>
  <action>
    Improve the topbar sections that currently show static placeholder text:

    1. **Period section (line ~121-126):** Currently shows static text "Periodo definido por pagina". This wastes precious topbar space. Replace with a compact date display showing today's date in Portuguese format:
       - Show: `new Date().toLocaleDateString('pt-BR', { weekday: 'short', day: 'numeric', month: 'short' })`
       - Example output: "ter, 18 de mar"
       - Use `text-sm text-muted-foreground` styling
       - This is a simple client-side date — no API needed

    2. **Session section:** Currently shows hardcoded "Sessao ativa". Replace with actual user context:
       - The app uses session-based auth. Check if the topbar component has access to session data (likely passed via props or available from a hook).
       - If session/user info is accessible: show the user's email or name truncated to 20 chars
       - If session/user info is NOT readily available without significant refactoring: show "Operador" as a generic label with a colored dot indicating active session status (green dot + "Ativo")
       - Do NOT add new API calls or hooks — only use what's already available in scope

    3. **Alert section:** Currently shows hardcoded "Sem alertas operacionais". This is fine as a default state — leave it as-is. The alert badge already shows count when alerts exist.

    Ensure no emojis are introduced in any topbar section.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | head -20</automated>
    <manual>Topbar shows current date in period section; session section shows user info or "Operador Ativo"</manual>
  </verify>
  <done>Topbar period section shows current date; session section shows user context; no static placeholder text; no emojis</done>
</task>

<task type="auto">
  <name>Task 2: Commit orphaned chip-health-grid changes</name>
  <files>src/components/chip-health-grid.tsx</files>
  <action>
    The file `src/components/chip-health-grid.tsx` has an uncommitted modification from a prior aborted session (22-03 execution). This needs to be reviewed and committed:

    1. **Review the current diff:** Read the file and check `git diff src/components/chip-health-grid.tsx` to understand what changed.

    2. **Verify the changes are valid:** The changes from the prior session were part of the professional chip health grid redesign (no-emoji policy — colored dots, table layout). Confirm the changes align with OPS-01 requirements from Phase 22.

    3. **If changes look correct:** Commit them with message `fix(22-08): commit orphaned chip-health-grid polish from prior session`

    4. **If changes look broken or incomplete:** Restore the file to its last committed state with `git checkout -- src/components/chip-health-grid.tsx` and note this in the summary.

    Do NOT modify the file content — only review and commit (or revert).
  </action>
  <verify>
    <automated>git status src/components/chip-health-grid.tsx | grep -c "nothing to commit\|clean"</automated>
    <manual>chip-health-grid.tsx has no uncommitted changes</manual>
  </verify>
  <done>chip-health-grid.tsx is in a clean committed state (either committed or reverted)</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `node_modules/.bin/tsc --noEmit` passes
- git status shows no orphaned modifications to chip-health-grid.tsx
- Topbar has no hardcoded placeholder text (except alert default)
- No emoji characters in topbar or chip-health-grid
</verification>

<success_criteria>
- Topbar period section shows current date (not "Periodo definido por pagina")
- Topbar session section shows user context (not "Sessao ativa")
- chip-health-grid.tsx has clean git status (committed or reverted)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/22-ui-polish/22-08-SUMMARY.md`
</output>
