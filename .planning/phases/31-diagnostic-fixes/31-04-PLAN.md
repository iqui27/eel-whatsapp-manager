---
phase: 31-diagnostic-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/historico/page.tsx
  - src/app/api/messages/history/route.ts
autonomous: true
requirements: [DIAG-07, DIAG-08]

must_haves:
  truths:
    - "Empty state explains why table is empty and provides actionable next step"
    - "Message history can show inbound (received) messages alongside outbound"
    - "User can filter between outbound, inbound, or all messages"
  artifacts:
    - path: "src/app/historico/page.tsx"
      provides: "Improved empty state with campaign CTA, direction filter toggle"
    - path: "src/app/api/messages/history/route.ts"
      provides: "Inbound message query from conversationMessages table, direction filter param"
  key_links:
    - from: "src/app/historico/page.tsx"
      to: "/api/messages/history"
      via: "fetch with direction param"
      pattern: "direction=(all|outbound|inbound)"
    - from: "src/app/api/messages/history/route.ts"
      to: "conversationMessages"
      via: "SELECT from conversationMessages WHERE sender='voter'"
      pattern: "conversationMessages"
---

<objective>
Enhance Message History page with meaningful empty state and inbound message support.

Purpose: The /historico page queries only the `message_queue` table (outbound campaign messages). If no campaigns have been sent, the page shows a generic "Nenhuma mensagem encontrada" which confuses users. Additionally, inbound/conversation messages are completely invisible in history.

Output: Improved empty state with guidance, inbound message query support, direction filter toggle.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/historico/page.tsx
@src/app/api/messages/history/route.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve empty state + add direction filter to history page</name>
  <files>src/app/historico/page.tsx</files>
  <action>
  **Improve empty state:**
  Replace the generic "Nenhuma mensagem encontrada" empty state with a contextual empty state that explains WHY and provides a next step:

  1. When no messages exist AND no search/filters are active, show:
     - Icon: MessageSquare or Send (from lucide-react)
     - Title: "Nenhuma mensagem no histórico"
     - Description: "O histórico é preenchido automaticamente quando campanhas são enviadas ou leads respondem às suas mensagens."
     - CTA button: "Criar primeira campanha" linking to `/campanhas/nova`
     - Secondary text: "Ou importe sua base de leads em Segmentação → Importar"

  2. When no messages match active filters/search, show:
     - Title: "Nenhum resultado encontrado"
     - Description: "Tente ajustar os filtros ou a busca"

  **Add direction filter toggle:**
  1. Add a new filter state: `const [direction, setDirection] = useState<'all' | 'outbound' | 'inbound'>('all');`
  2. Add a toggle group (three buttons or segmented control) above the table:
     - "Todas" (all), "Enviadas" (outbound), "Recebidas" (inbound)
  3. Pass `direction` param to the API fetch: append `&direction=${direction}` to the query string
  4. Include direction in the dependency array for the data fetch useEffect
  5. Style the toggle with Button variant="outline" for inactive and variant="default" for active, in a flex group
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/integration-evolution-wizard && npx next build 2>&1 | tail -5</automated>
    <manual>Open /historico with no campaigns sent — should see helpful empty state with CTA. Toggle between Todas/Enviadas/Recebidas.</manual>
  </verify>
  <done>Empty state shows contextual guidance with campaign CTA. Direction filter toggles between all/outbound/inbound.</done>
</task>

<task type="auto">
  <name>Task 2: Add inbound message support to history API</name>
  <files>src/app/api/messages/history/route.ts</files>
  <action>
  The current API only queries `messageQueue` table (outbound campaign messages). Add support for inbound messages from `conversationMessages` table.

  1. Import `conversationMessages`, `conversations` from `@/db/schema`
  2. Add `direction` query param handling: `const direction = searchParams.get('direction') ?? 'all';`
  3. When direction is 'all' or 'inbound', query inbound messages:
     ```ts
     // Query inbound messages from conversationMessages
     const inboundQuery = db
       .select({
         id: conversationMessages.id,
         voterPhone: conversations.voterPhone,
         voterName: conversations.voterName,
         content: conversationMessages.content,
         createdAt: conversationMessages.createdAt,
         status: sql`'received'`.as('status'),
         direction: sql`'inbound'`.as('direction'),
       })
       .from(conversationMessages)
       .innerJoin(conversations, eq(conversationMessages.conversationId, conversations.id))
       .where(eq(conversationMessages.sender, 'voter'));
     ```
  4. When direction is 'all', merge outbound (messageQueue) + inbound results, sort by createdAt desc, then paginate
  5. When direction is 'outbound', use existing messageQueue-only query
  6. When direction is 'inbound', use only the inbound query
  7. Map inbound rows to the same `MessageHistoryRow` shape the frontend expects, with `direction: 'inbound'` and `status: 'received'`
  8. Add `direction` field to outbound rows as `direction: 'outbound'`
  9. Apply search filter to inbound messages too (search by voterName/voterPhone/content)
  10. Apply date range filter to inbound messages too

  **Keep backward compatible:** If `direction` param is omitted, default to 'all' which returns both outbound and inbound (merged and sorted).
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/integration-evolution-wizard && npx next build 2>&1 | tail -5</automated>
    <manual>Call GET /api/messages/history?direction=inbound — should return conversation messages. Call without direction param — should return both.</manual>
  </verify>
  <done>API returns inbound messages from conversationMessages table. Direction filter works for all/outbound/inbound. Results merged and sorted by createdAt.</done>
</task>

</tasks>

<verification>
- Build passes: `npx next build` completes without errors
- Empty state shows contextual help with campaign CTA
- Direction toggle switches between all/outbound/inbound
- API returns inbound messages alongside outbound
- Search and date filters apply to both directions
</verification>

<success_criteria>
1. Empty history page shows helpful message with CTA to create campaign
2. Direction filter (Todas/Enviadas/Recebidas) toggles message view
3. Inbound messages from conversations visible in history
4. All existing outbound history functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/31-diagnostic-fixes/31-04-SUMMARY.md`
</output>
