---
phase: 13-zero-pendency-release-closure
plan: 04
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified:
  - src/app/relatorios/page.tsx
  - src/app/api/(new report schedule routes)
  - src/db/schema.ts
  - src/lib/(new report scheduling/export helpers)
autonomous: true
requirements: [REP-02]
---

<objective>
Finish report automation so reporting is operationally complete instead of partially manual.

Purpose: current reports already support KPI review and CSV export, but scheduled email delivery remains deferred and PDF/export automation is still incomplete relative to the roadmap promise.

Output: reports can be exported consistently and scheduled for delivery without manual operator intervention.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-zero-pendency-release-closure/13-CONTEXT.md
@.planning/phases/13-zero-pendency-release-closure/13-RESEARCH.md
@.planning/phases/13-zero-pendency-release-closure/13-VALIDATION.md
@src/app/relatorios/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete export automation for reports</name>
  <files>src/app/relatorios/page.tsx, new report export helpers</files>
  <action>
  1. Keep CSV export consistent with the current aggregated reporting contract.
  2. Add the missing export path(s) required to satisfy the roadmap promise, including PDF if still absent.
  3. Make export output deterministic and safe for operational sharing.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <manual>Generate exports from `/relatorios` and verify they reflect the selected period and campaign table data.</manual>
  </verify>
  <done>Reports export behavior is complete and no longer partially manual or format-limited.</done>
</task>

<task type="auto">
  <name>Task 2: Add scheduled email report delivery</name>
  <files>src/db/schema.ts, new report scheduling helpers/routes, src/app/relatorios/page.tsx</files>
  <action>
  1. Add a persisted report scheduling contract with recipients/frequency.
  2. Add the dispatcher path required to send scheduled reports automatically.
  3. Expose the schedule management flow in the product UI.
  4. Support safe dry-run or operator-visible error handling for delivery failures.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Create a report schedule, verify it persists, and confirm the scheduling pipeline can render and dispatch the expected report payload.</manual>
  </verify>
  <done>Scheduled report delivery is part of the shipped product rather than a deferred promise.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit`
- `npm run build`
- Reports export paths are complete and match visible reporting data
- Scheduled email delivery can be configured, persisted, and dispatched safely
</verification>

<success_criteria>
- Reporting no longer has partial automation promises left open.
- Operators can generate and schedule reports without leaving the product.
</success_criteria>
