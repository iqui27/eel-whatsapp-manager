---
phase: 13-zero-pendency-release-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/topbar.tsx
  - src/components/SidebarLayout.tsx
  - src/app/page.tsx
  - src/app/setup/page.tsx
  - src/app/api/setup/route.ts
autonomous: true
requirements: [CLOSE-01, CLOSE-02]
---

<objective>
Close the remaining shell/setup/dashboard contract drift so the product no longer ships demo-only controls or misleading configured-state behavior.

Purpose: the shell is the first thing every operator sees, and `/setup` plus dashboard KPIs still expose unfinished MVP behavior. This plan turns those surfaces into a finished contract or intentionally removes unsupported claims.

Output: topbar/setup/dashboard behave like a real production product, not a partially hardcoded shell.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-zero-pendency-release-closure/13-CONTEXT.md
@.planning/phases/13-zero-pendency-release-closure/13-RESEARCH.md
@.planning/phases/13-zero-pendency-release-closure/13-VALIDATION.md
@src/components/topbar.tsx
@src/components/SidebarLayout.tsx
@src/app/page.tsx
@src/app/setup/page.tsx
@src/app/api/setup/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace demo-only shell controls with a real or intentionally reduced contract</name>
  <files>src/components/topbar.tsx, src/components/SidebarLayout.tsx, src/app/page.tsx</files>
  <action>
  1. Remove hardcoded/demo-only defaults from the topbar where they imply real behavior.
  2. Bind user/profile, alert, period, and search behavior to real application state where the data already exists.
  3. If a control is still unsupported, reduce it explicitly instead of leaving a fake interactive affordance.
  4. Reconcile dashboard KPI cards so unsupported “estimated” metrics are either correctly sourced or intentionally demoted.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <manual>Open the shell on dashboard, CRM, campaigns, and reports and confirm the topbar/dashboard no longer look like demo data.</manual>
  </verify>
  <done>Shell and dashboard surfaces expose only real or intentionally scoped behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Fix configured setup-state UX</name>
  <files>src/app/setup/page.tsx, src/app/api/setup/route.ts</files>
  <action>
  1. Detect configured environments reliably.
  2. Stop rendering the initial setup wizard as if reconfiguration were valid.
  3. Redirect to the correct page or render an explicit already-configured state with the right operator guidance.
  4. Keep first-time setup behavior intact for fresh environments.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Visit `/setup` on a configured environment and confirm the misleading wizard no longer appears.</manual>
  </verify>
  <done>The setup route reflects actual environment state instead of exposing a dead-end wizard in configured environments.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit`
- `npm run build`
- Topbar/search/period/alerts/profile no longer rely on demo defaults
- `/setup` behaves correctly for configured and unconfigured states
- Dashboard KPI contract no longer includes misleading unsupported metrics
</verification>

<success_criteria>
- Operators do not encounter fake shell controls or misleading configured-state UX.
- The dashboard only claims what the product can actually support.
</success_criteria>
