---
phase: 13-zero-pendency-release-closure
plan: 03
type: execute
wave: 2
depends_on: [13-02]
files_modified:
  - src/db/schema.ts
  - src/lib/db-voters.ts
  - src/app/api/voters/route.ts
  - src/app/crm/[id]/page.tsx
  - src/app/conversas/page.tsx
  - src/app/(new mobile surface files)
autonomous: true
requirements: [CRM-01, MOB-01, MOB-02]
---

<objective>
Finish the remaining operator workflows by persisting CRM field notes/checklists and delivering the promised mobile workflows.

Purpose: CRM notes/checklists are still local-only, and the roadmap still promises mobile offline capture plus a mobile priority inbox. This plan closes those operator-facing gaps instead of leaving them as “acceptable for MVP”.

Output: CRM operator notes/checklists become shared persisted data, and the product gains the missing mobile capture/inbox flows.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-zero-pendency-release-closure/13-CONTEXT.md
@.planning/phases/13-zero-pendency-release-closure/13-RESEARCH.md
@.planning/phases/13-zero-pendency-release-closure/13-VALIDATION.md
@src/app/crm/[id]/page.tsx
@src/app/conversas/page.tsx
@src/db/schema.ts
@src/lib/db-voters.ts
@src/app/api/voters/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist CRM checklist and notes server-side</name>
  <files>src/db/schema.ts, src/lib/db-voters.ts, src/app/api/voters/route.ts, src/app/crm/[id]/page.tsx</files>
  <action>
  1. Replace localStorage-only checklist/notes with persisted backend storage.
  2. Keep the workflow lightweight and compatible with the current voter profile page.
  3. Ensure data survives reloads, sessions, and device changes.
  4. Preserve the current operator UX while upgrading the persistence contract.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <manual>Save checklist/notes on a voter, reload, and confirm the data persists from the backend.</manual>
  </verify>
  <done>CRM notes/checklist are part of the shared system record rather than browser-local state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement mobile offline capture and mobile priority inbox workflows</name>
  <files>new mobile-facing page/components/routes, relevant conversations/voters surfaces</files>
  <action>
  1. Add a mobile-friendly offline capture form with online/offline status and sync queue behavior.
  2. Route synced captures into the existing voter/CRM pipeline instead of creating a parallel data path.
  3. Add the promised mobile priority inbox workflow for urgent conversations, quick replies, and handoff visibility.
  4. Keep the mobile flow intentionally scoped to current product goals rather than creating a second full desktop UI.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Use a mobile viewport, simulate offline capture + reconnect sync, and verify urgent conversation handling in the mobile inbox flow.</manual>
  </verify>
  <done>The milestone no longer carries unfinished CRM/mobile operator workflows as deferred work.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit`
- `npm run build`
- CRM notes/checklist persist server-side
- Mobile offline capture supports queue/sync behavior
- Mobile priority inbox exists and supports urgent operator handling
</verification>

<success_criteria>
- Operators do not lose CRM working context across devices or sessions.
- The promised mobile workflows exist in the shipped product instead of the deferred backlog.
</success_criteria>
