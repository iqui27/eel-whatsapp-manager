---
phase: 13-zero-pendency-release-closure
plan: 05
type: execute
wave: 4
depends_on: [13-01, 13-02, 13-03, 13-04]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/13-zero-pendency-release-closure/13-VERIFICATION.md
  - production deploy/runtime assets as needed
autonomous: true
requirements: [REL-01, REL-02]
---

<objective>
End the milestone with real deployment parity, live verification, and an explicit zero-pendency verdict.

Purpose: “no pending items” is not a local-code claim. This plan makes production match the intended head, runs a final full live sweep, and prevents the phase from closing with hidden residual blockers.

Output: the milestone ends with deployed code, live evidence, and either a clean sign-off or an explicit blocking finding that must be fixed before completion.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md
@.planning/phases/13-zero-pendency-release-closure/13-CONTEXT.md
@.planning/phases/13-zero-pendency-release-closure/13-RESEARCH.md
@.planning/phases/13-zero-pendency-release-closure/13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy the intended build and restore production parity</name>
  <files>production host/runtime + any deployment scripts or runtime docs needed</files>
  <action>
  1. Deploy the current intended head for this milestone.
  2. Apply any required database/runtime migrations.
  3. Confirm production routes and APIs reflect the expected contract after deploy.
  4. Record exact environment parity evidence instead of assuming the deploy succeeded.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <manual>Check representative production routes/APIs and confirm they match the just-deployed code path.</manual>
  </verify>
  <done>Production is actually running the intended milestone build.</done>
</task>

<task type="auto">
  <name>Task 2: Execute final live zero-pendency UAT and sign-off</name>
  <files>.planning/phases/13-zero-pendency-release-closure/13-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
  1. Run a page-by-page live verification sweep across all shipped desktop/mobile/operator/admin/reporting surfaces.
  2. Verify auth, authz, CRM, campaigns, realtime conversations, compliance, admin, reports, mobile workflows, and setup/shell behavior.
  3. Fix any residual blockers discovered during this sweep inside the phase instead of carrying them forward implicitly.
  4. Close the phase only when the final ledger supports a real “no pending items” verdict.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Record explicit PASS/FAIL evidence for all shipped routes and end with a final verdict.</manual>
  </verify>
  <done>The milestone ends with an explicit live sign-off rather than another open-ended “next step”.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit`
- `npm run build`
- Production is on the intended head after deploy
- Final live route/API sweep has explicit PASS/FAIL evidence
- Phase closes only if no unresolved pending items remain
</verification>

<success_criteria>
- There is no remaining mismatch between local intent, planning intent, and live production behavior.
- The milestone can honestly be described as closed without hidden deferred work.
</success_criteria>
