---
phase: 13-zero-pendency-release-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db-auth.ts
  - src/lib/db-users.ts
  - src/app/api/users/route.ts
  - src/app/api/voters/route.ts
  - src/app/api/campaigns/route.ts
  - src/app/api/conversations/route.ts
  - src/app/api/compliance/route.ts
  - src/app/admin/page.tsx
autonomous: true
requirements: [SEC-01, SEC-02]
---

<objective>
Finish the authorization story so authenticated access is no longer equivalent to full access.

Purpose: the milestone already shipped users/roles/region scope, but the actual APIs mostly enforce session auth only. This plan makes authorization real and aligns the admin UI with the actual permission contract.

Output: sensitive APIs return `403` when authenticated users lack rights, and the admin/role surfaces describe the real policy instead of a placeholder matrix.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-zero-pendency-release-closure/13-CONTEXT.md
@.planning/phases/13-zero-pendency-release-closure/13-RESEARCH.md
@.planning/phases/13-zero-pendency-release-closure/13-VALIDATION.md
@src/app/admin/page.tsx
@src/app/api/users/route.ts
@src/app/api/voters/route.ts
@src/app/api/campaigns/route.ts
@src/app/api/conversations/route.ts
@src/app/api/compliance/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a reusable authorization policy layer</name>
  <files>src/lib/db-auth.ts, src/lib/db-users.ts, src/app/api/users/route.ts, src/app/api/voters/route.ts, src/app/api/campaigns/route.ts, src/app/api/conversations/route.ts, src/app/api/compliance/route.ts</files>
  <action>
  1. Add reusable role/permission/region-scope authorization helpers on top of the existing session model.
  2. Upgrade sensitive APIs from auth-only to authz-aware behavior.
  3. Return `403 Forbidden` for authenticated users without sufficient rights.
  4. Cover admin-only, coordinator, and region-scoped behavior where relevant.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <manual>Exercise representative APIs with multiple roles and confirm the app distinguishes `401` from `403` correctly.</manual>
  </verify>
  <done>Protected APIs enforce real authorization instead of only checking whether a session exists.</done>
</task>

<task type="auto">
  <name>Task 2: Align admin and UI affordances with the real permission contract</name>
  <files>src/app/admin/page.tsx, relevant protected pages/components</files>
  <action>
  1. Replace the static/informational permissions story in `/admin` with a view that matches the real policy layer.
  2. Hide or disable forbidden UI actions based on the same authorization contract used by the APIs.
  3. Keep the UX understandable by surfacing clear messaging when access is limited by role or region scope.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Open `/admin` and at least one non-admin workflow with different roles and confirm the UI mirrors the enforced permissions.</manual>
  </verify>
  <done>The admin surface and operator affordances reflect the real authorization model, not a placeholder description.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit`
- `npm run build`
- Protected APIs distinguish `401` vs `403`
- Admin/role UI reflects the actual permission contract
- Forbidden actions are hidden or clearly blocked in the UI
</verification>

<success_criteria>
- Roles and region scope materially affect what authenticated users can do.
- The app no longer claims permission behavior that it does not enforce.
</success_criteria>
