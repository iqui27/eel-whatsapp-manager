---
phase: 14-chip-health-reliability
plan: 02
type: execute
wave: 1
depends_on: [14-01]
files_modified:
  - src/app/chips/page.tsx
  - src/app/api/chips/sync/route.ts
  - src/app/api/chips/route.ts
autonomous: false
requirements: [CHIP-06]

must_haves:
  truths:
    - "Chips page shows real-time health status with 7-state color indicators"
    - "Each chip shows messages sent today/limit as a progress bar"
    - "Last health check and last webhook event timestamps are visible"
    - "One-click restart button for disconnected/degraded chips"
    - "Connection status is reliable — no more intermittent false-offline"
  artifacts:
    - path: "src/app/chips/page.tsx"
      provides: "Rebuilt chips management page with health dashboard"
      min_lines: 200
    - path: "src/app/api/chips/sync/route.ts"
      provides: "Updated sync endpoint using new health system"
  key_links:
    - from: "src/app/chips/page.tsx"
      to: "/api/chips"
      via: "fetch + 15s auto-refresh for health data"
      pattern: "fetch.*api/chips"
    - from: "src/app/chips/page.tsx"
      to: "/api/chips/sync"
      via: "manual sync button triggers health check"
      pattern: "fetch.*api/chips/sync"
---

<objective>
Rebuild the chips page as a health monitoring dashboard — each chip shows a colored status indicator (🟢 healthy, 🟡 degraded, 🟠 cooldown, 🔴 quarantined, ⛔ banned, 🔵 warming up, ⚫ disconnected), messages sent today/limit progress bar, hourly rate, last seen timestamp, and one-click restart/reconnect actions.

Purpose: The operator needs to see at a glance which chips are healthy and ready to send, which are having issues, and take immediate action. The current page shows "connected/disconnected" which is unreliable.

Output: Rebuilt chips page with health-first UX
</objective>

<execution_context>
@/Users/hrocha/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-chip-health-reliability/14-01-SUMMARY.md
@src/app/chips/page.tsx
@src/app/api/chips/route.ts
@src/app/api/chips/sync/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update chips API to return health data + add restart endpoint</name>
  <files>src/app/api/chips/route.ts, src/app/api/chips/sync/route.ts</files>
  <action>
**Update GET /api/chips** to include ALL new health fields in the response:
- healthStatus, messagesSentToday, messagesSentThisHour, dailyLimit, hourlyLimit
- lastHealthCheck, lastWebhookEvent, cooldownUntil, bannedAt
- errorCount, blockRate, assignedSegments

**Update POST /api/chips/sync** to use the new health check logic:
1. Load config (apiUrl, apiKey)
2. For each chip, call `getConnectionState()` from the rewritten evolution.ts
3. Check lastWebhookEvent staleness
4. Set healthStatus using the same logic as the cron:
   - open + fresh webhook → healthy
   - open + stale webhook → degraded
   - connecting → attempt restart
   - close → disconnected
5. Update chip in DB
6. Return summary: { synced: N, healthy: N, degraded: N, disconnected: N }

**Add PUT /api/chips/restart endpoint** (or add as action param to existing route):
Accept `{ chipId, action: 'restart' }` in the chips PUT handler.
1. Load the chip by ID
2. Call `restartInstance()` from evolution.ts
3. Wait 5 seconds
4. Call `getConnectionState()` to verify
5. Update healthStatus in DB
6. Return new status

Keep all existing CRUD operations (POST create, PUT update, DELETE) unchanged.
  </action>
  <verify>
    curl -s http://localhost:3000/api/chips 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); print('OK' if isinstance(d, list) else 'FAIL')" || echo "Server not running - check types only: npx tsc --noEmit"
  </verify>
  <done>Chips API returns full health data, sync uses new health logic, restart action available</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild chips page with health monitoring dashboard</name>
  <files>src/app/chips/page.tsx</files>
  <action>
Rewrite `src/app/chips/page.tsx` as a health-first monitoring page.

**Layout:**
- Top: Summary bar — "X healthy, Y degraded, Z disconnected" with colored dots
- "Sincronizar Saúde" button (calls /api/chips/sync)
- Auto-refresh every 15 seconds (fetch /api/chips)

**Chip cards (grid layout, 2-3 per row):**
Each chip rendered as a card with:
1. **Status indicator** — Large colored dot with label:
   - 🟢 Saudável (healthy)
   - 🟡 Degradado (degraded) 
   - 🟠 Descanso (cooldown)
   - 🔴 Quarentena (quarantined)
   - ⛔ Banido (banned)
   - 🔵 Aquecendo (warming_up)
   - ⚫ Desconectado (disconnected)
2. **Chip name** (bold) + phone number
3. **Instance name** (smaller, muted text)
4. **Progress bar** — "Enviados hoje: X / Y" (messagesSentToday / dailyLimit)
   - Green when <70%, yellow 70-90%, red >90%
5. **Hourly rate** — "Esta hora: X / Y" (messagesSentThisHour / hourlyLimit)
6. **Last seen** — "Último check: 30s atrás" (from lastHealthCheck)
7. **Last webhook** — "Último evento: 2min atrás" (from lastWebhookEvent)
   - Show warning icon if >2min (stale)
8. **Error count** badge if > 0
9. **Actions:**
   - "Reiniciar" button (calls restart endpoint) — visible for degraded/disconnected
   - "Habilitar/Desabilitar" toggle
   - "Editar" (existing edit dialog)
   - "Excluir" (existing delete with confirmation)

**Filters:**
- Dropdown: Todos | Saudável | Degradado | Desconectado | Quarentena | Banido | Aquecendo | Descanso
- Search by name/phone (existing)

**Add chip dialog:** Keep existing functionality (name, phone, instanceName) but add:
- dailyLimit field (default 200)
- hourlyLimit field (default 25)

**Empty state:** "Nenhum chip cadastrado. Adicione um chip para começar."

Use the V2 design tokens (warm white bg, slate accents, warm borders). Follow existing component patterns (Card from shadcn, Badge for status).
  </action>
  <verify>
    npx tsc --noEmit 2>&1 | tail -5 && echo "TypeScript OK"
  </verify>
  <done>Chips page shows 7-state health indicators, progress bars for daily/hourly limits, auto-refreshes every 15s, restart button works</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify chip health monitoring on production</name>
  <what-built>Complete chip health monitoring system — Evolution API wrapper, extended schema, health cron, webhook hardening, rebuilt chips page UI</what-built>
  <how-to-verify>
1. Run locally: `npm run dev`
2. Open http://localhost:3000/chips
3. Verify each chip shows colored health status (not the old connected/disconnected)
4. Verify progress bars show messages sent today/limit
5. Verify "Último check" and "Último evento" timestamps update
6. Click "Sincronizar Saúde" — verify statuses refresh
7. If a chip shows degraded/disconnected: click "Reiniciar" and verify it recovers
8. Check that the status no longer shows intermittent false-offline — it should be stable and match what you see in Evolution API dashboard
9. Open Evolution API dashboard and force-disconnect a chip — verify the chips page reflects this within ~30s
  </how-to-verify>
  <resume-signal>Type "approved" if health monitoring works correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors
2. Chips page loads with new health layout
3. Health status matches Evolution API reality
4. Auto-refresh works (status updates without page reload)
5. Restart button triggers instance restart
</verification>

<success_criteria>
- Chip connection status is RELIABLE — no more false-offline reports
- 7-state health visible at a glance with color indicators
- Daily/hourly limits visible per chip
- Operator can restart chips with one click
- Foundation ready for mass messaging engine (Phase 15)
</success_criteria>

<output>
After completion, create `.planning/phases/14-chip-health-reliability/14-02-SUMMARY.md`
</output>
