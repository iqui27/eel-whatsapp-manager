---
phase: 36-dashboard-editorial-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/ChatQueuePanel.tsx
autonomous: true
requirements: [DASH-V2-01, DASH-V2-02, DASH-V2-03, DASH-V2-04]

must_haves:
  truths:
    - "KPI cards have consistent margins, warm V2 Editorial Light styling, and proper alignment in a 4-column grid"
    - "ChatQueuePanel (Fila de Conversas) renders BELOW the Campanhas Ativas table, not in the right sidebar"
    - "All Ações Rápidas links navigate to existing, non-deprecated routes"
    - "Dashboard layout feels cohesive with V2 Editorial Light design tokens"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Rebuilt dashboard layout with relocated ChatQueuePanel and polished KPI cards"
      contains: "ChatQueuePanel"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/ChatQueuePanel.tsx"
      via: "import and render below campaigns table"
      pattern: "ChatQueuePanel"
---

<objective>
Polish the main dashboard to V2 Editorial Light standards — fix KPI card styling/margins, relocate the ChatQueuePanel from right sidebar to below the Campanhas Ativas table, and audit all Ações Rápidas action links for deprecated routes.

Purpose: The dashboard is the first screen operators see. KPI cards currently use bare `rounded-xl border` styling without V2 warmth, the ChatQueuePanel is awkwardly positioned in a narrow right column, and some quick-action links may point to deprecated/moved routes.

Output: A polished, cohesive dashboard that matches V2 Editorial Light direction.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.tsx
@src/components/ChatQueuePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild KPI cards with V2 Editorial Light styling and fix dashboard layout</name>
  <files>src/app/page.tsx</files>
  <action>
  Rebuild the 4 KPI cards (Entregues, Leituras, Respostas, Bloqueios) at the top of src/app/page.tsx:

  1. **KPI Card Styling** — Replace current bare `rounded-xl border` cards with V2 Editorial Light treatment:
     - Background: `bg-[#F8F6F1]` warm white (or `bg-card` if already tokenized)
     - Border: `border-[#E8E4DD]` warm border
     - Padding: consistent `p-5` internal padding
     - Shadow: subtle `shadow-sm` for depth
     - Grid: `grid grid-cols-2 lg:grid-cols-4 gap-4` for responsive 2→4 column layout
     - Each card: label (text-sm text-muted-foreground), value (text-2xl font-semibold), optional trend indicator (colored dot, not emoji)
     - Ensure no NaN in values (division by zero guard already exists, verify it's working)

  2. **Layout Restructure** — Change from current `grid-cols-[1fr_320px]` two-column layout to a single-column flow:
     - KPI cards row (full width, 4-col grid)
     - Campanhas Ativas table (full width)
     - ChatQueuePanel (full width, below campaigns)
     - Move CommandPanel (Ações Rápidas) to a compact horizontal bar above the campaigns table, or keep as a right-column panel if space allows — use judgment, but ChatQueuePanel MUST be below campaigns, not in right sidebar
     - If keeping CommandPanel in right column, only CommandPanel goes there (not ChatQueuePanel)

  3. **Ações Rápidas Audit** — In the CommandPanel, verify all 7 action links:
     - "Aquecer chips" → should link to `/chips` (verify exists)
     - "Nova campanha" → `/campanhas/nova` (verify exists)
     - "Importar eleitores" → `/importar` (verify exists)
     - "Ver compliance" → `/compliance` (verify exists)
     - "Ver relatórios" → `/relatorios` (verify exists)
     - "Captura mobile" → `/mobile/captura` — CHECK if this route exists. If deprecated, either remove this action or update to the correct route
     - "Inbox prioritária" → `/mobile/inbox` — CHECK if this route exists. If deprecated, either remove or update
     - For any link pointing to a non-existent route, replace with the closest valid alternative or remove with a comment explaining why

  Use V2 design tokens throughout: warm backgrounds, warm borders, serif-compatible headings, no emoji indicators (use colored dots or text badges).
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/integration-evolution-wizard && npx next build 2>&1 | tail -5</automated>
    <manual>Dashboard renders with warm-toned KPI cards, ChatQueuePanel below campaigns table, all action links work</manual>
  </verify>
  <done>
  - 4 KPI cards render with V2 Editorial Light warm styling (bg-[#F8F6F1], warm borders, shadow-sm)
  - ChatQueuePanel renders below the Campanhas Ativas section, not in the right sidebar
  - All Ações Rápidas links point to valid, existing routes (deprecated links removed or updated)
  - Layout is responsive (2-col KPIs on mobile, 4-col on desktop)
  - No NaN values in KPI cards
  - Build succeeds without errors
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Dashboard page loads and displays 4 KPI cards with V2 Editorial Light styling
- ChatQueuePanel renders below campaigns table
- All action links in Ações Rápidas navigate to existing pages
</verification>

<success_criteria>
Dashboard renders with polished V2 Editorial Light styling, ChatQueuePanel relocated below campaigns, all links functional.
</success_criteria>

<output>
After completion, create `.planning/phases/36-dashboard-editorial-polish/36-01-SUMMARY.md`
</output>
