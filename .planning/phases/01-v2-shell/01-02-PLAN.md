---
phase: 01-v2-shell
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - src/components/topbar.tsx
  - src/components/SidebarLayout.tsx
autonomous: true
requirements: [SHELL-03, SHELL-04]

must_haves:
  truths:
    - "Topbar shows global search input with placeholder 'Buscar campanha, eleitor ou tag...'"
    - "Topbar shows period selector dropdown (last 7 days default)"
    - "Topbar shows critical alerts badge with count"
    - "Topbar shows user profile/role indicator"
  artifacts:
    - path: "src/components/topbar.tsx"
      provides: "V2 Topbar component with search, period, alerts, profile"
      exports: ["Topbar"]
    - path: "src/components/SidebarLayout.tsx"
      provides: "Updated layout using new Topbar component"
      contains: "Topbar"
  key_links:
    - from: "src/components/topbar.tsx"
      to: "src/components/SidebarLayout.tsx"
      via: "imported and rendered in header slot"
      pattern: "import.*Topbar"
---

<objective>
Build the V2 Topbar component and integrate it into the shell layout.

Purpose: The topbar is the second piece of the V2 shell — it provides global search, period filtering, alert awareness, and user context. Every page sees this bar.
Output: New Topbar component matching Paper wireframe 26T-0 topbar, integrated into SidebarLayout.
</objective>

<execution_context>
@/Users/hrocha/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/SidebarLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create V2 Topbar component</name>
  <files>src/components/topbar.tsx</files>
  <action>
Create a new `src/components/topbar.tsx` client component that renders the V2 topbar matching Paper wireframe node 2A2-0.

The topbar has 4 sections in a horizontal flex row (h-[74px], items-center, px-6, gap-4, border-b border-border bg-background):

**Section 1 — Global Search (flex-1, max-w-[420px]):**
- Styled input with Search icon (from lucide) on the left
- Placeholder: "Buscar campanha, eleitor ou tag..."
- Rounded-lg, bg-muted/50, border-none, h-[42px], text-sm
- On focus: ring-1 ring-ring
- For now this is visual-only (no search logic). Value controlled by local state.

**Section 2 — Period Selector (w-[180px]):**
- Button/dropdown styled element showing "Periodo: ultimos 7 dias"
- Calendar icon (from lucide) on the left
- ChevronDown icon on the right
- bg-transparent, border border-border, rounded-lg, h-[42px], text-sm
- For now this is visual-only — no dropdown menu. Just a styled button.

**Section 3 — Critical Alerts (w-[220px]):**
- Button/indicator showing "2 alertas de bloqueio"
- AlertTriangle icon (from lucide) on the left, in warning color
- bg-warning/10, text-warning, border border-warning/20, rounded-lg, h-[42px], text-sm, font-medium
- For now this is visual-only with hardcoded "2 alertas de bloqueio".

**Section 4 — User Profile (w-[120px]):**
- Shows user role text "Coord. Sul"
- Small avatar circle (32x32) with initials "CS" on the left, bg-primary/10 text-primary
- text-sm, font-medium
- For now this is visual-only with hardcoded role.

Export the component as named export: `export function Topbar() { ... }`

Props (all optional, for future data binding):
- `alertCount?: number` (defaults to 2)
- `alertText?: string` (defaults to "alertas de bloqueio")
- `userRole?: string` (defaults to "Coord. Sul")
- `userInitials?: string` (defaults to "CS")
- `periodLabel?: string` (defaults to "ultimos 7 dias")

Import Lucide icons: Search, Calendar, ChevronDown, AlertTriangle, User.
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/dashboard-v2 && test -f src/components/topbar.tsx && grep -q "Buscar campanha" src/components/topbar.tsx && grep -q "export function Topbar" src/components/topbar.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Topbar component exists with 4 sections (search, period, alerts, profile) matching V2 wireframe</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Topbar into SidebarLayout</name>
  <files>src/components/SidebarLayout.tsx</files>
  <action>
Update SidebarLayout.tsx to use the new Topbar component instead of the current inline header:

1. Import Topbar: `import { Topbar } from './topbar';`

2. Replace the existing `<header>` element (the one with h-14, border-b, containing hamburger + title + CommandTrigger + ThemeToggle) with:

```tsx
<header className="hidden md:block shrink-0">
  <Topbar />
</header>
```

3. Keep a simplified mobile header (md:hidden) that still has the hamburger button, page title, and ThemeToggle. The mobile header stays at h-14. Don't put the full Topbar on mobile — it's too wide.

4. Move the ThemeToggle into the Topbar component itself — add it as a 5th element after the user profile section, with a small gap. This keeps the toggle accessible on desktop.

5. Keep CommandPalette rendered at the bottom of the layout (it's a portal, position doesn't matter).

6. Remove the old CommandTrigger from the header since the global search in Topbar replaces its visual function. Keep the CommandPalette component itself — users can still use Cmd+K.
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/dashboard-v2 && grep -q "import.*Topbar" src/components/SidebarLayout.tsx && echo "PASS" || echo "FAIL"</automated>
    <manual>Desktop shows the new 4-section topbar. Mobile shows simplified header with hamburger. ThemeToggle accessible on both.</manual>
  </verify>
  <done>Desktop layout shows V2 Topbar with search/period/alerts/profile. Mobile shows simplified header. Cmd+K still works.</done>
</task>

</tasks>

<verification>
- App builds without errors
- Desktop: topbar shows search input, "Periodo: ultimos 7 dias", "2 alertas de bloqueio" (amber), "Coord. Sul" with avatar
- Mobile: simplified header with hamburger + title + theme toggle
- Cmd+K command palette still works
- Theme toggle still accessible on both desktop and mobile
- Topbar height is ~74px matching wireframe
</verification>

<success_criteria>
The V2 shell is complete — sidebar + topbar match the Paper wireframe 26T-0. The layout is ready for content pages to be built inside it.
</success_criteria>

<output>
After completion, create `.planning/phases/01-v2-shell/01-02-SUMMARY.md`
</output>
