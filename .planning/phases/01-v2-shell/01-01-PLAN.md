---
phase: 01-v2-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/SidebarLayout.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02]

must_haves:
  truths:
    - "V2 design tokens are active — warm white #F8F6F1 background, slate sidebar, compact density"
    - "Sidebar shows 8 electoral navigation items: Dashboard, Campanhas, Segmentacao, Conversas, CRM, Compliance, Relatorios, Admin"
    - "Sidebar footer shows API status + block risk level"
    - "Brand section shows 'EEL Eleicao' with subtitle 'Operacao WhatsApp'"
  artifacts:
    - path: "src/app/globals.css"
      provides: "V2 design tokens overlaid on existing theme system"
      contains: "--background: #F8F6F1"
    - path: "src/components/SidebarLayout.tsx"
      provides: "V2 sidebar with 8 electoral nav items"
      contains: "Campanhas"
  key_links:
    - from: "src/app/globals.css"
      to: "src/components/SidebarLayout.tsx"
      via: "CSS variables consumed by Tailwind classes"
      pattern: "bg-sidebar|bg-background|text-foreground"
---

<objective>
Update design tokens to V2 Editorial Light direction and rebuild the sidebar with electoral campaign navigation.

Purpose: Establish the visual foundation (tokens) and primary navigation (sidebar) for all subsequent screens. Every page shares this shell.
Output: Updated globals.css with V2 tokens, updated SidebarLayout with 8 electoral nav items matching Paper wireframe 26T-0.
</objective>

<execution_context>
@/Users/hrocha/.config/Claude/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN-BRIEF.md
@src/app/globals.css
@src/components/SidebarLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update design tokens to V2 Editorial Light</name>
  <files>src/app/globals.css</files>
  <action>
Update the CSS custom properties in globals.css to match V2 Editorial Light direction:

**Light mode (:root) changes:**
- `--background`: change from `#FAFAFA` to `#F8F6F1` (warm white, V2 signature)
- `--card`: keep `#FFFFFF` (clean white cards)
- `--sidebar`: change from `#FFFFFF` to `#F2F0EB` (slightly warm sidebar, distinguishes from content)
- `--sidebar-foreground`: keep `#0A0A0A`
- `--sidebar-accent`: change from `#EFF6FF` to `#E8E5DE` (warm active state, not blue-tinted)
- `--sidebar-accent-foreground`: change from `#2563EB` to `#1A1A1A` (dark text on warm bg)
- `--border`: change from `#E5E7EB` to `#E2DFD8` (warm border)
- `--sidebar-border`: change from `#E5E7EB` to `#DDD9D1` (warm sidebar border)

**Keep dark mode (.dark) unchanged** — V2 is primarily a light-mode editorial direction, dark mode remains functional as-is.

**Add new V2-specific utility classes** after the existing utility classes:
```css
/* V2 Editorial Light tokens */
.v2-headline {
  font-family: Georgia, 'Times New Roman', serif;
  letter-spacing: -0.02em;
}
.v2-density-compact {
  --space-card-padding: 12px;
  --space-card-gap: 8px;
  --space-table-row-height: 40px;
}
```

Do NOT remove any existing classes (badges, KPI gradients, shimmer). Only modify the token values listed above and add the new utility classes.
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/dashboard-v2 && grep -q "F8F6F1" src/app/globals.css && grep -q "v2-headline" src/app/globals.css && echo "PASS" || echo "FAIL"</automated>
    <manual>Open the app — background should appear warm white, not cool gray</manual>
  </verify>
  <done>Light mode shows warm white #F8F6F1 background, sidebar has warm tint, V2 utility classes exist</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild sidebar with V2 electoral navigation</name>
  <files>src/components/SidebarLayout.tsx</files>
  <action>
Update SidebarLayout.tsx with the following changes:

**1. Update PageId type and navItems array:**
Replace the current 6-item navigation with 8 electoral items matching Paper wireframe 26T-0:

```typescript
type PageId = 'dashboard' | 'campanhas' | 'segmentacao' | 'conversas' | 'crm' | 'compliance' | 'relatorios' | 'admin' | 'chips' | 'contacts' | 'clusters' | 'history' | 'settings';
```

New navItems (replace existing array):
```
Dashboard    → /           → LayoutDashboard
Campanhas    → /campanhas  → Send (from lucide)
Segmentacao  → /segmentacao → Target (from lucide)
Conversas    → /conversas  → MessageCircle (from lucide)
CRM          → /crm        → UserCheck (from lucide)
Compliance   → /compliance → ShieldCheck (from lucide)
Relatorios   → /relatorios → BarChart3 (from lucide)
Admin        → /admin      → Lock (from lucide)
```

Keep the old pages accessible — add a separator line and secondary nav section below the main 8 items for legacy pages (Chips, Contacts, Clusters, History, Settings) with smaller text and `text-muted-foreground` styling. Label it "Operacional" as a small uppercase section header (11px, muted, tracking-wide).

**2. Update brand section:**
- Change "EEL" to "EEL Eleicao"
- Change "WhatsApp Manager" to "Operacao WhatsApp"

**3. Update sidebar footer:**
Keep the API status indicator. Add a second line below it: "Risco de bloqueio: Baixo" in success green color. This matches the Paper wireframe exactly.

**4. Update mobile BottomNav:**
Show 5 most important electoral items: Dashboard, Campanhas, Conversas, CRM, Relatorios.

**5. Update Lucide imports:**
Add: Send, Target, MessageCircle, UserCheck, ShieldCheck, BarChart3, Lock. Remove any unused imports.

Keep ALL existing animations (Framer Motion sidebar collapse, active bar indicator, mobile drawer, bottom nav). Keep CommandPalette and ThemeToggle in topbar. Do NOT change the overall layout structure (sidebar + main + topbar) — only update content within sidebar and navItems.
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/dashboard-v2 && grep -q "Campanhas" src/components/SidebarLayout.tsx && grep -q "EEL Eleicao" src/components/SidebarLayout.tsx && grep -q "Risco de bloqueio" src/components/SidebarLayout.tsx && echo "PASS" || echo "FAIL"</automated>
    <manual>Navigate the app — sidebar shows 8 electoral items + legacy section, brand says "EEL Eleicao", footer shows API + block risk</manual>
  </verify>
  <done>Sidebar shows 8 electoral navigation items with correct icons, legacy section with old pages, brand updated, footer with block risk indicator, mobile bottom nav with 5 electoral items</done>
</task>

</tasks>

<verification>
- App builds without errors: `npm run build` (or at minimum, TypeScript check passes)
- Light mode background is warm white #F8F6F1
- Sidebar shows 8 electoral items: Dashboard, Campanhas, Segmentacao, Conversas, CRM, Compliance, Relatorios, Admin
- Legacy pages (Chips, Contacts, etc.) still accessible via secondary nav section
- Mobile bottom nav shows 5 electoral items
- Brand: "EEL Eleicao" / "Operacao WhatsApp"
- Footer: API status + "Risco de bloqueio: Baixo"
- All existing pages still render correctly (no broken imports)
</verification>

<success_criteria>
The app shell visually matches V2 Editorial Light direction with warm tokens and electoral navigation. All existing pages remain functional. New navigation items point to routes that may not exist yet (that's expected — they'll 404 until built).
</success_criteria>

<output>
After completion, create `.planning/phases/01-v2-shell/01-01-SUMMARY.md`
</output>
