---
phase: 35-campaign-management
plan: 03
type: execute
wave: 2
depends_on: ["35-02"]
files_modified:
  - src/app/campanhas/nova/page.tsx
  - src/app/campanhas/[id]/editar/page.tsx
  - src/app/campanhas/[id]/agendar/page.tsx
  - src/app/api/campaigns/route.ts
  - src/lib/db-campaigns.ts
autonomous: true
requirements: [CMPFIX-07, CMPFIX-08, CMPFIX-09]

must_haves:
  truths:
    - "Campaign creation/edit form has send config section (batch size, delays, send rate, time windows)"
    - "Campaign scheduling page saves time window and send rate to DB (not just decorative)"
    - "Multi-chip selection shows chip health status next to each chip name"
    - "All send config values persist to campaigns table and survive page reload"
  artifacts:
    - path: "src/app/campanhas/nova/page.tsx"
      provides: "Campaign creation with send config section"
      contains: "batchSize"
    - path: "src/app/campanhas/[id]/editar/page.tsx"
      provides: "Campaign edit with send config section"
      contains: "batchSize"
    - path: "src/app/campanhas/[id]/agendar/page.tsx"
      provides: "Scheduling page that actually persists time windows and send rate"
      contains: "windowStart"
    - path: "src/app/api/campaigns/route.ts"
      provides: "Campaign API accepting new send config fields"
      contains: "batchSize"
    - path: "src/lib/db-campaigns.ts"
      provides: "DB operations including new send config fields"
      contains: "batchSize"
  key_links:
    - from: "src/app/campanhas/nova/page.tsx"
      to: "src/app/api/campaigns/route.ts"
      via: "POST /api/campaigns with send config fields"
      pattern: "batchSize.*minDelayMs"
    - from: "src/app/campanhas/[id]/agendar/page.tsx"
      to: "src/app/api/campaigns/route.ts"
      via: "PUT /api/campaigns with windowStart/windowEnd/sendRate"
      pattern: "windowStart.*windowEnd.*sendRate"
---

<objective>
Add a send configuration panel to campaign creation, edit, and scheduling pages. Replace the decorative scheduling controls with real, persistent config that writes to the new DB fields from plan 35-02.

Purpose: Campaign operators need to control how campaigns are sent — batch size, delays, time windows, chip selection, send rate — instead of all config being hardcoded in the cron.
Output: Working send config UI on create/edit/schedule pages, all values persisted to campaigns table.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-campaign-management/35-02-SUMMARY.md
@src/app/campanhas/nova/page.tsx
@src/app/campanhas/[id]/editar/page.tsx
@src/app/campanhas/[id]/agendar/page.tsx
@src/app/api/campaigns/route.ts
@src/lib/db-campaigns.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update campaign API and DB layer to accept send config fields</name>
  <files>
    src/app/api/campaigns/route.ts
    src/lib/db-campaigns.ts
  </files>
  <action>
    **db-campaigns.ts:** Update the `createCampaign()` and `updateCampaign()` functions to accept and persist the new send config fields:
    - `batchSize`, `minDelayMs`, `maxDelayMs`, `sendRate`, `typingDelayMin`, `typingDelayMax`
    - `maxDailyPerChip`, `maxHourlyPerChip`, `pauseOnChipDegraded`
    - `selectedChipIds`, `chipStrategy`
    - `restPauseEvery`, `restPauseDurationMs`, `longBreakEvery`, `longBreakDurationMs`
    - `circuitBreakerThreshold`
    - `windowStart`, `windowEnd` (these already exist in schema but were never written)

    The existing `chipId` field stays for backward compat. `selectedChipIds` is the new multi-chip field.

    **campaigns/route.ts:** Update POST and PUT handlers:
    - Accept the new fields from request body
    - Pass them through to db-campaigns functions
    - Include them in GET response (they already come from the campaigns table select)

    When reading campaigns for the edit page, make sure all send config fields are returned. They'll have defaults from the schema for old campaigns, so no null handling issues.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Campaign POST/PUT accepts and persists all send config fields. GET returns them.</done>
</task>

<task type="auto">
  <name>Task 2: Add send config UI to create, edit, and schedule pages</name>
  <files>
    src/app/campanhas/nova/page.tsx
    src/app/campanhas/[id]/editar/page.tsx
    src/app/campanhas/[id]/agendar/page.tsx
  </files>
  <action>
    **Create a reusable "Configuração de Envio" section** that appears on all three pages. Use a collapsible card (shadcn Collapsible or details/summary) since most users will want defaults.

    The section contains:

    1. **Velocidade de Envio** — Three preset buttons (Lento/Normal/Rápido) that map to:
       - Lento: batchSize=5, minDelay=30s, maxDelay=90s, restEvery=15, longBreakEvery=60
       - Normal: batchSize=10, minDelay=15s, maxDelay=60s, restEvery=20, longBreakEvery=100
       - Rápido: batchSize=20, minDelay=8s, maxDelay=30s, restEvery=25, longBreakEvery=120
       Plus a "Personalizado" option that reveals the individual fields.

    2. **Janela de Envio** — Two time inputs for windowStart/windowEnd (e.g., 08:00 – 20:00). Replace the fake morning/afternoon/evening buttons in `agendar/page.tsx` with REAL time inputs that save to DB.

    3. **Seleção de Chips** — Multi-select checklist showing all chips with their current health status (colored dot: 🟢=healthy, 🟡=degraded, 🔴=disconnected). Fetch from GET /api/chips. Show `messagesSentToday/dailyLimit` next to each.
       - Chip strategy selector: Rodízio (round_robin) | Menos Carregado (least_loaded) | Afinidade (affinity)

    4. **Limites por Chip** — maxDailyPerChip and maxHourlyPerChip number inputs (with sensible min/max bounds).

    5. **Circuit Breaker** — Threshold percentage input (default 5%). Label: "Pausar envio se taxa de erro ultrapassar X%"

    **On create (nova/page.tsx):** Add the section below the existing message editor. Initialize with "Normal" preset. Submit all fields with the POST.

    **On edit (editar/page.tsx):** Load existing campaign config. Display saved values or defaults. Submit all fields with the PUT.

    **On schedule (agendar/page.tsx):** Replace the decorative time-window buttons and send-rate selector with the REAL controls. The existing `handleSchedule()` must now include windowStart, windowEnd, sendRate, and all config fields in the PUT request.

    Use consistent Portuguese labels. Follow existing page patterns (shadcn components, Tailwind classes, toast.error on failures).
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
    <manual>Navigate to /campanhas/nova — should see "Configuração de Envio" collapsible section. Expand it, change values, create campaign, then edit it — values should be preserved.</manual>
  </verify>
  <done>Send config UI appears on create/edit/schedule pages, values persist to DB, chip health visible in multi-select.</done>
</task>

</tasks>

<verification>
- `npx next build` passes
- Create a campaign → check DB row has batchSize, selectedChipIds, etc.
- Edit existing campaign → send config fields load from DB
- Schedule page → time windows save to windowStart/windowEnd columns (not just local state)
- Chip multi-select shows health status dots and daily usage
</verification>

<success_criteria>
- Campaign creation includes send config with working presets
- All config values round-trip through create → edit → save
- Time windows on scheduling page actually persist (not decorative)
- Chip selection shows health visibility
</success_criteria>

<output>
After completion, create `.planning/phases/35-campaign-management/35-03-SUMMARY.md`
</output>
