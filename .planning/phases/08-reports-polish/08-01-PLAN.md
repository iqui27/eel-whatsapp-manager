# Phase 08 Plan 01: Reports Page

**Phase:** 08-reports-polish
**Plan:** 08-01
**Type:** autonomous
**Goal:** Relatorios page — performance KPI cards, bar/line charts (simulated data), campaign comparison table, and CSV export — REP-01 + REP-02 (MVP)

---

## Context

- `/relatorios` nav item already exists in sidebar (href="/relatorios")
- Campaigns table has: name, status, stats (JSON with sent/delivered/read/replied), createdAt
- No real analytics DB — use simulated/mocked data with realistic shapes (same pattern as campaign monitor)
- Recharts is NOT installed — use pure CSS/SVG bar charts to avoid adding heavy deps
- shadcn Tabs, Table, Badge, Button, Select already installed

---

## Tasks

### Task 1 — KPI summary cards

Create `src/app/relatorios/page.tsx`:
- Header: "Relatórios" + period selector (Últimos 7 dias / 30 dias / Esta campanha)
- 4 KPI cards: Mensagens enviadas, Taxa de entrega, Taxa de resposta, Eleitores alcançados — all with simulated numbers + trend arrow (↑/↓ vs previous period)

### Task 2 — Bar chart (simulated daily send volume)

Inline SVG bar chart (no library):
- 7 bars (Mon–Sun) showing daily message volume
- Each bar = `<rect>` with height proportional to value
- Tooltip on hover (CSS title attribute)
- Labeled x-axis (day names), y-axis (max value)
- Label: "Volume de envios — últimos 7 dias"

### Task 3 — Campaign performance table

Below chart:
- Table: Campaign Name, Status badge, Sent, Delivered (%), Read (%), Replied (%), Date
- Data: fetch from GET /api/campaigns (real data)
- Sort by date desc
- Empty state if no campaigns
- "Exportar CSV" button: downloads campaign performance as CSV

### Task 4 — TypeScript check + build

Run `node_modules/.bin/tsc --noEmit` (0 errors required).
Run `npm run build` (must pass).

---

## Verification

- `/relatorios` loads with 4 KPI cards + bar chart + campaigns table
- Period selector changes KPI display (simulated different numbers)
- CSV export downloads valid file
- TypeScript: 0 errors, build passes

## Success Criteria

- `/relatorios` page working with all sections
- Full build passes cleanly
