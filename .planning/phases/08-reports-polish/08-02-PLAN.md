# Phase 08 Plan 02: Final Polish

**Phase:** 08-reports-polish
**Plan:** 08-02
**Type:** autonomous
**Goal:** Final responsive pass — fix mobile layout gaps, sidebar label truncation, empty states on all main pages, and minor UX polish — POL-01

---

## Context

- All 7 phases shipped. App has 38 pages.
- V2 Editorial Light design direction throughout
- Mobile sidebar: collapsed state uses icon-only nav
- Some pages may have missing `currentPage` prop or broken mobile layout

---

## Tasks

### Task 1 — Sidebar label audit + fix

Check `src/components/SidebarLayout.tsx`:
- Confirm `relatorios` PageId is in the type union and nav array
- All 8 electoral nav items have correct href
- Mobile bottom nav shows correct items (max 5)

### Task 2 — Empty state audit

Visit each main page (code review) and ensure consistent empty states:
- `/campanhas` — ✅ (had empty state from Phase 04)
- `/crm` — ✅ (had empty state)
- `/conversas` — check if empty queue has a message
- `/compliance` — ✅ (had empty state)
- `/admin` — ✅ (had empty state)
- `/relatorios` — check campaigns table empty state (from Plan 08-01)

Add empty state to `/conversas` queue if missing.

### Task 3 — Dashboard "Relatórios" quick-link card

In `src/app/page.tsx` command panel section:
- Ensure "Ver relatórios" quick action links to `/relatorios`
- Ensure "Conformidade" quick action links to `/compliance`
- Check all 4 quick-action buttons have correct hrefs

### Task 4 — TypeScript check + final build

Run `node_modules/.bin/tsc --noEmit` (0 errors).
Run `npm run build` — confirm all pages listed, 0 errors.
Record final page count.

---

## Verification

- Sidebar has `relatorios` in PageId type + nav array
- All pages have empty states
- Dashboard quick-actions link correctly
- Final build: 0 errors

## Success Criteria

- TypeScript: 0 errors
- Build: 0 errors, all pages present
- All polish items resolved
