---
phase: 23-critical-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/setup/page.tsx
  - src/app/conversas/page.tsx
autonomous: true
requirements:
  - LAY-03
  - LAY-04
  - LAY-05

must_haves:
  truths:
    - "Setup page renders correctly using theme tokens in both light and dark mode"
    - "Conversas page height calculation accounts for topbar (74px) and mobile bottom nav (64px)"
    - "Wizard page is wrapped in SidebarLayout with proper sidebar navigation"
  artifacts:
    - path: "src/app/setup/page.tsx"
      provides: "Setup page using Tailwind theme classes instead of hardcoded hex"
      contains: "bg-background"
    - path: "src/app/conversas/page.tsx"
      provides: "Correct height calculation accounting for topbar and bottom nav"
      contains: "calc(100vh"
  key_links:
    - from: "src/app/setup/page.tsx"
      to: "Tailwind theme tokens"
      via: "CSS classes bg-background, text-foreground, border-border, etc."
      pattern: "bg-background|text-foreground|border-border"
---

<objective>
Fix orphaned page wrappers, replace hardcoded hex colors in setup, and correct height calculations for conversas.

Purpose: The setup page uses 46 instances of hardcoded hex colors (#0B1220, #A1A1AA, etc.) bypassing the theme system entirely — it looks broken in light mode. The conversas page uses `h-[calc(100vh-4rem)]` which doesn't account for the actual topbar height (74px) or mobile bottom nav (64px). The wizard page was already wrapped in SidebarLayout in Phase 21 so LAY-04 just needs verification.

Output: Theme-compliant setup page, correct conversas height, verified wizard wrapper
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/setup/page.tsx
@src/app/conversas/page.tsx
@src/app/wizard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all hardcoded hex colors in setup page with Tailwind theme tokens</name>
  <files>src/app/setup/page.tsx</files>
  <action>
Replace all 46 instances of hardcoded hex colors with Tailwind CSS theme classes. The mapping:

**Backgrounds:**
- `bg-[#0B1220]` → `bg-background` (main page bg)
- `bg-[#111827]` → `bg-card` (card/panel bg)
- `bg-[#1F2937]` → `bg-muted` (input bg)
- `bg-[#0F172A]` → `bg-muted/50` (subtle info bg)

**Borders:**
- `border-[#1F2937]` → `border-border`
- `border-[#374151]` → `border-input`
- `border-[#4B5563]` → `border-input hover state`

**Text:**
- `text-white` → `text-foreground`
- `text-[#A1A1AA]` → `text-muted-foreground`
- `text-[#71717A]` → `text-muted-foreground/70`
- `text-[#52525B]` → `text-muted-foreground/60`
- `text-[#4B5563]` → `placeholder:text-muted-foreground/50`
- `text-[#374151]` → `text-muted-foreground/30`

**Interactive:**
- `bg-[#3B82F6]` → `bg-primary`
- `hover:bg-[#2563EB]` → `hover:bg-primary/90`
- `text-[#3B82F6]` → `text-primary`
- `border-[#3B82F6]` → `border-primary`
- `focus:border-[#3B82F6]` → `focus:border-primary`
- `bg-[#22C55E]` → `bg-green-600`
- `hover:bg-[#16A34A]` → `hover:bg-green-700`
- `border-[#22C55E]` → `border-green-600`
- `text-[#EF4444]` → `text-destructive`

**Inputs:** Convert raw `<input>` and `<textarea>` to use consistent theme classes:
- `border border-input bg-muted text-foreground` base pattern
- `focus:border-primary focus:ring-1 focus:ring-primary/20` focus pattern
- `placeholder:text-muted-foreground/50` for placeholders

**Buttons:** Convert raw `<button>` elements to use consistent theme classes:
- Primary: `bg-primary text-primary-foreground hover:bg-primary/90`
- Secondary: `border border-input bg-transparent text-muted-foreground hover:bg-accent`
- Destructive: `bg-green-600 text-white hover:bg-green-700` (the submit/confirm button)

Replace `text-[14px]` → `text-sm`, `text-[13px]` → `text-xs`, `text-[12px]` → `text-[11px]` or `text-xs`, `text-[18px]` → `text-lg`, `text-[28px]` → `text-2xl` to use standard Tailwind type scale.

After all replacements, there should be ZERO hex color references in setup/page.tsx (verify with grep).
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | grep -v "Cannot find module '@/lib/notifications'" | grep -v "Cannot find module '@/lib/group-link-cache'" | grep -c "error TS" || echo "0 errors"</automated>
    <manual>Load /setup page in both light and dark mode — all text and backgrounds should use theme tokens, no hardcoded dark-only colors</manual>
  </verify>
  <done>Setup page has zero hardcoded hex colors. Renders correctly in both light and dark mode using Tailwind theme tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Fix conversas height calc and verify wizard SidebarLayout</name>
  <files>src/app/conversas/page.tsx</files>
  <action>
1. **Fix conversas height calculation** (line ~652):
   Change `h-[calc(100vh-4rem)]` to `h-[calc(100vh-74px)]` to account for the actual topbar height.
   
   Also add a CSS variable-based approach for mobile: On mobile, bottom nav is 64px, so the height should be `h-[calc(100vh-74px-64px)] md:h-[calc(100vh-74px)]`. Use a simpler approach: add `pb-16 md:pb-0` to the main container to account for mobile bottom nav overlap instead of complex calc.

2. **Verify wizard page** already has SidebarLayout wrapper (it was added in Phase 21 plan 21-05). If it does, LAY-04 is satisfied. If not, wrap the wizard page content in `<SidebarLayout currentPage="wizard" pageTitle="Assistente de Setup">`.

No other files need changes for this task.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | grep -v "Cannot find module '@/lib/notifications'" | grep -v "Cannot find module '@/lib/group-link-cache'" | grep -c "error TS" || echo "0 errors"</automated>
    <manual>Load /conversas on mobile viewport (375px) — message area should not overlap with bottom nav. Load /wizard — sidebar should be visible.</manual>
  </verify>
  <done>Conversas page height accounts for topbar and mobile bottom nav. Wizard page renders inside SidebarLayout with sidebar navigation visible.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit` passes (ignoring known pre-existing errors)
- `grep -c '#[0-9A-Fa-f]' src/app/setup/page.tsx` returns 0
- Conversas page has height calc with 74px topbar value
- Wizard page has SidebarLayout import and wrapper
</verification>

<success_criteria>
- Setup page has zero hardcoded hex colors and renders in both light/dark mode
- Conversas height is correct for desktop (74px topbar) and mobile (74px + 64px bottom nav)
- Wizard page is wrapped in SidebarLayout
- No TypeScript compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/23-critical-layout/23-02-SUMMARY.md`
</output>
