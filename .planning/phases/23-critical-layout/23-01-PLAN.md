---
phase: 23-critical-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/topbar.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - LAY-01
  - LAY-02

must_haves:
  truths:
    - "Topbar does not horizontally overflow on screens between 768px and 1200px"
    - "Dashboard right panel chat queue scrolls vertically when content exceeds viewport"
    - "Topbar sections collapse gracefully on medium screens"
  artifacts:
    - path: "src/components/topbar.tsx"
      provides: "Responsive topbar with flex-shrink and breakpoint-aware sections"
      contains: "flex-shrink"
    - path: "src/app/page.tsx"
      provides: "Dashboard right panel with overflow-y-auto and max-height"
      contains: "overflow-y-auto"
  key_links:
    - from: "src/components/topbar.tsx"
      to: "SidebarLayout"
      via: "Component rendered inside layout"
      pattern: "Topbar"
    - from: "src/app/page.tsx"
      to: "ChatQueuePanel"
      via: "Rendered in right column with scroll container"
      pattern: "ChatQueuePanel"
---

<objective>
Fix topbar responsive overflow and dashboard right panel scroll — the two most visible layout bugs affecting every page.

Purpose: The topbar currently has 6 sections with fixed widths totaling >1100px minimum, causing horizontal overflow on screens < 1200px. The dashboard right panel uses `lg:sticky lg:top-24` but has no max-height or overflow, so the chat queue gets cut off when content is long. These are foundational layout fixes that must land before any page-specific work.

Output: Responsive topbar that works down to 768px, scrollable dashboard right panel
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/topbar.tsx
@src/app/page.tsx
@src/components/ChatQueuePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make topbar responsive with flex-shrink and breakpoint hiding</name>
  <files>src/components/topbar.tsx</files>
  <action>
Refactor the topbar's 6 sections to use responsive flex layout instead of fixed pixel widths:

1. **Section 1 (Search)**: Keep `flex-1` but remove `max-w-[420px]`. Add `min-w-[180px]` so it shrinks gracefully.

2. **Section 2 (Date)**: Change from `w-[220px]` to `w-auto min-w-fit`. Hide the day-of-week text on screens < 1024px using `hidden lg:inline`.

3. **Section 3 (Operational status)**: Change from `w-[240px]` to `max-w-[240px] flex-shrink`. On screens < 1024px, hide the text and show only the icon using responsive classes (`hidden lg:inline` on the span, keep AlertIcon always visible).

4. **Section 4 (Session context)**: Change from `w-[180px]` to `w-auto`. Hide the pageTitle line on screens < 1024px (`hidden lg:block` on the first `<p>`).

5. **Sections 5 & 6 (Notification + Theme)**: Already `shrink-0`, these are fine.

6. **Container**: Keep `gap-4` but add `gap-2 lg:gap-4` for tighter spacing on medium screens. Add `overflow-hidden` to prevent any edge-case overflow.

Ensure no fixed widths remain that would cause overflow. The total must fit comfortably at 768px.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | grep -v "Cannot find module '@/lib/notifications'" | grep -v "Cannot find module '@/lib/group-link-cache'" | grep -c "error TS" || echo "0 errors"</automated>
    <manual>Resize browser to 768px — topbar should not overflow or show horizontal scrollbar</manual>
  </verify>
  <done>Topbar renders without horizontal overflow at 768px, 1024px, and 1440px. Sections collapse gracefully with hidden text on smaller screens.</done>
</task>

<task type="auto">
  <name>Task 2: Add max-height and scroll to dashboard right panel</name>
  <files>src/app/page.tsx</files>
  <action>
Fix the dashboard right column so it scrolls when content exceeds viewport:

1. **Right panel container** (line ~620): Change from `lg:sticky lg:top-24 h-fit space-y-4` to `lg:sticky lg:top-24 space-y-4 max-h-[calc(100vh-74px-48px)] overflow-y-auto`. The 74px accounts for topbar height, 48px for top padding.

2. Add `pr-1` to the right panel container so scrollbar doesn't overlap content.

3. Add `pb-4` at the bottom of the right panel so the last element (ChatQueuePanel) has breathing room when scrolled to bottom.

4. Ensure both `CommandPanel` and `ChatQueuePanel` retain their existing styling — only the outer container changes.

The right panel should now scroll independently when its content (Command Panel + Chat Queue) exceeds the available height.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | grep -v "Cannot find module '@/lib/notifications'" | grep -v "Cannot find module '@/lib/group-link-cache'" | grep -c "error TS" || echo "0 errors"</automated>
    <manual>Load dashboard with chat queue entries — right panel should scroll independently without cutting off content</manual>
  </verify>
  <done>Dashboard right panel scrolls vertically when content exceeds viewport. CommandPanel and ChatQueuePanel are both fully accessible via scroll.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit` passes (ignoring known pre-existing errors)
- Topbar has no fixed widths > 240px
- Dashboard right panel has `overflow-y-auto` and `max-h-[calc(...)]`
</verification>

<success_criteria>
- Topbar renders without horizontal overflow on screens 768px to 1440px+
- Dashboard right panel scrolls when content exceeds viewport height
- No TypeScript compilation errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/23-critical-layout/23-01-SUMMARY.md`
</output>
