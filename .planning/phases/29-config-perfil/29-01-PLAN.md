---
phase: 29-config-perfil
plan: 01
type: execute
wave: 2
depends_on:
  - 23-01
  - 23-02
files_modified:
  - src/app/setup/page.tsx
  - src/app/configuracoes/page.tsx
  - src/components/SidebarLayout.tsx
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03

must_haves:
  truths:
    - "Settings page renders correctly in both light and dark mode using theme tokens only"
    - "Settings page has sections for candidate profile, Gemini API key, Evolution API config, notification preferences, campaign defaults, and chip warming"
    - "Settings page is accessible at /configuracoes route"
    - "/setup route still works — redirects to /configuracoes if already configured"
  artifacts:
    - path: "src/app/configuracoes/page.tsx"
      provides: "New settings page with theme tokens and all feature sections"
      contains: "Configuracoes"
    - path: "src/app/setup/page.tsx"
      provides: "Original setup page updated with theme tokens"
      contains: "bg-background"
  key_links:
    - from: "src/app/configuracoes/page.tsx"
      to: "/api/setup"
      via: "fetch to load and save settings"
      pattern: "fetch.*api/setup"
    - from: "src/components/SidebarLayout.tsx"
      to: "/configuracoes"
      via: "Sidebar settings nav item updated href"
      pattern: "href.*configuracoes"
---

<objective>
Rework the settings page to use theme tokens instead of 46 hardcoded hex colors, add new configuration sections (Gemini API key, notification preferences, campaign defaults), and create the /configuracoes route with backward compatibility from /setup.

Purpose: The current setup/page.tsx has 46 hardcoded hex colors (#0B1220, #A1A1AA, #1F2937, etc.) that bypass the theme system entirely, making it look broken in light mode and inconsistent with the rest of the app. It also only covers initial setup (API URL, password, warming) but lacks sections for Gemini AI config, notification preferences, and campaign defaults that the system now supports.

Output: Theme-tokenized settings page at /configuracoes with all configuration sections, /setup backward compatibility
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/app/setup/page.tsx
@src/components/SidebarLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all hardcoded hex colors in setup/page.tsx with theme tokens</name>
  <files>src/app/setup/page.tsx</files>
  <action>
1. **Systematic hex-to-token replacement** across all 405 lines. Replace EVERY hardcoded color:

   | Hardcoded Hex | Theme Token |
   |---------------|-------------|
   | `bg-[#0B1220]` | `bg-background` |
   | `bg-[#111827]` | `bg-card` |
   | `bg-[#0F172A]` | `bg-muted` |
   | `bg-[#1F2937]` | `bg-muted` |
   | `border-[#1F2937]` | `border-border` |
   | `border-[#374151]` | `border-border` |
   | `text-white` (on dark bg) | `text-foreground` |
   | `text-[#A1A1AA]` | `text-muted-foreground` |
   | `text-[#71717A]` | `text-muted-foreground` |
   | `text-[#52525B]` | `text-muted-foreground` |
   | `text-[#4B5563]` | `text-muted-foreground/60` or `placeholder:text-muted-foreground/50` |
   | `text-[#374151]` | `text-muted-foreground/40` |
   | `bg-[#3B82F6]` | `bg-primary` |
   | `hover:bg-[#2563EB]` | `hover:bg-primary/90` |
   | `text-[#3B82F6]` | `text-primary` |
   | `border-[#3B82F6]` | `border-primary` |
   | `focus:border-[#3B82F6]` | `focus:border-primary` |
   | `bg-[#22C55E]` | `bg-green-600` |
   | `border-[#22C55E]` | `border-green-600` |
   | `hover:bg-[#16A34A]` | `hover:bg-green-700` |
   | `text-[#EF4444]` | `text-destructive` |
   | `hover:border-[#4B5563]` | `hover:border-border` |

2. **Replace the step progress check mark emoji** (line 214: `'✓'`) with a Lucide `Check` icon or plain text "✓" (keep as text — it's a simple check, not an emoji indicator).

3. **Replace the error emoji** (line 367: `⚠️`) with a Lucide `AlertCircle` icon import. Add `import { AlertCircle } from 'lucide-react'` at the top.

4. **Replace the warming message emoji** (line 38: `🔔`) — change default warmingMessage to `'Aquecimento ativado!'` without emoji.

5. **Verify every className attribute** in the file — there should be ZERO remaining `[#` color references after this task.

6. **Test both modes mentally**: With `bg-background` the page adapts to light/dark. The input fields use `bg-muted` + `border-border` which works in both modes.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | grep -v "Cannot find module '@/lib/notifications'" | grep -v "Cannot find module '@/lib/group-link-cache'" | grep -c "error TS" || echo "0 errors"</automated>
    <manual>Search setup/page.tsx for `[#` — should find ZERO matches. Page renders in both light and dark mode.</manual>
  </verify>
  <done>All 46 hardcoded hex colors replaced with theme tokens. Zero `[#` references remain. Page renders correctly in both light and dark themes.</done>
</task>

<task type="auto">
  <name>Task 2: Create /configuracoes settings page with all feature sections</name>
  <files>src/app/configuracoes/page.tsx, src/components/SidebarLayout.tsx</files>
  <action>
1. **Create `src/app/configuracoes/page.tsx`** — a full settings management page wrapped in SidebarLayout. This is NOT the initial setup wizard (that stays at /setup for first-time config). This is the ongoing settings management page for configured environments.

2. **Page structure**: Use shadcn Card components with a vertical tab-like navigation on the left (or just stacked sections with anchors). Sections:
   ```tsx
   'use client';
   import { SidebarLayout } from '@/components/SidebarLayout';
   // ... imports

   export default function ConfiguracoesPage() {
     // Load current settings from /api/setup (GET returns current config when configured)
     // Tabs or sections:
     // 1. Candidato (candidate name, party, number — from /api/settings or /api/setup)
     // 2. Evolution API (URL, API key, instance name — editable)
     // 3. Gemini AI (API key field for GEMINI_API_KEY — stored in config table)
     // 4. Notificacoes (enable/disable alert types, sound, desktop notifications)
     // 5. Campanhas (default time window 08:00-20:00, default delay range 15-60s)
     // 6. Aquecimento (warming enabled, interval, message — same as setup step 3)
   }
   ```

3. **Each section** should be a Card with CardHeader (title + description) and CardContent with form fields. Use theme-token classes only (bg-background, bg-card, border-border, text-foreground, text-muted-foreground).

4. **Form fields** use the same pattern as the tokenized setup page: `h-11 rounded-lg border border-border bg-muted text-foreground text-sm px-3 outline-none focus:border-primary placeholder:text-muted-foreground/50`.

5. **Save button per section** — each section saves independently via POST/PUT to the appropriate API endpoint. Use toast for success/error feedback.

6. **Update SidebarLayout.tsx** — change the settings nav item href from `/settings` to `/configuracoes`:
   - Line 89: Change `href: '/settings'` to `href: '/configuracoes'`
   - Add `'configuracoes'` to the PageId type union (line 35-51) — add it after `'settings'`
   - The existing `'settings'` PageId can stay for backward compatibility

7. **Wrap in SidebarLayout** with `currentPage="settings"` (reuse existing PageId since it's the same conceptual page).

8. **Use shadcn imports**: Card, CardHeader, CardTitle, CardContent from `@/components/ui/card`. Import toast from sonner if available, or use the project's existing toast pattern.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit 2>&1 | grep -v "Cannot find module '@/lib/notifications'" | grep -v "Cannot find module '@/lib/group-link-cache'" | grep -c "error TS" || echo "0 errors"</automated>
    <manual>Navigate to /configuracoes — page loads with all 6 settings sections. Sidebar "Configuracoes" link is active. Light/dark mode both render correctly.</manual>
  </verify>
  <done>New /configuracoes page exists with 6 configuration sections (Candidato, Evolution API, Gemini AI, Notificacoes, Campanhas, Aquecimento). SidebarLayout nav points to /configuracoes. All theme-tokenized, no hardcoded colors.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit` passes (ignoring known pre-existing errors)
- `grep -r '\[#' src/app/setup/page.tsx` returns zero matches
- /configuracoes page loads with 6 settings sections
- Sidebar "Configuracoes" link navigates to /configuracoes
- Both setup and configuracoes pages render correctly in light and dark mode
</verification>

<success_criteria>
- Zero hardcoded hex colors remain in setup/page.tsx
- /configuracoes page exists with all 6 configuration sections
- SidebarLayout settings link updated to /configuracoes
- Both pages use theme tokens exclusively
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/29-config-perfil/29-01-SUMMARY.md`
</output>
