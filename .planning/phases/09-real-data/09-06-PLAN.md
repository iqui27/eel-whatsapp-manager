---
phase: 09-real-data
plan: 06
type: execute
wave: 2
depends_on: [09-02, 09-05]
files_modified:
  - src/app/page.tsx
  - src/app/relatorios/page.tsx
  - src/app/campanhas/[id]/monitor/page.tsx
  - src/app/campanhas/page.tsx
autonomous: true
requirements: [RD-17, RD-18, RD-19, RD-20]

must_haves:
  truths:
    - "Dashboard KPIs (delivered, open rate, responses, blocks) come from real campaign stats"
    - "Reports page KPIs and bar chart come from real DB aggregates"
    - "Monitor page delivery log shows real send events, not hardcoded entries"
    - "Campaign list shows segment name instead of UUID"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Dashboard with real KPIs from campaign aggregates"
      contains: "totalDelivered|totalSent"
    - path: "src/app/relatorios/page.tsx"
      provides: "Reports with real KPIs and bar chart from DB"
      contains: "campaigns"
    - path: "src/app/campanhas/[id]/monitor/page.tsx"
      provides: "Real delivery log"
      contains: "totalSent"
  key_links:
    - from: "src/app/page.tsx"
      to: "/api/campaigns"
      via: "Aggregate stats from all campaigns"
      pattern: "api/campaigns"
    - from: "src/app/relatorios/page.tsx"
      to: "/api/campaigns"
      via: "Aggregate stats for KPI cards and bar chart"
      pattern: "api/campaigns"
---

<objective>
Replace all hardcoded/mock data in the dashboard, reports, and monitor pages with real campaign statistics from the database.

Purpose: After Plans 02 and 05 fix the send pipeline and segmentation, campaigns will have real stats (totalSent, totalDelivered, totalFailed, totalReplied). This plan makes those stats visible across dashboard KPIs, reports, and the campaign monitor.

Output: Dashboard, reports, and monitor pages all display real data from the database. No more fabricated constants.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-real-data/09-02-SUMMARY.md
@.planning/phases/09-real-data/09-05-SUMMARY.md
@src/app/page.tsx
@src/app/relatorios/page.tsx
@src/app/campanhas/[id]/monitor/page.tsx
@src/app/campanhas/page.tsx
@src/lib/db-campaigns.ts
@src/lib/db-voters.ts
@src/lib/db-segments.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard + Reports real KPIs from campaign aggregates</name>
  <files>src/app/page.tsx, src/app/relatorios/page.tsx</files>
  <action>
  **Dashboard (page.tsx):**
  1. Remove `const openRate = 62` (line ~379) and all other hardcoded KPI values
  2. Fetch campaign stats: `GET /api/campaigns` → aggregate across all campaigns (or campaigns within selected period):
     - Total Enviadas = SUM(totalSent)
     - Total Entregues = SUM(totalDelivered)
     - Total Respostas = SUM(totalReplied)
     - Total Bloqueios = SUM(totalFailed)
     - Taxa de entrega = totalDelivered / totalSent * 100 (if totalSent > 0)
     - Taxa de abertura: WhatsApp doesn't have true open tracking — show "N/D" or calculate delivered/sent as a proxy, with "(estimado)" label
  3. The KPI cards should use these real aggregates
  4. For voter-related KPIs (total voters, etc.): fetch `GET /api/voters?limit=1` to get the `total` from paginated response (from Plan 04)
  5. Handle zero state gracefully: if no campaigns exist, show 0 values with "Nenhuma campanha enviada"

  **Reports (relatorios/page.tsx):**
  1. Remove the `KPI_DATA` and `DAILY_BARS` hardcoded constants
  2. Fetch all campaigns (or filtered by period selector): `GET /api/campaigns`
  3. Aggregate for KPI cards: same logic as dashboard — sum totalSent, totalDelivered, totalFailed, totalReplied
  4. For the bar chart (daily send volume):
     - Group campaigns by `updatedAt` date (the send completion date)
     - For each day: sum totalSent for that day
     - Generate bar data array: `[{ date: 'DD/MM', value: totalSent }]`
     - If no campaigns on a given day, show 0
     - Use the last 7 or 14 days based on period selector
  5. Campaign performance table: already uses real data from campaigns list — just verify segment name is shown (not UUID, see Issue 27)
  6. CSV export: ensure it uses the same real aggregated data (not the old fake constants)
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <done>Dashboard and reports pages show real campaign statistics aggregated from DB. No hardcoded KPI_DATA or DAILY_BARS constants remain.</done>
</task>

<task type="auto">
  <name>Task 2: Monitor delivery log + campaign list segment name</name>
  <files>src/app/campanhas/[id]/monitor/page.tsx, src/app/campanhas/page.tsx</files>
  <action>
  **Monitor page (monitor/page.tsx):**
  1. Remove the 5 hardcoded delivery log entries
  2. The monitor page polls campaign stats already — use the real `totalSent`, `totalDelivered`, `totalFailed`, `totalReplied` from the campaign record
  3. For the delivery log timeline: since we don't have per-message delivery tracking (Evolution API doesn't provide granular webhooks for each message), show aggregate stats that update on each poll:
     - "Enviando... {totalSent} mensagens para {segmentName}"
     - "Entregues: {totalDelivered}/{totalSent}"
     - "Falhas: {totalFailed}"
     - "Respostas: {totalReplied}"
  4. Show progress bar: `totalDelivered / totalSent * 100`
  5. When campaign status becomes `'sent'`, show completion summary
  6. Fetch segment name: resolve `campaign.segmentId` → `GET /api/segments?id={segmentId}` or fetch segment name inline

  **Campaign list (campanhas/page.tsx):**
  1. Issue 27: Campaign list currently shows the raw segment UUID instead of the segment name
  2. When fetching campaigns, also fetch segments: `GET /api/segments`
  3. Build a lookup map: `segmentId → segmentName`
  4. In the campaign table, replace the UUID display with the segment name
  5. If segment not found (deleted), show "Segmento removido" in muted text
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit && npm run build</automated>
  </verify>
  <done>Monitor page shows real send progress (not hardcoded log), campaign list shows segment name instead of UUID. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Dashboard has no hardcoded `openRate = 62` or similar constants
- Reports has no `KPI_DATA` or `DAILY_BARS` constants
- Monitor page delivery log updates from real campaign stats
- Campaign list shows segment name, not UUID
</verification>

<success_criteria>
All data-displaying pages (dashboard, reports, campaign list, monitor) show real statistics from the database. Zero hardcoded mock values remain in any KPI card, chart, or table.
</success_criteria>

<output>
After completion, create `.planning/phases/09-real-data/09-06-SUMMARY.md`
</output>
