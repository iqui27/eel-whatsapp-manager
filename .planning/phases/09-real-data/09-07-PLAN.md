---
phase: 09-real-data
plan: 07
type: execute
wave: 2
depends_on: [09-02, 09-04]
files_modified:
  - src/app/campanhas/[id]/editar/page.tsx
  - src/app/crm/[id]/page.tsx
autonomous: true
requirements: [RD-21, RD-22, RD-23]

must_haves:
  truths:
    - "Edit campaign page loads campaign data and allows editing (not a stub redirect)"
    - "Voter profile 'Ver conversas' button filters conversations by voterId"
    - "Voter profile 'Criar campanha' button pre-fills voter segment"
  artifacts:
    - path: "src/app/campanhas/[id]/editar/page.tsx"
      provides: "Working campaign edit page"
      contains: "campaigns"
    - path: "src/app/crm/[id]/page.tsx"
      provides: "Voter profile with working action links"
      contains: "conversas|campanha"
  key_links:
    - from: "src/app/campanhas/[id]/editar/page.tsx"
      to: "/api/campaigns"
      via: "GET to load, PUT to save"
      pattern: "api/campaigns"
    - from: "src/app/crm/[id]/page.tsx"
      to: "/conversas"
      via: "Link with voterId query parameter"
      pattern: "conversas.*voterId"
---

<objective>
Fix campaign edit page (currently a stub redirect) and voter profile action links (conversations and campaign creation).

Purpose: Campaign editing is essential for correcting mistakes before send. Voter profile action links ("Ver conversas" and "Criar campanha") currently don't work — they should deep-link with voter context to enable seamless workflow from CRM to conversations/campaigns.

Output: Working campaign editor and functional voter-to-conversations/campaigns links.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-real-data/09-02-SUMMARY.md
@.planning/phases/09-real-data/09-04-SUMMARY.md
@src/app/campanhas/[id]/editar/page.tsx
@src/app/campanhas/nova/page.tsx
@src/app/crm/[id]/page.tsx
@src/lib/db-campaigns.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement campaign edit page</name>
  <files>src/app/campanhas/[id]/editar/page.tsx</files>
  <action>
  Replace the stub redirect with a real edit page:

  1. **Load campaign data:** On mount, fetch `GET /api/campaigns?id={id}` (or if the API doesn't support ?id= param, add it similar to what Plan 04 does for voters)
     - Actually check: the campaigns API route likely already supports getting by ID via the main route or a sub-route. If not, add `?id=` support to GET handler.
  2. **Reuse the campaign editor UI from `nova/page.tsx`:**
     - The simplest approach: extract the editor form into a shared component, or duplicate the form logic with pre-filled values
     - Pre-fill: name, template (message body), segmentId, variables, abTest settings
     - Show the WhatsApp preview panel (same as nova page)
     - Show the CTA score (same as nova page)
  3. **Save changes:** On submit, PUT to `/api/campaigns` with `{ id, ...updatedFields }`
  4. **Status guard:** If campaign status is `'sent'` or `'sending'`, show read-only view with a banner "Campanha já enviada — edição não disponível"
  5. **After save:** Redirect to campaign list with success toast
  6. **Keep the V2 Editorial Light styling** consistent with the rest of the app (warm borders, serif headlines)
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <done>Campaign edit page loads campaign data, allows editing template/name/segment, saves via PUT, guards against editing sent campaigns</done>
</task>

<task type="auto">
  <name>Task 2: Fix voter profile action links</name>
  <files>src/app/crm/[id]/page.tsx</files>
  <action>
  Fix the action buttons in the voter profile page:

  **"Ver conversas" button (Issue 22):**
  1. Currently links to `/conversas` without any filter
  2. Change to link to `/conversas?voterId={voter.id}`
  3. The conversations page already supports `?voterId=` filtering in its GET handler (check route.ts line ~22: `const voterId = searchParams.get('voterId')`)
  4. The conversations page UI needs to read this param on mount and filter the conversation list accordingly
  5. If no conversations exist for this voter, show "Nenhuma conversa encontrada" with a "Iniciar conversa" button that opens the new conversation dialog pre-filled with this voter

  **"Criar campanha" button (Issue 23):**
  1. Currently links to `/campanhas/nova` without any context
  2. Change to link to `/campanhas/nova?voterId={voter.id}&voterName={voter.name}`
  3. In the campaign editor (nova/page.tsx): read these query params and if present, show a hint: "Campanha direcionada para {voterName}" — but note that campaigns target segments, not individual voters. The practical approach:
     - If voterId is provided, suggest creating a new segment with just this voter, or show a note "Para enviar para um eleitor específico, crie um segmento primeiro"
     - This is a UX hint, not a functional requirement — the link just navigates with context

  **Conversations page voterId filter (supporting change):**
  1. In `src/app/conversas/page.tsx`: read `?voterId=` from URL on mount
  2. If present, pass it to the conversations fetch: `GET /api/conversations?voterId={voterId}`
  3. Show a filter badge: "Filtrado por: {voterName}" with an "X" to clear
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit && npm run build</automated>
  </verify>
  <done>"Ver conversas" opens conversations filtered by voter, "Criar campanha" navigates with voter context. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Campaign edit page at `/campanhas/{id}/editar` loads and allows editing
- Voter profile "Ver conversas" links to `/conversas?voterId={id}`
- Voter profile "Criar campanha" links to `/campanhas/nova?voterId={id}`
</verification>

<success_criteria>
Campaign edit page is functional (not a stub), voter profile action buttons link to the correct pages with voter context, enabling seamless CRM-to-action workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/09-real-data/09-07-SUMMARY.md`
</output>
