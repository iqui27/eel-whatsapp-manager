---
phase: 37-operacoes-layout-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/operacoes/page.tsx
  - src/components/chip-health-grid.tsx
  - src/components/campaign-progress-bars.tsx
  - src/components/alerts-panel.tsx
  - src/components/group-capacity-grid.tsx
  - src/components/conversion-kpis.tsx
  - src/components/message-feed.tsx
  - src/components/system-status-card.tsx
  - src/components/next-actions-panel.tsx
  - src/components/quick-actions-panel.tsx
  - src/components/help-panel.tsx
autonomous: true
requirements: [OPS-V2-01, OPS-V2-02, OPS-V2-03, OPS-V2-04]

must_haves:
  truths:
    - "All Operações sub-components have consistent card styling, readable text, and no truncation"
    - "Every component displays real data from APIs, not hardcoded values"
    - "Layout is readable on screens 1024px+ with proper spacing and visual hierarchy"
    - "No emoji indicators — all status uses colored dots or text badges"
  artifacts:
    - path: "src/app/operacoes/page.tsx"
      provides: "Polished operations page layout"
    - path: "src/components/chip-health-grid.tsx"
      provides: "ChipHealthGrid with clean table layout"
    - path: "src/components/message-feed.tsx"
      provides: "MessageFeed with flexible column widths"
  key_links:
    - from: "src/app/operacoes/page.tsx"
      to: "/api/dashboard/operations"
      via: "fetch in useEffect"
      pattern: "api/dashboard/operations"
    - from: "src/app/operacoes/page.tsx"
      to: "/api/dashboard/kpis"
      via: "fetch in useEffect"
      pattern: "api/dashboard/kpis"
---

<objective>
Improve the layout, readability, and data fidelity of the Operações page and all its sub-components — verify every component shows real data, fix spacing/truncation issues, and ensure consistent V2 Editorial Light styling.

Purpose: The Operações page is the operational command center. It currently has 8+ sub-components that need layout polish, readability improvements, and real-data verification. Components may have text truncation, inconsistent card styling, or stale placeholder data.

Output: A polished, data-verified Operações page with consistent visual hierarchy.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/operacoes/page.tsx
@src/components/chip-health-grid.tsx
@src/components/campaign-progress-bars.tsx
@src/components/message-feed.tsx
@src/components/system-status-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix layout/styling of all Operações sub-components</name>
  <files>
    src/app/operacoes/page.tsx
    src/components/chip-health-grid.tsx
    src/components/campaign-progress-bars.tsx
    src/components/alerts-panel.tsx
    src/components/group-capacity-grid.tsx
    src/components/conversion-kpis.tsx
    src/components/message-feed.tsx
    src/components/system-status-card.tsx
  </files>
  <action>
  Read each component and fix layout/styling issues:

  1. **operacoes/page.tsx** — Review the two-column grid layout. Ensure:
     - Cards have consistent padding (p-4 or p-5), warm V2 borders (border-[#E8E4DD])
     - Section headings have proper hierarchy (h2/h3 with font-semibold)
     - Grid gaps are consistent (gap-4 or gap-6)
     - No cards are awkwardly sized or misaligned

  2. **ChipHealthGrid** — Ensure:
     - Uses colored dots (green/yellow/red/gray) NOT emoji for health status
     - Table columns have flexible widths (not fixed w-16/w-24 that clip text)
     - Chip names are not truncated — use min-w-0 + truncate only if necessary with title tooltip
     - Column headers: "Chip", "Status", "Enviados Hoje", "Limite", "Última Atividade"

  3. **CampaignProgressBars** — Ensure:
     - Campaign names visible (not clipped)
     - Progress bars have proper height and color coding (green=sent, blue=sending, gray=queued)
     - Percentage labels are readable

  4. **AlertsPanel** — Ensure:
     - Alert items have proper spacing
     - Severity indicators use colored dots/badges (not emoji)
     - Text not truncated

  5. **GroupCapacityGrid** — Ensure:
     - Group names visible
     - Capacity bars show current/max (e.g., "892/1024")
     - Status badges (active/full/archived) properly styled

  6. **ConversionKPIs** — Ensure:
     - KPI values are large and readable
     - Labels are descriptive
     - Trend indicators use colored text (green up, red down) not emoji

  7. **MessageFeed** — Ensure:
     - Columns use flexible widths (flex-1, min-w-0) instead of fixed w-16/w-24
     - Messages are not clipped
     - Timestamps readable
     - Direction indicators (inbound/outbound) use styled badges

  8. **SystemStatusCard** — Ensure:
     - TrafficLightIndicator uses modern CSS (flex/grid, not float-left + negative margin)
     - Status text is readable

  Apply V2 Editorial Light tokens consistently: warm bg-[#F8F6F1] for card backgrounds, border-[#E8E4DD] for borders, no emoji anywhere.
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/integration-evolution-wizard && npx next build 2>&1 | tail -5</automated>
    <manual>Operações page renders with consistent styling, no truncated text, no emoji indicators</manual>
  </verify>
  <done>
  - All 8 sub-components have consistent V2 Editorial Light card styling
  - No text truncation issues
  - No emoji indicators (replaced with colored dots/badges)
  - MessageFeed uses flexible column widths
  - SystemStatusCard uses modern CSS for traffic light
  - Layout is readable on 1024px+ screens
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all Operações components use real data from APIs</name>
  <files>
    src/app/operacoes/page.tsx
    src/components/next-actions-panel.tsx
    src/components/quick-actions-panel.tsx
    src/components/help-panel.tsx
  </files>
  <action>
  Audit every data source on the Operações page to verify real data connections:

  1. **Data flow audit** — The page fetches from 5 endpoints:
     - `/api/dashboard/operations` — chip health + campaign progress + alerts
     - `/api/dashboard/kpis` — conversion metrics
     - `/api/dashboard/messages` — message feed
     - `/api/dashboard/groups` — group capacity
     - `/api/voters?limit=1` — voter total count
     
     For each endpoint: verify it exists, returns real DB data (not hardcoded), and the component correctly maps the response fields.

  2. **NextActionsPanel** — Verify it derives actions from real system state (chips offline count, groups near capacity, campaigns stalled). If using hardcoded suggestions, update to read from the API response data already fetched by the parent.

  3. **QuickActionsPanel** — Verify all action links point to valid routes. If it shows contextual counts (e.g., "3 chips offline"), verify those come from real data.

  4. **HelpPanel** — Verify:
     - Support email is real (not suporte@exemplo.com) — if no real email exists, use a placeholder that's clearly marked as configurable
     - Help links point to actual documentation or valid internal routes
     - FAQ content is relevant to the electoral campaign context

  5. **Auto-refresh verification** — The page auto-refreshes every 10s with visibility guard. Verify this is working (the useEffect with setInterval + visibilitychange listener pattern).

  If any component uses hardcoded/mock data, replace with the real data already available from the parent page's API fetches.
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/integration-evolution-wizard && npx next build 2>&1 | tail -5</automated>
    <manual>All Operações components show real data, NextActionsPanel derives from system state, no placeholder emails</manual>
  </verify>
  <done>
  - All 5 API endpoints verified as returning real DB data
  - NextActionsPanel derives suggestions from real system state
  - QuickActionsPanel links all point to valid routes
  - HelpPanel has appropriate content (no placeholder emails/links)
  - Auto-refresh working with visibility guard
  - No hardcoded/mock data in any component
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Operações page loads with all components rendering consistently
- No text truncation, no emoji indicators, real data throughout
</verification>

<success_criteria>
Operações page is polished, consistent, and fully connected to real data APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/37-operacoes-layout-data/37-01-SUMMARY.md`
</output>
