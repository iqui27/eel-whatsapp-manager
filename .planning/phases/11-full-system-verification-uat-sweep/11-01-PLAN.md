---
phase: 11-full-system-verification-uat-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md
  - .planning/phases/11-full-system-verification-uat-sweep/11-01-SUMMARY.md
autonomous: true
requirements: [QA-01, QA-02]

must_haves:
  truths:
    - "Phase 11 is a verification phase: execution records reality and blockers before proposing fixes."
    - "Auth, setup, and legacy operational surfaces must be tested because they remain shipped entry points and can regress independently of the electoral modules."
    - "The output of this plan is evidence-driven PASS/FAIL/BLOCKED coverage in a shared verification ledger."
  artifacts:
    - path: ".planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md"
      provides: "Centralized verification ledger for baseline, access, setup, and operational modules"
      contains: "PASS|FAIL|BLOCKED"
    - path: ".planning/phases/11-full-system-verification-uat-sweep/11-01-SUMMARY.md"
      provides: "Execution summary for the baseline/auth/legacy sweep"
      contains: "login|setup|settings|chips|contacts|clusters|history"
  key_links:
    - from: ".planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md"
      to: "src/app/login/page.tsx"
      via: "the ledger must capture real login/logout/protected-route behavior"
      pattern: "login"
    - from: ".planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md"
      to: "src/app/setup/page.tsx"
      via: "setup and settings are validated before deeper feature-level UAT begins"
      pattern: "setup"
---

<objective>
Establish a trustworthy baseline for the full-system verification sweep by validating access control, setup/state prerequisites, and the legacy operational modules that still ship in the current shell.

Purpose: if auth, settings, chip readiness, or older operational pages are broken, later campaign/chat UAT produces noisy or misleading results. This plan proves the baseline first and records evidence in a shared verification ledger.

Output: `11-VERIFICATION.md` contains PASS/FAIL/BLOCKED evidence for login/logout, protected-route behavior, setup/settings, and the legacy operational surfaces.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-full-system-verification-uat-sweep/11-CONTEXT.md
@.planning/phases/11-full-system-verification-uat-sweep/11-RESEARCH.md
@.planning/phases/11-full-system-verification-uat-sweep/11-VALIDATION.md
@src/app/login/page.tsx
@src/app/setup/page.tsx
@src/app/settings/page.tsx
@src/app/chips/page.tsx
@src/app/contacts/page.tsx
@src/app/clusters/page.tsx
@src/app/history/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize the shared verification ledger and baseline prerequisites</name>
  <files>.planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md</files>
  <action>
  Prepare the execution artifact before touching feature flows:

  1. Use `11-VERIFICATION.md` as the single ledger for this phase.
  2. Confirm it contains sections for:
     - auth/session baseline
     - setup/settings baseline
     - legacy operational modules
     - blockers and environment assumptions
  3. Record the exact environment being tested (local, production-like, or production), the available auth/session state, and any safe-test-data constraints.
  4. Do not leave any covered surface implicit; if a surface cannot be exercised, mark it `BLOCKED` with the reason.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <manual>Review `11-VERIFICATION.md` and confirm the baseline/auth/legacy sections exist before execution continues.</manual>
  </verify>
  <done>The phase has one authoritative verification ledger with explicit baseline sections and environment assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Verify access control, setup, and legacy operational modules</name>
  <files>.planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md</files>
  <action>
  Execute and document the baseline sweep:

  1. Verify `/login`, protected-route access, and logout behavior.
  2. Verify `/setup` and `/settings` load and persist correctly enough to support later flows.
  3. Verify the legacy operational pages and their critical actions:
     - `/chips`
     - `/contacts`
     - `/clusters`
     - `/history`
  4. Where useful, pair UI checks with safe API smoke checks (`/api/chips`, `/api/contacts`, `/api/clusters`, `/api/warming`, `/api/logs`) to confirm auth/error behavior.
  5. Record PASS/FAIL/BLOCKED results with the exact route or API and brief evidence notes.
  6. Do not broaden into bug fixing; this plan is about capturing baseline truth.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit && npm run build</automated>
    <manual>Confirm the ledger contains an explicit result for every baseline surface and no shipped operational page was silently skipped.</manual>
  </verify>
  <done>The verification ledger contains evidence-backed results for auth, setup, and all legacy operational surfaces.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit`
- `npm run build`
- `11-VERIFICATION.md` records PASS/FAIL/BLOCKED for `/login`, protected routes, `/setup`, `/settings`, `/chips`, `/contacts`, `/clusters`, and `/history`
- Any environment prerequisite missing from this baseline is documented before Plans 11-02 and 11-03 begin
</verification>

<success_criteria>
- Later UAT work starts from a known-good or explicitly-blocked baseline instead of assumptions.
- All shipped operational modules still exposed in the shell are accounted for.
- The verification ledger can be used as the canonical evidence source for the remainder of the phase.
</success_criteria>
