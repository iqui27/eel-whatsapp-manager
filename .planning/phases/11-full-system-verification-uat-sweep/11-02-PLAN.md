---
phase: 11-full-system-verification-uat-sweep
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - .planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md
  - .planning/phases/11-full-system-verification-uat-sweep/11-02-SUMMARY.md
autonomous: true
requirements: [QA-03, QA-04]

must_haves:
  truths:
    - "The electoral core must be validated as real user workflows, not as isolated page opens."
    - "Verification must stay safe: campaign send and schedule checks use tightly scoped test data and avoid uncontrolled real-audience actions."
    - "Cross-page handoffs among import, segmentation, CRM, and campaigns are part of the product contract and must be explicitly tested."
  artifacts:
    - path: ".planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md"
      provides: "Centralized evidence for the electoral core flow"
      contains: "import|segment|crm|campaign|schedule|monitor"
    - path: ".planning/phases/11-full-system-verification-uat-sweep/11-02-SUMMARY.md"
      provides: "Execution summary for the electoral core UAT block"
      contains: "segmentacao|crm|campanhas"
  key_links:
    - from: ".planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md"
      to: "src/app/segmentacao/importar/page.tsx"
      via: "verification records the import-to-segmentation handoff"
      pattern: "import"
    - from: ".planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md"
      to: "src/app/campanhas/nova/page.tsx"
      via: "verification records the segment-to-campaign and CRM-to-campaign handoffs"
      pattern: "campanhas"
---

<objective>
Validate the core electoral workflow from data entry and targeting through campaign lifecycle actions, using safe verification data and documenting every outcome in the shared ledger.

Purpose: this is the heart of the shipped electoral product. Import, segmentation, CRM, campaigns, scheduling, and monitoring now rely on real database-backed behavior from Phase 09, so they require an end-to-end UAT pass instead of isolated code inspection.

Output: `11-VERIFICATION.md` contains PASS/FAIL/BLOCKED evidence for the electoral core workflow, including cross-page deep links and campaign lifecycle checkpoints.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-full-system-verification-uat-sweep/11-CONTEXT.md
@.planning/phases/11-full-system-verification-uat-sweep/11-RESEARCH.md
@.planning/phases/11-full-system-verification-uat-sweep/11-VALIDATION.md
@.planning/phases/11-full-system-verification-uat-sweep/11-01-PLAN.md
@src/app/segmentacao/importar/page.tsx
@src/app/segmentacao/page.tsx
@src/app/crm/page.tsx
@src/app/crm/[id]/page.tsx
@src/app/campanhas/page.tsx
@src/app/campanhas/nova/page.tsx
@src/app/campanhas/[id]/editar/page.tsx
@src/app/campanhas/[id]/agendar/page.tsx
@src/app/campanhas/[id]/monitor/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify import, segmentation, CRM, and contextual handoffs</name>
  <files>.planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md</files>
  <action>
  Execute the data/targeting half of the electoral workflow:

  1. Verify `/segmentacao/importar` with a small safe CSV:
     - upload
     - field mapping
     - validation/preview
     - processing result
  2. Verify `/segmentacao`:
     - live filter options
     - audience preview
     - save, edit, and delete segment behavior
     - materialized audience expectations stay coherent
  3. Verify `/crm` and `/crm/[id]`:
     - search
     - pagination
     - add/delete or edit actions available in the current environment
     - profile rendering and not-found handling
  4. Verify the deep links from CRM into:
     - `/conversas?voterId=...`
     - campaign creation context
  5. Record PASS/FAIL/BLOCKED evidence with enough detail to reproduce any issue later.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <manual>Confirm the ledger captures import, segmentation, CRM, and both deep-link handoffs as separate verification outcomes.</manual>
  </verify>
  <done>The verification ledger contains evidence-backed results for the import-to-targeting-to-CRM workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Verify campaign creation, edit, scheduling, send, and monitor behavior</name>
  <files>.planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md</files>
  <action>
  Execute the campaign lifecycle half of the electoral workflow:

  1. Verify `/campanhas` list rendering, filters, and segment-name resolution.
  2. Verify `/campanhas/nova`:
     - create flow
     - template/variable behavior
     - chip selection
     - segment selection or voter-context handoff
  3. Verify `/campanhas/[id]/editar` and read-only guards where applicable.
  4. Verify `/campanhas/[id]/agendar` with safe constraints and confirm:
     - the persisted schedule state matches operator intent
     - any exposed time-window/velocity controls map coherently to the saved behavior
  5. Verify `/campanhas/[id]/monitor` against the created/scheduled/sent campaign state and confirm:
     - timeline and counters behave coherently
     - cancel-related UX matches actual send-state behavior as closely as the environment allows
  6. If environment permits a tightly scoped safe send, verify the `/api/campaigns/[id]/send` path without affecting uncontrolled real data, including whether the selected chip is the one actually used.
  7. Record which steps were fully passed, blocked by environment, or failed functionally.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit && npm run build</automated>
    <manual>Review the ledger and confirm the campaign lifecycle is covered from list/create through schedule/send/monitor, with safe-data notes where relevant.</manual>
  </verify>
  <done>The verification ledger contains an evidence-backed campaign lifecycle result, including schedule/send/monitor checkpoints.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit`
- `npm run build`
- `11-VERIFICATION.md` records PASS/FAIL/BLOCKED for `/segmentacao/importar`, `/segmentacao`, `/crm`, `/crm/[id]`, `/campanhas`, `/campanhas/nova`, `/campanhas/[id]/editar`, `/campanhas/[id]/agendar`, and `/campanhas/[id]/monitor`
- Safe-data constraints for schedule/send checks are written down explicitly
- Scheduling, cancellation, and chip-routing observations are written down explicitly if they do not match operator intent
</verification>

<success_criteria>
- The electoral product's main operator flow is validated as a connected workflow, not as isolated page opens.
- Any blocked or failing step in the campaign chain is documented with enough detail to become explicit gap work.
- Deep links and contextual handoffs between modules are covered, since they are common regression points.
</success_criteria>
