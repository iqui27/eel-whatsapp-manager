---
phase: 11-full-system-verification-uat-sweep
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - .planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md
  - .planning/phases/11-full-system-verification-uat-sweep/11-03-SUMMARY.md
autonomous: true
requirements: [QA-05, QA-06, QA-07, QA-08]

must_haves:
  truths:
    - "Realtime operator flows must be explicitly re-verified after the Phase 10 SSE migration and hotfixes."
    - "Governance, reporting, exports, and destructive safeguards count as shipped functionality and cannot be omitted from final verification."
    - "The last step of this phase is a clear verdict and routed gap list, not a vague statement that the app 'looks fine'."
  artifacts:
    - path: ".planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md"
      provides: "Final cross-product verification ledger and release-readiness verdict"
      contains: "dashboard|conversas|webhook|compliance|admin|relatorios|Final Verdict"
    - path: ".planning/phases/11-full-system-verification-uat-sweep/11-03-SUMMARY.md"
      provides: "Execution summary for realtime, governance, and final sign-off"
      contains: "SSE|reports|exports|gaps"
  key_links:
    - from: ".planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md"
      to: "src/app/conversas/page.tsx"
      via: "verification must capture queue, thread, and reconnect behavior after the SSE rollout"
      pattern: "conversas"
    - from: ".planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md"
      to: "src/app/relatorios/page.tsx"
      via: "verification must capture reporting/export behavior before final sign-off"
      pattern: "relatorios"
---

<objective>
Close the verification sweep by validating realtime operator behavior, governance/reporting surfaces, and cross-cutting regression criteria, then produce a clear final verdict with routed follow-up gaps.

Purpose: the last mile risk now lives in dashboard/chat realtime behavior, compliance/admin/reporting surfaces, and the final release-readiness pass. These areas are easy to skip and were affected directly or indirectly by the hardening work in Phases 09 and 10.

Output: `11-VERIFICATION.md` contains PASS/FAIL/BLOCKED evidence for realtime and governance surfaces plus a final verdict and gap list ready for milestone wrap-up or follow-up planning.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-full-system-verification-uat-sweep/11-CONTEXT.md
@.planning/phases/11-full-system-verification-uat-sweep/11-RESEARCH.md
@.planning/phases/11-full-system-verification-uat-sweep/11-VALIDATION.md
@.planning/phases/11-full-system-verification-uat-sweep/11-02-PLAN.md
@src/app/page.tsx
@src/components/ChatQueuePanel.tsx
@src/app/conversas/page.tsx
@src/app/compliance/page.tsx
@src/app/admin/page.tsx
@src/app/relatorios/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify realtime operator flows on dashboard and conversations</name>
  <files>.planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md</files>
  <action>
  Execute the realtime/operator verification block:

  1. Verify dashboard `/` KPI rendering and `ChatQueuePanel` behavior with current data.
  2. Verify `/conversas`:
     - queue rendering
     - active-thread rendering
     - empty state behavior
     - status/priority/handoff actions
     - new conversation creation
     - agent reply flow
  3. Explicitly verify post-Phase-10 SSE concerns:
     - queue updates appear without polling delay
     - thread updates appear without polling delay
     - reconnect behavior does not duplicate or destabilize UI state
     - unauthorized stream access still returns `401`
  4. Where feasible, trigger or simulate inbound activity through the existing webhook path and confirm it fans out into the persisted operator views; if duplicate protection cannot be proven safely, record that as residual risk.
  5. Record PASS/FAIL/BLOCKED evidence with exact routes and any environment limitations.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <manual>Confirm the ledger separately records dashboard queue behavior, `/conversas` behavior, SSE auth behavior, and webhook/reply propagation.</manual>
  </verify>
  <done>The verification ledger contains explicit realtime/operator results for dashboard, conversations, webhook ingress, and SSE behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Verify governance/reporting surfaces and produce the final verdict</name>
  <files>.planning/phases/11-full-system-verification-uat-sweep/11-VERIFICATION.md</files>
  <action>
  Finish the sweep and classify release readiness:

  1. Verify `/compliance`:
     - consent status cards
     - revoke/anonymize safeguards
     - timeline/export behavior
  2. Verify `/admin`:
     - users list
     - role updates
     - invite/remove safeguards
     - permissions matrix visibility
  3. Verify `/relatorios`:
     - KPI cards
     - charts/tables
     - CSV export path
  4. Run a final cross-cutting smoke pass on:
     - deep links between pages
     - empty states
     - KPI consistency across dashboard, monitor, and reports where the same underlying campaign data is shown
     - responsive narrow-width rendering on major surfaces
     - expected auth/error behavior on critical APIs
  5. Complete `11-VERIFICATION.md` with:
     - final verdict
     - release blockers, if any
     - routed gap list for follow-up phases or decimal inserts
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit && npm run build</automated>
    <manual>Review the final ledger and confirm it ends with a clear verdict plus explicit release blockers/gaps rather than open-ended notes.</manual>
  </verify>
  <done>The phase finishes with an evidence-backed verdict covering realtime, governance, reporting, and cross-cutting release-readiness checks.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit`
- `npm run build`
- `11-VERIFICATION.md` records PASS/FAIL/BLOCKED for dashboard queue, `/conversas`, `/api/conversations/stream`, `/api/webhook`, `/compliance`, `/admin`, and `/relatorios`
- The verification ledger ends with a final verdict and routed gap list
</verification>

<success_criteria>
- The product has one explicit release-readiness verdict grounded in evidence across all shipped surface areas.
- Realtime chat regressions introduced by the SSE migration are either cleared or documented as gaps.
- Governance, reporting, exports, and cross-cutting regressions are not left unverified.
</success_criteria>
