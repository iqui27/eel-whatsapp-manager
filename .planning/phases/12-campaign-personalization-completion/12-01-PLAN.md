---
phase: 12-campaign-personalization-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle/0003_phase_12_campaign_personalization.sql
  - src/db/schema.ts
  - src/lib/db-config.ts
  - src/app/api/settings/route.ts
  - src/app/settings/page.tsx
  - src/lib/campaign-variables.ts
autonomous: true
requirements: [PERS-01, PERS-02]

must_haves:
  truths:
    - "Candidate information must have a real persisted source of truth before `{candidato}` can be considered a supported production variable."
    - "Campaign variable definitions cannot remain duplicated across pages; the editor and backend must read from one registry."
    - "The existing stack and settings/config model remain the base architecture for this phase."
  artifacts:
    - path: "src/lib/campaign-variables.ts"
      provides: "Shared campaign variable registry, extraction helpers, preview/runtime context builders, and validation helpers"
      contains: "SUPPORTED_CAMPAIGN_VARIABLES|extractVariables|validateTemplateVariables"
    - path: "src/app/settings/page.tsx"
      provides: "Operator-facing candidate profile configuration"
      contains: "candidate|candidato"
    - path: "src/app/api/settings/route.ts"
      provides: "Read/write persistence for candidate profile fields"
      contains: "candidate"
  key_links:
    - from: "src/app/settings/page.tsx"
      to: "src/app/api/settings/route.ts"
      via: "settings UI saves and reloads the candidate profile through the authenticated settings API"
      pattern: "candidate"
    - from: "src/lib/campaign-variables.ts"
      to: "src/db/schema.ts"
      via: "the shared registry understands which placeholders depend on voter data vs candidate config"
      pattern: "candidato|zona|secao"
---

<objective>
Establish the data and helper foundation for campaign personalization so the rest of Phase 12 can build on a real candidate profile and a single variable contract.

Purpose: today the product exposes `{candidato}` in the campaign editor without any persisted candidate config, while variable definitions are duplicated and inconsistent across UI and backend. This plan creates the source of truth that later plans will consume.

Output: settings/config supports candidate profile fields, and the codebase has one shared module that defines the campaign variable contract.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-campaign-personalization-completion/12-CONTEXT.md
@.planning/phases/12-campaign-personalization-completion/12-RESEARCH.md
@.planning/phases/12-campaign-personalization-completion/12-VALIDATION.md
@src/db/schema.ts
@src/lib/db-config.ts
@src/app/api/settings/route.ts
@src/app/settings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend config/settings with a real candidate profile</name>
  <files>drizzle/0003_phase_12_campaign_personalization.sql, src/db/schema.ts, src/lib/db-config.ts, src/app/api/settings/route.ts, src/app/settings/page.tsx</files>
  <action>
  Add the missing candidate source of truth to the existing settings flow:

  1. Extend the config schema and migration layer with candidate profile fields appropriate for the current product scope.
  2. Keep the settings model backward-compatible for already-configured environments.
  3. Add a dedicated settings section where operators can save and review candidate information.
  4. Ensure the authenticated settings API reads and writes the new fields without exposing unrelated secrets.
  5. Make the UI clearly communicate which field feeds campaign personalization.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <manual>Save candidate profile fields in `/settings`, reload the page, and confirm the values round-trip correctly.</manual>
  </verify>
  <done>The product has a persisted candidate profile that can serve as the source of truth for campaign personalization.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce one shared campaign variable registry</name>
  <files>src/lib/campaign-variables.ts, src/app/settings/page.tsx, src/app/api/settings/route.ts</files>
  <action>
  Create the contract every later campaign surface must reuse:

  1. Define the supported placeholder list in one shared module.
  2. Categorize each variable by its source:
     - voter-backed
     - candidate-backed
     - schedule/runtime-backed
  3. Add extraction and validation helpers so other layers can ask:
     - which variables are present in a template
     - whether any variable is unsupported
     - whether the current environment/config can resolve all required values
  4. Add preview-value helpers or metadata so the editor can render realistic substitutions without inventing a second contract.
  5. Include existing backend-supported variables like `{zona}` and `{secao}` in the canonical set.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Review the registry and confirm there is exactly one canonical definition for all supported campaign variables.</manual>
  </verify>
  <done>The codebase has one shared campaign-variable contract that later plans can consume without duplicating logic.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit`
- `npm run build`
- `/settings` saves and reloads candidate profile data successfully
- `src/lib/campaign-variables.ts` is the only canonical source for supported campaign placeholders
</verification>

<success_criteria>
- `{candidato}` is no longer a mock-only concept; there is a real persisted configuration source for it.
- Future campaign UI/backend work can import one shared variable registry instead of defining local arrays.
- The phase is grounded on existing infrastructure rather than introducing a parallel settings model.
</success_criteria>
