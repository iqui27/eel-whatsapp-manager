---
phase: 12-campaign-personalization-completion
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - src/lib/campaign-delivery.ts
  - src/app/api/campaigns/[id]/send/route.ts
  - src/app/api/cron/campaigns/route.ts
  - src/app/campanhas/[id]/agendar/page.tsx
  - src/lib/campaign-variables.ts
autonomous: true
requirements: [PERS-03, PERS-05]

must_haves:
  truths:
    - "The delivery pipeline must resolve exactly the same supported placeholders that the editor preview advertises."
    - "Manual sends and scheduled sends must share the same interpolation and validation logic."
    - "Phase 12 is not done until UAT proves that no raw placeholders leak into outbound content."
  artifacts:
    - path: "src/lib/campaign-delivery.ts"
      provides: "Runtime campaign template resolution with candidate/schedule/voter context"
      contains: "resolveCampaignTemplate|candidato|data"
    - path: "src/app/api/campaigns/[id]/send/route.ts"
      provides: "Pre-send validation and operator-safe error surfacing"
      contains: "placeholder|variable"
    - path: "src/app/api/cron/campaigns/route.ts"
      provides: "Scheduled-send parity with the same validation and interpolation path"
      contains: "executeCampaignSend"
  key_links:
    - from: "src/lib/campaign-delivery.ts"
      to: "src/lib/campaign-variables.ts"
      via: "delivery uses the same resolver semantics as the editor preview"
      pattern: "resolveCampaignTemplate"
    - from: "src/app/api/campaigns/[id]/send/route.ts"
      to: "src/lib/campaign-delivery.ts"
      via: "manual dispatch blocks unresolved templates before provider calls"
      pattern: "send"
---

<objective>
Make outbound campaign execution match the editor exactly and close the phase with focused UAT on personalization integrity.

Purpose: the operator-visible problem is only fully solved when manual and scheduled sends resolve the same variables shown in the editor preview and reject anything unsupported or unconfigured before delivery.

Output: send-time interpolation is shared and deterministic, scheduled/manual sends behave the same, and the phase has evidence that placeholders do not leak into outbound messages.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-campaign-personalization-completion/12-CONTEXT.md
@.planning/phases/12-campaign-personalization-completion/12-RESEARCH.md
@.planning/phases/12-campaign-personalization-completion/12-VALIDATION.md
@src/lib/campaign-variables.ts
@src/lib/campaign-delivery.ts
@src/app/api/campaigns/[id]/send/route.ts
@src/app/api/cron/campaigns/route.ts
@src/app/campanhas/[id]/agendar/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify manual and scheduled delivery interpolation</name>
  <files>src/lib/campaign-delivery.ts, src/app/api/campaigns/[id]/send/route.ts, src/app/api/cron/campaigns/route.ts, src/lib/campaign-variables.ts</files>
  <action>
  Replace the current ad hoc runtime interpolation with the shared contract:

  1. Make the send pipeline resolve templates through the shared campaign-variable helper instead of local string replacement.
  2. Support the full approved variable set, including candidate-backed and date-backed placeholders.
  3. Define and implement explicit `{data}` semantics for both immediate and scheduled execution.
  4. Ensure manual send and cron-triggered send both use the same code path and validation behavior.
  5. Fail fast with clear errors if a campaign still contains unresolved or unsupported placeholders at send time.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <manual>Run one safe manual send and one scheduled-send path review, confirming the resolved outbound content matches the preview contract.</manual>
  </verify>
  <done>The send pipeline uses the same personalization semantics as the editor and cannot leak unsupported placeholders silently.</done>
</task>

<task type="auto">
  <name>Task 2: Finish the phase with focused personalization UAT</name>
  <files>src/app/campanhas/[id]/agendar/page.tsx, src/lib/campaign-delivery.ts, src/lib/campaign-variables.ts</files>
  <action>
  Close the operator trust gap with targeted verification:

  1. Review the scheduling/continue flow for candidate-config warnings and invalid-template blocking.
  2. Verify the preview, saved template metadata, and actual resolved send content stay aligned.
  3. Ensure the phase evidence explicitly covers:
     - new campaign flow
     - edit existing campaign flow
     - schedule/continue flow
     - manual/scheduled send parity
  4. Capture any residual edge case as a routed follow-up instead of leaving it implicit.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Complete the focused UAT matrix from settings through send and confirm no raw placeholders remain in the delivered content or send logs.</manual>
  </verify>
  <done>Phase 12 has explicit evidence that campaign personalization is trustworthy end to end.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit`
- `npm run build`
- A valid campaign using `{candidato}` and `{data}` resolves correctly on actual dispatch
- Unknown placeholders are blocked before provider send calls
- Manual and scheduled sends share the same interpolation semantics
</verification>

<success_criteria>
- The operator sees the same personalization contract in settings, editor, preview, and delivery.
- No raw campaign placeholders leak into outbound WhatsApp content.
- Phase 12 ends with a focused UAT record rather than another implicit assumption.
</success_criteria>
