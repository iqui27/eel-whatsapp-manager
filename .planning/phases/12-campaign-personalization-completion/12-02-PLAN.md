---
phase: 12-campaign-personalization-completion
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/app/campanhas/nova/page.tsx
  - src/app/campanhas/[id]/editar/page.tsx
  - src/app/campanhas/[id]/agendar/page.tsx
  - src/app/api/campaigns/route.ts
  - src/lib/db-campaigns.ts
  - src/lib/campaign-variables.ts
autonomous: true
requirements: [PERS-02, PERS-04]

must_haves:
  truths:
    - "The campaign editor must not present variables the backend cannot resolve."
    - "Draft, edit, and schedule flows must know which placeholders a campaign actually uses."
    - "Validation errors must surface before dispatch, not after a send attempt."
  artifacts:
    - path: "src/app/campanhas/nova/page.tsx"
      provides: "New-campaign editor driven by the shared personalization contract"
      contains: "campaign-variables|preview|validation"
    - path: "src/app/campanhas/[id]/editar/page.tsx"
      provides: "Edit flow parity with the same shared variable/preview behavior"
      contains: "campaign-variables|preview|validation"
    - path: "src/app/api/campaigns/route.ts"
      provides: "Create/update validation and variables persistence"
      contains: "variables"
  key_links:
    - from: "src/app/campanhas/nova/page.tsx"
      to: "src/lib/campaign-variables.ts"
      via: "toolbar chips, preview data, and operator guidance all come from the shared contract"
      pattern: "SUPPORTED_CAMPAIGN_VARIABLES"
    - from: "src/app/api/campaigns/route.ts"
      to: "src/lib/db-campaigns.ts"
      via: "validated variable metadata is persisted with the campaign record"
      pattern: "variables"
---

<objective>
Align the campaign authoring surfaces with the shared personalization contract so the editor, preview, and saved campaign data all describe the same thing.

Purpose: after Plan 12-01 establishes the source of truth, this plan removes page-local mocks, exposes the right variable set to operators, and prevents invalid templates from quietly entering the campaign lifecycle.

Output: create/edit/schedule flows use the shared registry, preview realistic substitutions, and persist validated variable metadata into the campaign record.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-campaign-personalization-completion/12-CONTEXT.md
@.planning/phases/12-campaign-personalization-completion/12-RESEARCH.md
@.planning/phases/12-campaign-personalization-completion/12-VALIDATION.md
@src/lib/campaign-variables.ts
@src/app/campanhas/nova/page.tsx
@src/app/campanhas/[id]/editar/page.tsx
@src/app/campanhas/[id]/agendar/page.tsx
@src/app/api/campaigns/route.ts
@src/lib/db-campaigns.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace local campaign-editor mocks with the shared personalization contract</name>
  <files>src/app/campanhas/nova/page.tsx, src/app/campanhas/[id]/editar/page.tsx, src/lib/campaign-variables.ts</files>
  <action>
  Update both campaign editor pages so the UI reflects the real contract:

  1. Remove the page-local variable arrays and import the shared registry instead.
  2. Render toolbar chips and labels from the canonical supported-variable list.
  3. Make preview values come from the shared preview builder so `{candidato}` and `{data}` behave consistently.
  4. Surface operator-facing guidance when a template references candidate-backed variables but candidate config is missing.
  5. Preserve the current UX quality bar: variable insertion, preview, CTA score, and edit-flow parity must all keep working.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <manual>Open `/campanhas/nova` and `/campanhas/[id]/editar` and confirm both show the same variable set, including `{zona}` and `{secao}` if supported.</manual>
  </verify>
  <done>Campaign authoring surfaces no longer rely on page-local mock variables or hardcoded preview values.</done>
</task>

<task type="auto">
  <name>Task 2: Validate and persist campaign variable usage on create/edit/schedule</name>
  <files>src/app/api/campaigns/route.ts, src/lib/db-campaigns.ts, src/app/campanhas/[id]/agendar/page.tsx, src/lib/campaign-variables.ts</files>
  <action>
  Make campaign persistence aware of the template contract:

  1. On create/update, extract the variables actually referenced by the template.
  2. Reject unsupported placeholders instead of saving them silently.
  3. Persist the extracted variable list into `campaigns.variables`.
  4. Ensure the schedule/continue path respects the same validation rules instead of bypassing them.
  5. Return clear API/UI errors when a template is invalid or depends on missing candidate configuration.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Create a draft with valid placeholders, edit it, and confirm the saved campaign still reflects the expected variable set and validation behavior.</manual>
  </verify>
  <done>The saved campaign record accurately represents which variables it uses, and invalid templates are blocked before dispatch.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit`
- `npm run build`
- `/campanhas/nova` and `/campanhas/[id]/editar` expose the same supported variables
- Saving or scheduling a campaign with an unsupported placeholder returns a clear validation error
- `campaigns.variables` reflects the placeholders actually used by the template
</verification>

<success_criteria>
- Operators can trust the editor toolbar and preview as the canonical personalization contract.
- Draft/edit/schedule flows persist validated variable metadata instead of losing that context.
- Unsupported or unconfigured placeholders are surfaced before the campaign reaches delivery.
</success_criteria>
