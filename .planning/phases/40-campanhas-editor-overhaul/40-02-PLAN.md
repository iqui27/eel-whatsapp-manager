---
phase: 40-campanhas-editor-overhaul
plan: 02
type: execute
wave: 3
depends_on: ["39-02", "40-01"]
files_modified:
  - src/app/campanhas/nova/page.tsx
  - src/app/campanhas/[id]/editar/page.tsx
  - src/components/whatsapp-format-toolbar.tsx
autonomous: true
requirements: [EDIT-04, EDIT-05, EDIT-06, EDIT-07]

must_haves:
  truths:
    - "Campaign editor has a WhatsApp formatting toolbar (bold, italic, strikethrough, monospace)"
    - "Gemini AI assistant is integrated into the campaign creation/edit pages"
    - "Message validation warns about unsupported formatting or overly long messages"
    - "Data validation prevents saving campaigns with empty templates or invalid date ranges"
  artifacts:
    - path: "src/components/whatsapp-format-toolbar.tsx"
      provides: "WhatsApp formatting toolbar for text editor"
      exports: ["WhatsAppFormatToolbar"]
    - path: "src/app/campanhas/nova/page.tsx"
      provides: "Enhanced campaign creation page with AI + formatting"
      contains: "GeminiMessageAssistant"
  key_links:
    - from: "src/app/campanhas/nova/page.tsx"
      to: "src/components/gemini-message-assistant.tsx"
      via: "import GeminiMessageAssistant"
      pattern: "GeminiMessageAssistant"
    - from: "src/app/campanhas/nova/page.tsx"
      to: "src/components/whatsapp-format-toolbar.tsx"
      via: "import WhatsAppFormatToolbar"
      pattern: "WhatsAppFormatToolbar"
---

<objective>
Integrate the Gemini AI assistant and a WhatsApp formatting toolbar into the campaign editor pages — operators can format text with bold/italic/strikethrough and get AI help writing messages, with proper data validation before save.

Purpose: Campaign editors need rich text formatting support (WhatsApp style) and AI assistance. This plan wires the previously-created components into the actual campaign pages and adds data validation.

Output: Campaign create/edit pages with formatting toolbar, AI assistant, and validation.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-campanhas-whatsapp-foundation/39-02-SUMMARY.md
@.planning/phases/40-campanhas-editor-overhaul/40-01-SUMMARY.md
@src/app/campanhas/nova/page.tsx
@src/app/campanhas/[id]/editar/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WhatsApp formatting toolbar</name>
  <files>src/components/whatsapp-format-toolbar.tsx</files>
  <action>
  Create src/components/whatsapp-format-toolbar.tsx — a toolbar that inserts WhatsApp formatting markers into a textarea:

  **Props:**
  ```typescript
  interface WhatsAppFormatToolbarProps {
    textareaRef: React.RefObject<HTMLTextAreaElement>;
    onTextChange: (newText: string) => void;
    currentText: string;
    className?: string;
  }
  ```

  **Toolbar buttons (horizontal row above textarea):**
  - **Bold** (B icon, bold weight) — wraps selected text with `*text*` or inserts `**` at cursor
  - **Italic** (I icon, italic) — wraps with `_text_` or inserts `__`
  - **Strikethrough** (S with line through) — wraps with `~text~` or inserts `~~`
  - **Monospace** (code icon `</>`) — wraps with `` `text` `` or inserts ``` `` ```
  - **Separator** (visual divider)
  - **Variable chips** — dropdown showing available variables: {nome}, {bairro}, {candidato}, {data}, {zona}, {secao}
    - Clicking a variable inserts it at cursor position
    - Import variable list from campaign-variables.ts SUPPORTED_CAMPAIGN_VARIABLES

  **Behavior:**
  - When text is selected in the textarea, clicking a format button wraps the selection
  - When no text is selected, clicking a format button inserts opening+closing markers at cursor, places cursor between them
  - After insertion, refocus the textarea and update cursor position
  - Use `textareaRef.current.selectionStart` and `selectionEnd` for cursor management
  - Call `onTextChange(newText)` after every modification

  **Styling:**
  - Compact toolbar: height ~36px, warm background matching V2 tokens
  - Buttons: icon-only with tooltips, 28x28, hover:bg-accent
  - Active state: if cursor is inside a formatting pair, highlight the corresponding button
  - Variable dropdown: shadcn DropdownMenu

  Use 'use client'. Use lucide-react for icons (Bold, Italic, Strikethrough, Code2, Variable).
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/integration-evolution-wizard && npx next build 2>&1 | tail -5</automated>
    <manual>WhatsAppFormatToolbar renders buttons that wrap selected text with formatting markers</manual>
  </verify>
  <done>
  - WhatsAppFormatToolbar renders bold/italic/strikethrough/monospace buttons
  - Wraps selected text with appropriate markers
  - Inserts markers at cursor when no selection
  - Variable dropdown inserts campaign variables
  - Compact styling matching V2 tokens
  - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate AI assistant + formatting toolbar + validation into campaign pages</name>
  <files>
    src/app/campanhas/nova/page.tsx
    src/app/campanhas/[id]/editar/page.tsx
  </files>
  <action>
  Wire GeminiMessageAssistant and WhatsAppFormatToolbar into the campaign create and edit pages:

  1. **Campaign create page (nova/page.tsx):**
     - Add a `ref` to the message textarea: `const textareaRef = useRef<HTMLTextAreaElement>(null)`
     - Render `<WhatsAppFormatToolbar textareaRef={textareaRef} onTextChange={setTemplate} currentText={template} />` directly above the textarea
     - Render `<GeminiMessageAssistant currentMessage={template} onInsertMessage={setTemplate} candidateName={config?.candidateName} segmentDescription={selectedSegment?.name} />` below the textarea (or in a right panel if space allows)
     - The `onInsertMessage` callback from GeminiMessageAssistant should update the template state and trigger a preview update

  2. **Campaign edit page (editar/page.tsx):**
     - Same integration as create page
     - For read-only campaigns (sent/sending), hide the toolbar and AI assistant

  3. **Data validation before save:**
     Add validation checks before the campaign is saved/scheduled:
     - Template must not be empty (show error: "Mensagem não pode estar vazia")
     - Template must not exceed 65,536 chars (show error: "Mensagem excede o limite do WhatsApp")
     - If startDate and endDate are set, endDate must be after startDate (show error: "Data de fim deve ser posterior à data de início")
     - If scheduling, scheduledAt must be in the future (show error: "Data de agendamento deve ser no futuro")
     - Campaign name must be 3-100 chars (show error: "Nome deve ter entre 3 e 100 caracteres")
     - Use toast.error for validation messages (sonner)
     - Prevent form submission until all validations pass

  4. **WhatsApp preview live update:**
     - Ensure the WhatsAppPreview component updates in real-time as formatting toolbar inserts markers
     - The formatted text (*bold*, _italic_) should render correctly in the preview panel

  5. **Layout adjustment:**
     - Editor area: textarea with toolbar above it + AI assistant below
     - Preview area: WhatsAppPreview component (right side in split-pane)
     - Ensure the split-pane layout works on medium screens (stack vertically below lg breakpoint)
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/integration-evolution-wizard && npx next build 2>&1 | tail -5</automated>
    <manual>Campaign create page has formatting toolbar, AI assistant, live preview, and validation</manual>
  </verify>
  <done>
  - Campaign create page has WhatsAppFormatToolbar above textarea
  - Campaign create page has GeminiMessageAssistant below textarea
  - Campaign edit page has same tools (hidden for read-only campaigns)
  - Data validation prevents saving invalid campaigns
  - Live preview updates with formatted text
  - Split-pane layout responsive
  - Build succeeds
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Campaign editor has formatting toolbar, AI assistant, and validation
- WhatsApp preview shows formatted text in real-time
</verification>

<success_criteria>
Campaign editor is fully enhanced with formatting, AI assistance, and validation.
</success_criteria>

<output>
After completion, create `.planning/phases/40-campanhas-editor-overhaul/40-02-SUMMARY.md`
</output>
