---
phase: 10-real-time-chat-via-sse
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - src/components/ChatQueuePanel.tsx
autonomous: true
requirements: [RT-04]

must_haves:
  truths:
    - "The dashboard queue panel stops using a 15-second polling loop and instead consumes the conversation SSE contract created earlier in the phase."
    - "The panel remains intentionally lightweight: it shows the open-conversation slice only and does not reimplement the full `/conversas` screen."
    - "Dashboard realtime support is additive and isolated to the chat queue panel, not a dashboard-wide subscription refactor."
  artifacts:
    - path: "src/components/ChatQueuePanel.tsx"
      provides: "Dashboard queue panel wired to initial REST bootstrap plus live SSE updates for open conversations"
      contains: "useConversationStream|status=open"
  key_links:
    - from: "src/components/ChatQueuePanel.tsx"
      to: "src/lib/use-conversation-stream.ts"
      via: "panel reuses the shared client hook without creating a second stream implementation"
      pattern: "useConversationStream"
    - from: "src/components/ChatQueuePanel.tsx"
      to: "src/app/api/conversations/stream/route.ts"
      via: "panel subscribes only to the open-queue slice of the authenticated SSE endpoint"
      pattern: "status=open"
---

<objective>
Extend the realtime work from `/conversas` into the dashboard queue panel so operators get fresher queue awareness on the home screen without reopening the full chat workspace.

Purpose: the dashboard panel is the secondary Phase 10 target. It should reuse the same stream contract and client hook already established for `/conversas`, while staying visually small and operationally non-intrusive.

Output: `ChatQueuePanel` boots from REST, subscribes to open-conversation SSE updates, and removes its 15-second polling interval.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-real-time-chat-via-sse/10-CONTEXT.md
@.planning/phases/10-real-time-chat-via-sse/10-RESEARCH.md
@.planning/phases/10-real-time-chat-via-sse/10-VALIDATION.md
@.planning/phases/10-real-time-chat-via-sse/10-02-PLAN.md
@src/components/ChatQueuePanel.tsx
@src/app/page.tsx
@src/app/conversas/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace dashboard queue polling with the shared SSE hook</name>
  <files>src/components/ChatQueuePanel.tsx</files>
  <action>
  Migrate the dashboard queue panel onto the Phase 10 realtime primitives:

  1. Keep the initial REST fetch so the panel still renders immediately on first dashboard load.
  2. Remove the `setInterval(fetchConversations, 15000)` polling loop.
  3. Subscribe the panel to the shared hook with the open-queue filter (`status=open`).
  4. Merge incoming conversation upserts into the existing panel state, preserving:
     - newest-first ordering
     - status badges
     - empty state behavior
     - the visible count badge
  5. Keep the panel limited to the open queue slice; do not expand it into assigned/bot tabs or full thread rendering.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <manual>Open the dashboard, trigger a new or updated open conversation, and confirm the queue panel count/list changes without waiting 15 seconds.</manual>
  </verify>
  <done>The dashboard panel receives live queue changes through the shared SSE path instead of a dedicated polling timer.</done>
</task>

<task type="auto">
  <name>Task 2: Keep dashboard realtime behavior quiet and resilient</name>
  <files>src/components/ChatQueuePanel.tsx</files>
  <action>
  Finish the dashboard-side migration without turning the panel into a noisy second chat client:

  1. Surface connection state only in a lightweight way if needed, for example subtle copy or a small status hint. Do not add toast spam for transient reconnects.
  2. Preserve the current "silently fail — non-critical panel" behavior philosophy: if the stream drops, the panel should remain readable and recover when possible.
  3. Ensure the panel still links cleanly to `/conversas` and does not own write actions, reply boxes, or extra control state.
  4. Keep the implementation contained to `ChatQueuePanel.tsx`; avoid unrelated dashboard layout refactors in `src/app/page.tsx` unless absolutely required by the existing component contract.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit && npm run build</automated>
    <manual>Simulate a reconnect or refresh while the dashboard is open and confirm the panel recovers without losing its empty/loading states or emitting noisy operator-facing errors.</manual>
  </verify>
  <done>The dashboard panel gains realtime behavior without regressing its lightweight, non-critical UX.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit`
- `npm run build`
- `src/components/ChatQueuePanel.tsx` no longer contains a 15-second polling interval
- The dashboard panel uses the shared SSE client hook with `status=open`
- The panel remains read-only and non-disruptive while reflecting live queue changes
</verification>

<success_criteria>
- The dashboard home screen shows open-conversation queue changes in near real time.
- The `ChatQueuePanel` implementation does not duplicate the EventSource lifecycle already introduced for `/conversas`.
- Dashboard realtime support stays isolated to the queue panel and does not expand the phase scope beyond chat surfaces.
</success_criteria>
