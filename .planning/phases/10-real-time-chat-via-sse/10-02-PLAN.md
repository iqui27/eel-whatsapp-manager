---
phase: 10-real-time-chat-via-sse
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/lib/use-conversation-stream.ts
  - src/app/conversas/page.tsx
autonomous: true
requirements: [RT-03]

must_haves:
  truths:
    - "The `/conversas` screen no longer depends on a 10s queue poll and a separate 5s message poll for normal operation."
    - "Initial REST bootstrap remains in place so the screen still works on first load and after hard refresh."
    - "Realtime state merges are duplicate-safe and preserve persisted ordering when optimistic sends reconcile with stream events."
  artifacts:
    - path: "src/lib/use-conversation-stream.ts"
      provides: "Client-side EventSource lifecycle, cursor tracking, reconnect/backoff, and stream status"
      contains: "EventSource|cursor|reconnecting|offline-fallback"
    - path: "src/app/conversas/page.tsx"
      provides: "Conversation queue and active-thread UI driven by SSE deltas instead of polling timers"
      contains: "/api/conversations/stream"
  key_links:
    - from: "src/app/conversas/page.tsx"
      to: "src/lib/use-conversation-stream.ts"
      via: "page consumes a shared client hook instead of managing raw polling timers"
      pattern: "useConversationStream"
    - from: "src/app/conversas/page.tsx"
      to: "src/app/api/conversations/[id]/messages/route.ts"
      via: "optimistic agent sends reconcile against persisted message events from the stream"
      pattern: "messages"
---

<objective>
Migrate the operator conversation workspace from timer-based refreshes to SSE-driven realtime updates while preserving the existing write flows and first-load bootstrap behavior.

Purpose: `/conversas` is the primary beneficiary of Phase 10. The queue list and the active thread should update from one stream contract, with reconnect logic and fallback behavior centralized in a small client abstraction rather than scattered across intervals.

Output: the `/conversas` page uses a shared EventSource hook, removes the queue/message polling loops, and updates the visible queue plus selected thread from SSE deltas.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-real-time-chat-via-sse/10-CONTEXT.md
@.planning/phases/10-real-time-chat-via-sse/10-RESEARCH.md
@.planning/phases/10-real-time-chat-via-sse/10-VALIDATION.md
@.planning/phases/10-real-time-chat-via-sse/10-01-PLAN.md
@src/app/conversas/page.tsx
@src/app/api/conversations/route.ts
@src/app/api/conversations/[id]/messages/route.ts
@src/components/ChatQueuePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a reusable client hook for the conversation SSE contract</name>
  <files>src/lib/use-conversation-stream.ts</files>
  <action>
  Add a focused client abstraction that turns the Phase 10 backend stream into React-friendly state signals:

  1. Create `src/lib/use-conversation-stream.ts` around `EventSource`.
  2. The hook should accept the filters needed by `/conversas`, at minimum:
     - `conversationId`
     - `status`
     - `voterId`
     - `enabled`
  3. Track the last acknowledged cursor so reconnect attempts can resume with `since`.
  4. Expose:
     - stream status (`connecting`, `live`, `reconnecting`, `offline-fallback`)
     - callbacks or merged state helpers for queue upserts and message inserts
     - retry/backoff behavior on disconnect
  5. Keep the hook generic enough that the dashboard queue panel can consume it later without editing this file again in Plan 10-03.
  6. Avoid toast spam and avoid introducing global state libraries for this phase.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit</automated>
    <manual>Inspect the hook API and confirm it can be reused by both the full conversations page and the dashboard queue panel without adding page-specific assumptions.</manual>
  </verify>
  <done>The codebase has one reusable client hook for the conversation SSE contract and reconnect lifecycle.</done>
</task>

<task type="auto">
  <name>Task 2: Replace `/conversas` polling with SSE-driven queue and thread updates</name>
  <files>src/app/conversas/page.tsx, src/lib/use-conversation-stream.ts</files>
  <action>
  Rewire the main operator chat screen to consume the new realtime transport:

  1. Keep the existing REST bootstrap for initial queue load, selected conversation load, and first message load so hard refreshes still render immediately.
  2. Remove the queue `setInterval(..., 10000)` loop.
  3. Remove the active-thread `setInterval(..., 5000)` loop.
  4. Subscribe the page to the stream once the initial data is loaded, using:
     - `voterId` when the page is filtered from CRM
     - `conversationId` for active-thread message deltas
     - no extra dashboard behavior in this plan
  5. Merge incoming events carefully:
     - queue updates reorder by persisted recency/priority
     - message events append only once by stable message id
     - selection state stays stable when other conversations update
  6. Preserve operator flows already on the page:
     - manual status changes
     - manual priority changes
     - creating a new conversation
     - sending an agent reply
  7. If the stream drops repeatedly, degrade to a clearly detectable fallback state without reintroducing the old permanent polling timers automatically.
  </action>
  <verify>
    <automated>node_modules/.bin/tsc --noEmit && npm run build</automated>
    <manual>Open `/conversas`, trigger an inbound webhook event or agent reply, and confirm the queue and active thread update without waiting for 10s/5s polling windows.</manual>
  </verify>
  <done>The primary chat workspace updates from SSE deltas, with bootstrap and reconnect behavior handled through the shared hook.</done>
</task>

</tasks>

<verification>
- `node_modules/.bin/tsc --noEmit`
- `npm run build`
- `src/app/conversas/page.tsx` no longer contains the queue or message polling intervals
- `/conversas` still supports `?voterId=` filtering while receiving live queue/thread updates
- Agent-send optimistic updates reconcile cleanly with persisted `message.created` events from the stream
</verification>

<success_criteria>
- Operators see new queue activity and thread messages in `/conversas` without waiting for timer-based refreshes.
- Reloading the page still works via the existing REST bootstrap before the stream settles.
- Reconnects do not duplicate messages or reset the selected conversation unnecessarily.
</success_criteria>
