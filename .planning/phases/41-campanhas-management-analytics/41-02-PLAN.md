---
phase: 41-campanhas-management-analytics
plan: 02
type: execute
wave: 4
depends_on: ["41-01", "40-02"]
files_modified:
  - src/app/campanhas/[id]/page.tsx
  - src/app/api/campaigns/[id]/analytics/route.ts
  - src/lib/gemini.ts
autonomous: true
requirements: [MGMT-06, MGMT-07, MGMT-08, MGMT-09]

must_haves:
  truths:
    - "Campaign detail page shows comprehensive campaign info with delivery analytics"
    - "Campaign detail page has real-time analytics with delivery funnel"
    - "Gemini provides campaign performance analysis with actionable recommendations"
    - "Campaign detail page is navigable from the campaign list"
  artifacts:
    - path: "src/app/campanhas/[id]/page.tsx"
      provides: "Enhanced campaign detail page with analytics and AI insights"
    - path: "src/app/api/campaigns/[id]/analytics/route.ts"
      provides: "Campaign analytics API endpoint"
  key_links:
    - from: "src/app/campanhas/[id]/page.tsx"
      to: "/api/campaigns/[id]/analytics"
      via: "fetch analytics data"
      pattern: "api/campaigns/.*/analytics"
    - from: "src/app/campanhas/[id]/page.tsx"
      to: "src/lib/gemini.ts"
      via: "via API call for AI analysis"
      pattern: "analyzeCampaignPerformance"
---

<objective>
Enhance the campaign detail page with comprehensive analytics, delivery funnel visualization, and Gemini AI performance analysis with actionable recommendations.

Purpose: The current campaign detail page (443 lines, server component) has basic ConversionFunnel, DeliveryTimeline, ChipBreakdown, and anti-ban stats. This plan adds Gemini AI analysis for performance insights and makes the page a proper campaign command center with real-time analytics.

Output: A rich campaign detail page with AI-powered insights.
</objective>

<execution_context>
@/Users/hrocha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/hrocha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/campanhas/[id]/page.tsx
@src/app/api/campaigns/[id]/analytics/route.ts
@.planning/phases/41-campanhas-management-analytics/41-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Gemini campaign analytics function and enhance analytics API</name>
  <files>
    src/lib/gemini.ts
    src/app/api/campaigns/[id]/analytics/route.ts
  </files>
  <action>
  1. **Gemini campaign analysis function** — Add to src/lib/gemini.ts:
     ```typescript
     export async function analyzeCampaignPerformance(params: {
       campaignName: string;
       totalSent: number;
       totalDelivered: number;
       totalRead: number;
       totalReplied: number;
       totalFailed: number;
       totalBlocked: number;
       messageTemplate: string;
       segmentDescription?: string;
       duration?: string; // how long the campaign has been running
     }): Promise<{
       overallScore: number;        // 0-100 performance score
       summary: string;             // "Campanha com bom desempenho, taxa de leitura acima da média"
       insights: string[];          // ["Taxa de resposta 15% acima da média para este segmento", ...]
       recommendations: string[];   // ["Considere enviar follow-up para quem leu mas não respondeu", ...]
       riskFactors: string[];       // ["Taxa de bloqueio alta (>3%), reduzir velocidade de envio", ...]
     }>
     ```

     System prompt:
     ```
     Você é um analista de campanhas eleitorais via WhatsApp.
     Analise os dados da campanha e forneça:
     1. Score geral (0-100) baseado em taxas de entrega, leitura, resposta e bloqueio
     2. Resumo executivo (1-2 frases)
     3. Insights específicos sobre o desempenho
     4. Recomendações acionáveis para melhorar resultados
     5. Fatores de risco que precisam atenção
     
     Benchmarks de referência (WhatsApp político):
     - Taxa de entrega: >95% é bom, <90% é preocupante
     - Taxa de leitura: >70% é bom, <50% é preocupante
     - Taxa de resposta: >10% é bom, >20% é excelente
     - Taxa de bloqueio: <1% é aceitável, >3% é crítico
     ```

  2. **Enhanced analytics API** — Update /api/campaigns/[id]/analytics/route.ts:
     - Add optional `?ai=true` query param
     - When ai=true, also call `analyzeCampaignPerformance()` with the campaign's stats
     - Return: `{ stats: {...existing}, ai?: { overallScore, summary, insights, recommendations, riskFactors } }`
     - Cache AI analysis for 5 minutes (don't re-call Gemini on every page load)
     - Use in-memory cache keyed by `${campaignId}:${totalSent}` (invalidates when stats change)
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/integration-evolution-wizard && npx next build 2>&1 | tail -5</automated>
    <manual>Analytics API returns AI analysis when ?ai=true</manual>
  </verify>
  <done>
  - analyzeCampaignPerformance() function exists in gemini.ts
  - Analytics API supports ?ai=true for AI insights
  - AI analysis is cached for 5 minutes
  - Portuguese system prompt with political campaign benchmarks
  - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance campaign detail page with analytics and AI insights</name>
  <files>src/app/campanhas/[id]/page.tsx</files>
  <action>
  Enhance the campaign detail page (src/app/campanhas/[id]/page.tsx, currently 443 lines server component):

  **Note:** This page is currently a server component. Convert it to a client component (add 'use client') to support interactive features and API fetching, OR keep server component and add a client sub-component for the interactive parts.

  1. **Campaign header section:**
     - Campaign name (large heading)
     - Status badge (colored, matching list page styling)
     - Activate/Pause button (same logic as list page)
     - Date range: "15 Mar - 30 Mar" if set
     - Created date + last updated date

  2. **Campaign info cards row:**
     - **Segmento:** Segment name as chip + audience count
     - **Chips:** Assigned chips as avatar pills with names
     - **Mensagem:** Template preview (first 200 chars) with "Ver completa" expandable
     - **Configuração:** Speed preset + time window + delays (from sendConfig)

  3. **Analytics section (existing but enhanced):**
     - Keep existing ConversionFunnel, DeliveryTimeline, ChipBreakdown components
     - Add KPI cards row: Total Enviados, Taxa Entrega, Taxa Leitura, Taxa Resposta, Taxa Bloqueio
     - Each KPI with large number + trend arrow + color indicator

  4. **AI Insights panel (NEW):**
     - Card with "Análise IA" header + sparkle icon
     - Overall score as a large number/gauge (0-100) with color coding (red < 40, yellow 40-70, green > 70)
     - Summary text (1-2 sentences)
     - Insights list with bullet points
     - Recommendations list with action icons
     - Risk factors list with warning icons
     - "Atualizar análise" button to refresh AI analysis
     - Loading skeleton while analysis is being generated

  5. **Navigation:**
     - Breadcrumb: Campanhas > {campaign name}
     - Quick links to: Editar, Monitorar, Mensagens, Agendar
     - Back button to campaign list

  6. **Auto-refresh:**
     - For sending campaigns: auto-refresh analytics every 30 seconds
     - Use visibility guard (don't refresh in background tabs)
     - Show "Atualizando..." indicator during refresh

  V2 Editorial Light styling throughout. No emoji.
  </action>
  <verify>
    <automated>cd /Users/hrocha/.superset/worktrees/chip-fire/integration-evolution-wizard && npx next build 2>&1 | tail -5</automated>
    <manual>Campaign detail page shows header, info cards, analytics, AI insights panel</manual>
  </verify>
  <done>
  - Campaign detail page has header with status + controls
  - Info cards show segment, chips, message preview, config
  - Analytics section with KPI cards + existing funnel/timeline/breakdown
  - AI Insights panel with score, summary, insights, recommendations, risk factors
  - Breadcrumb navigation + quick links
  - Auto-refresh for active campaigns
  - V2 Editorial Light styling
  - Build succeeds
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Campaign detail page renders all sections with real data
- AI insights panel shows Gemini analysis
</verification>

<success_criteria>
Campaign detail page is a comprehensive command center with analytics and AI-powered insights.
</success_criteria>

<output>
After completion, create `.planning/phases/41-campanhas-management-analytics/41-02-SUMMARY.md`
</output>
