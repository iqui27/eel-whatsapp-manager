---
phase: 260403-1ta-group-specific-lid-mapping-ui-with-inlin
plan: "1ta"
type: quick
wave: 1
depends_on: [260403-105]
files_modified:
  - src/app/api/groups/[id]/members/lid-mapping/route.ts
  - src/app/grupos/[id]/page.tsx
  - src/lib/db-lid-manual-mapping.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Clicking unmapped @lid member row opens mapping modal"
    - "Saving modal calls POST API which upserts mapping"
    - "After save, member row shows voterName and modal is pre-filled"
    - "Delete button in modal removes mapping via DELETE API"
  artifacts:
    - path: "src/app/api/groups/[id]/members/lid-mapping/route.ts"
      provides: "POST + DELETE for lid manual mapping CRUD"
      exports: ["POST", "DELETE"]
    - path: "src/lib/db-lid-manual-mapping.ts"
      provides: "deleteLidMapping function (currently missing)"
      exports: ["deleteLidMapping"]
  key_links:
    - from: "src/app/grupos/[id]/page.tsx"
      to: "/api/groups/[id]/members/lid-mapping"
      via: "POST fetch on modal save"
    - from: "src/app/api/groups/[id]/members/lid-mapping/route.ts"
      to: "db-lid-manual-mapping.ts"
      via: "upsertLidManualMapping / deleteLidMapping"
---

<objective>
Add group-specific @lid mapping UI with inline edit in the group members view. Clicking an unmapped @lid row opens a modal with name input → save → API upsert. Delete button removes mapping.
</objective>

<context>
@src/app/api/groups/[id]/members/route.ts — Members API already enriches participants with voterName (3-tier: voters → cache → manual → null)
@src/lib/db-lid-manual-mapping.ts — Has upsertLidManualMapping, missing deleteLidMapping
@src/app/grupos/[id]/page.tsx — Members tab renders participant rows, currently shows only phone + id
@src/db/schema.ts (lid_manual_mapping) — Table with groupJid, lidJid, voterName, voterId, notes, createdBy, timestamps
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deleteLidMapping to db-lid-manual-mapping.ts</name>
  <files>src/lib/db-lid-manual-mapping.ts</files>
  <action>
Add deleteLidManualMapping(groupJid: string, lidJid: string): Promise<void> function to src/lib/db-lid-manual-mapping.ts.
Implementation: db.delete(lidManualMapping).where(and(eq(lidManualMapping.groupJid, groupJid), eq(lidManualMapping.lidJid, lidJid)))
</action>
  <verify>grep -n "deleteLidMapping" src/lib/db-lid-manual-mapping.ts</verify>
  <done>deleteLidManualMapping exported and functional</done>
</task>

<task type="auto">
  <name>Task 2: Create POST+DELETE /api/groups/[id]/members/lid-mapping route</name>
  <files>src/app/api/groups/[id]/members/lid-mapping/route.ts</files>
  <action>
Create src/app/api/groups/[id]/members/lid-mapping/route.ts.

POST handler:
- requirePermission(request, 'campaigns.edit')
- Parse body: { lidJid: string, voterName: string, voterId?: string, notes?: string }
- Call upsertLidManualMapping(groupJid, lidJid, voterName, voterId, notes, createdBy)
- Return 200 on success

DELETE handler:
- requirePermission(request, 'campaigns.edit')
- Parse body: { lidJid: string }
- Call deleteLidManualMapping(groupJid, lidJid)
- Return 200 on success

Both handlers: extract groupJid from params.id, use requirePermission to get createdBy user name for audit trail.
</action>
  <verify>ls src/app/api/groups/\[id\]/members/lid-mapping/route.ts</verify>
  <done>POST and DELETE endpoints exist and call correct db functions</done>
</task>

<task type="auto">
  <name>Task 3: Add LidMappingModal to group members UI with inline edit</name>
  <files>src/app/grupos/[id]/page.tsx</files>
  <action>
Modify src/app/grupos/[id]/page.tsx members tab (lines 471-498):

1. In participant row (line 472-497):
   - Show voterName when present (p.voterName ? `+{p.phone} — {p.voterName}` : ...)
   - For @lid participants where voterName is null AND p.id.endsWith('@lid'):
     - Show "Não mapeado" badge in amber/warning color, row is clickable
     - Use onClick={() => openLidModal(p)} to trigger modal

2. Add state:
   - lidModalOpen: boolean
   - lidModalParticipant: Participant | null (the @lid row being edited)

3. Add LidMappingModal:
   - Dialog with p.id (@lid ID, read-only label)
   - Input for voterName (pre-fill if mapping exists)
   - Optional notes textarea
   - Save button → POST /api/groups/[id]/members/lid-mapping → reload members on success
   - Delete button (only if mapping exists, p.voterName !== null) → DELETE → reload on success
   - Use toast for success/error feedback

4. Pass onLidSaved callback from loadMembers to reload after mapping changes.

Import Dialog components from shadcn/ui (Dialog, DialogContent, etc.) — check existing imports in file for shadcn usage pattern.
</action>
  <verify>grep -n "LidMappingModal\|lidModal\|openLidModal" src/app/grupos/\[id\]/page.tsx</verify>
  <done>Unmapped @lid rows show 'Não mapeado' badge, clicking opens modal, save/delete updates API and reloads members</done>
</task>

</tasks>

<verification>
Manual test:
1. Go to /grupos → select a group with @lid members
2. Click Membros tab → observe unmapped @lid rows with "Não mapeado" badge
3. Click a @lid row → modal opens with @lid ID (read-only) and name input
4. Type a name and Save → row updates to show voterName
5. Reopen modal → Delete removes mapping, row returns to "Não mapeado"
</verification>

<success_criteria>
- deleteLidMapping exists in db-lid-manual-mapping.ts
- POST /api/groups/[id]/members/lid-mapping upserts mapping
- DELETE /api/groups/[id]/members/lid-mapping removes mapping
- Members tab @lid rows with null voterName show "Não mapeado" badge
- Clicking @lid row opens modal pre-filled if mapping exists
- Save → POST → member row shows voterName after reload
- Delete → DELETE → member row returns to "Não mapeado"
</success_criteria>

<output>
After completion, create `.planning/quick/260403-1ta-group-specific-lid-mapping-ui-with-inlin/260403-1ta-SUMMARY.md`
</output>
